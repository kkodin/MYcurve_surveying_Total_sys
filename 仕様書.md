# M_APP_005（推進力設計計算）仕様書・引継ぎメモ

最終更新: 2026-08-11　対象フォルダ: M_APP_029（M_APP_028から複製・継続。M_APP_028はM_APP_027から、M_APP_027はM_APP_026から、M_APP_026はM_APP_025から複製・継続）

このドキュメントは、これまでの開発内容を次の担当（人・AIとも）が把握できるようにまとめたものです。コード自体にはコメントで簡潔な理由しか書いていないため、**なぜそうなっているか**をここに残します。

**重要な読み方の注意（2026-08-10追記）:** この仕様書は「後から仕様が訂正・変更された箇所」を、古い章をそのまま残したまま新しい章を追記する形で書いてきました。そのため**Ctrl+F等で特定の章にピンポイントで着地すると、既に上書きされた古い仕様を「現状」だと誤解する**ことがあります。特に以下のテーマは複数回仕様が変わっているため、必ず**一番大きい章番号（＝一番新しい記述）を先に確認**してください。
- **円形立坑・バックロックの支圧壁幅Beff算出方法**: 35章→36章→37.1章→38章→47.7章→47.10章→47.11章の順に6回変わっている。**最終形は47.11章**（`computeBackrockPlacement()`の角当たり計算からBeffを逆算、`wallThickness`/`wallBw`は読取専用）。36章・37.1章の説明は現状と異なる。
- **支圧壁「⑤推進作業スペース照査」の必要スペース式**: 47.2章→47.4章→47.5章→47.9章の順に変わっている。**最終形は47.9章**（`残り管長+Lc2×2+管長l′+押輪+ストラット`）で、47.2〜47.5章が説明する入力欄`wallJackHeight`・`wallStrutClearance`は47.9章で削除済み（現在は存在しない）。
- **ジャッキ配置カードの名称**: 27〜32章は「多段ジャッキ配置（曲線造成用）」という用語を使っているが、**33章で誤りと判明し「ジャッキ配置（推力分散用）」に訂正済み**（下の目次の27〜30番の項目名は旧称のまま残っているので注意）。
- **測量タブの「予測点」機能（60章）**: 60章は60.1〜60.11まで11回改訂されており、章全体の中でも特に更新が多い。**最新の正しい仕様は次の通り**（各項目の詳細はカッコ内の節を参照）: 予測点の判定は役割欄の文字入力に依存せず先頭4点をKP1〜4として位置ベースで常に計算する（60.5章。60.1〜60.4章が説明する`parsePipeRole()`による役割文字列マッチングは廃止済みで現存しない）／予測点の座標式はKP2→KP1・KP4→KP3の方向ベクトルとKP1を起点にした延長が正しい（60.8章。60.1章の式は方向が逆・起点が誤りだった）／予測点の名称は「（測量本数+1）本目予測」（60.9章）／Excel出力「結果一覧」表は誤差(mm)と左右を分離した12列構成（60.7章。60.3章が説明する「左右誤差」結合11列は廃止済み）／埋め込みグラフは縦軸=X・横軸=Yに反転し測量結果の系列とラベル付き拡大図を持つ（60.11章。60.10章の初版は軸が数学標準向きのままで測量結果の表示も無かった）。

**次回再開する人へ:** 目次で全62章を一覧できる。61〜62章はM_APP_028で実施し、2026-08-11にコミット・push済み（最新コミット`6aa40ba`）。62章（線形ビジュアル表示の測量KPラベル修正・実測線/予測線）はBrowserペイン非表示のため実画面での目視確認がまだ済んでいない。次回、実機・実ブラウザでの目視確認から着手すること。54章以降（M_APP_027→M_APP_028→M_APP_029、2026-08-10〜11）の作業は大きく分けて次の通り。
- **54章**: Excel出力の測定誤差計算式を、行位置ベースのINDEX/MATCHから区分（区間名）をキーにしたVLOOKUP参照に再設計。
- **55〜58章**: 秦野市・水無第6・入江土木など計10件超の一次資料（実際の設計計算書）とアプリを突き合わせ、`calcTerzaghi()`の2バグ修正・区間別土質（BP距離ごとのγ/φ/C/N切替）の新規実装・BC点管種検討や支圧壁の未実装ギャップの洗い出し・PB/FJクッション材の調査を行った。
- **59章**: 地震時継手抜出照査（レベル1=0.23%／レベル2=1.5%地盤ひずみ）・PB/FJクッション材選定（σ_allow入力、播磨コンクリート工業のWジョイント管許容応力度を管種変更時に自動反映）を新規実装。59.4節参照。
- **60章**: 測量タブのExcel出力を全面的に強化した。「一覧」表を画面の「計算結果一覧」と同じ列構成に統一→独立した「結果一覧」シートに分離→数値セルをユーザー定義表示形式化→役割欄不要で先頭4点から次の1本を予測する「予測点」機能（座標計算バグ修正・名称変更を経て確定）→「各座標データ」シートに曲線の弧を含む散布図（全体図＋測量結果ラベル付き拡大図）を自動埋め込み、という順で発展した。最終形は60.11章。
- 直近の未解決課題（引き続き未着手、次に着手する際は各章を参照）:
0. **50〜53章のExcel出力は、この開発環境にExcel本体もLibreOffice相当の実行環境も無いため、実際にExcelで開いて数式が正しく再計算されることの最終確認ができていない。** 検証はブラウザ内蔵のZIPパーサ・DOMParser・独立再計算したJS側キャッシュ値との突き合わせで行った（51.3節・53.4節に詳細な検証方法を記録）。次回、可能であれば実際にExcel（またはLibreOffice）で開いての最終確認を行うこと。
1. 支圧壁の許容支圧応力度f_a（15.4節、根拠のないプレースホルダー値）が未解決（18章のジャッキ当たり部の局所支圧照査は、この課題への対応として新規追加したもの）。
2. 43章・56章の通り、③受働土圧安定照査は入力する土質パラメータ（φ・土被り）に非常に敏感で、確認できた一次資料では低φ現場は軒並みNGになる一方、無対策で施工成功した実地情報もある（56.4節）。現場ごとに実際の地盤調査報告書の値を必ず確認・反映すること。
3. 45章で寸法図・測量履歴・テンキーは画面上の見た目まで確認できたが、**スマホ実機での音声読み上げ（`speechSynthesis`）・タップ操作感はまだ未確認**（Browserペインはデスクトップ相当のエミュレーションのため）。次回、実機での確認が望ましい。
4. 46章の「形状変更の提案」は、Hw・Beffという2つの寸法だけを動かす簡略化した探索であり、実際に施工可能な形状（立坑径の制約、バックロックの実在型番、コンクリート数量・コストなど）まではチェックしていない。**あくまで「目安」であり、最終的な採否は必ず技術者の判断で確認すること。**
5. BC点管種検討へのη有効長補正（57.3節・58.3節で式は確定済みだが未実装。QA_TABLEの出典未確認のため実装は次の明示指示待ち）・中押しジャッキ段数計算（56.2節、未実装）・現場打ち支圧壁の④相当照査（55.4節、未実装）は、いずれも一次資料で存在は確認済みだが本アプリには未実装のまま。

区間別個別仕様（4.6節）まわりを触るなら10章の教訓を先に読むこと。

### 場所・保存先（最初に確認すること）

- **作業フォルダ（絶対パス）:** `C:\Users\mande\Dropbox\Documents\07_曲線プログラム\M_APP_029\`（このフォルダ内で全て完結。他のM_APP_0XXフォルダは触らない）
- **git:** このフォルダ自体が独立したgitリポジトリ（`.git`あり、履歴は複製元からそのまま引き継ぎ）。`git remote -v` → `origin https://github.com/kkodin/MYcurve_surveying_Total_sys.git`、ブランチは`main`。**ローカルにコミットしても、ユーザーが明示的に「pushして」と言うまでは`git push`しないこと**（これまでの全セッションで徹底している方針）。1〜62章の内容はすべてコミット・push済み（M_APP_028で実施、最新コミット`6aa40ba`、2026-08-11）。M_APP_029コピー後の新しい変更はまずここにコミットする。次回作業を始める前に、まず`git status`/`git log`で最新のコミット状況を確認すること。
- **ブラウザ動作確認:** `M_APP_029\.claude\launch.json`に`m_app_029_static`（ポート8656、ディレクトリ絶対指定）を自己完結で定義済み。`C:\...\M_APP_004\.claude\launch.json`にも同名エントリがあるが、こちらが本体なので**新規セッションの作業ディレクトリがM_APP_029でもM_APP_004でも、どちらでも`preview_start({name:'m_app_029_static'})`が動く**。次のバージョン（M_APP_030等）を作る際は、両方のlaunch.jsonにエントリを追加する（コピー元の`.claude/launch.json`はrobocopyで自動的についてくるので、ポート番号と`name`だけ書き換えればよい）。
- **次バージョンを作る時の手順:** `robocopy <現フォルダ> <新フォルダ> /E`で.git込み複製 → 本ドキュメント（仕様書.md）を新フォルダにコピーしたものを最新状況に更新（冒頭の対象フォルダ名・最終更新日・目次・「次回再開する人へ」を必ず更新すること） → `C:\...\M_APP_004\.claude\launch.json`と新フォルダ自身の`.claude\launch.json`の両方に、新バージョン用のエントリ（ポート番号を1つずつ増やす）を追加。

### 目次（全62章。リンクではなく検索用の見出し一覧。該当章は`## <章番号>.`で`Ctrl+F`検索すると早い。同じテーマが複数章にまたがる場合は一番大きい番号が最新）

1. アプリの概要
2. 線形（直線・曲線）の呼び方 — 統一ルール
3. 推進管仕様タブの入力ルール
4. 推進力計算 — 最重要・要理解
5. 推進力グラフ（`drawForceGraph`）
6. 区間長按分の近似（解消済み・M_APP_019）
7. ファイル構成（M_APP_019）
8. 検証の進め方（このセッションで使った方法）
9. 基本情報タブのUI構成（アコーディオン・区間別仕様パネルの置き場所）
10. 重要な教訓：動的DOM再描画とテキスト入力欄
11. M_APP_018以前の作業履歴（新しい順、コミット済み）
12. 管本数・目地仕様集計 — 目地開口・継手判定の大幅見直し（M_APP_019）
13. 線形ビジュアル表示（新機能、M_APP_019で追加）
14. 全タブのアコーディオン化（M_APP_019）
15. 支圧壁計算 — 受働土圧安定照査の修正（M_APP_019）
16. 支圧壁 寸法図（平面図・縦断面図）（新機能、M_APP_019）
17. M_APP_019での作業履歴まとめ
18. ジャッキ当たり部の局所支圧照査（新機能、M_APP_020）
19. M_APP_020での作業履歴まとめ
20. 立坑形状（発進/到達立坑）が保存後にUIへ反映されないバグの修正
21. 立坑種別に「小判型立坑」を追加
22. 本ラウンドのコミット状況
23. 許容支圧応力度f_a（15.4節）への対応 — 自動計算はせず注釈のみ追加
24. 推進力計算が実際のPDF計算書と大きく乖離するバグを3件発見・修正、PDF準拠の計算書を新設
25. 計算書（PDF保存）が1ページで切れる・図解が不十分だった問題への対応
26. 許容支圧応力度f_aの単位をkN/m²→N/mm²に変更、NG時の警告表示を追加
27. 支圧壁の水準高（標高）入力・多段ジャッキ配置※・局所厚さ計算を追加
28. 多段ジャッキ配置※を実際の配置図の仕様（片側幅・容量別ジャッキ径・推進定規）に拡張
29. ジャッキ能力を全段共通に簡略化（jackStageCapsを撤廃）
30. 多段ジャッキ配置※を刃口推進工法専用に限定
　　※27〜30の「多段ジャッキ」という呼称は誤りと判明し、33章で「ジャッキ配置（推力分散用）」に訂正済み（用語のみの修正、機能自体はそのまま）。
31. 計算書「3.施工条件」の図を、線形ビジュアル表示と同じ描画に統一
32. 計算書の「計算過程」で項目名と数値が重なって表示されるCSSバグを修正
33. 「多段ジャッキ」「曲線造成用」という用語が誤りだったため全面訂正
34. 推進定規のH形鋼規格選択・段別設置高さ・リアルタイム断面図を追加
35. 円形立坑は「厚さ入力→幅を逆算」する向きに変更
36. バックロック（分割式プレキャスト支圧壁）を実カタログ準拠で追加、現場打ちの代わりに使用する前提に
37. 実際の承認済み計算書（BR-SST４台1350計算書.pdf）と突き合わせてバックロック計算を全面見直し
38. バックロックの据付位置（角当たり）計算・補強リング・推進中心線オフセットへの対応
39. 「バックロックの選定.xlsx」による38章の検算・「Tab2」表記修正・支圧壁高さの0.5mピッチ丸め
40. 印刷（PDF保存）時のファイル名を「現場名＋スパン名＋推力計算」にする
41. 支圧壁 寸法図（平面・断面）のテキスト重なりを解消、断面図に管内径を追加
42. 水準高モードの管中心高さ計算が誤って外径基準になっていたバグを修正
43. ③受働土圧安定照査のNG診断（館町雨水の実例）・α係数の意味・FAQ
44. 測量タブに測量履歴・音声読み上げテンキー・保護⇔解除を追加（M_APP_022）
45. 41・42・44章の未確認事項をBrowserペインの実画面で目視確認（M_APP_022）
46. 支圧壁4段階照査に「計算結果 vs 判定条件」比較表示・数式の改行整理・NG時の形状変更提案を追加（M_APP_023）
47. 支圧壁タブの各種修正（バックロック幅・厚さの算出方式見直し、発進立坑と支圧壁立坑径の連動、補強リング表記修正など。47.1〜47.13、M_APP_024）
48. 推進力タブの「元押ジャッキ仕様」を計算結果サマリーに統合・必要ジャッキ本数を選択式UIに変更／支圧壁タブの「ジャッキ配置（推力分散用）」を推進力タブへ移動（M_APP_025）
49. 測量タブ: スプレッドシート出力・PC版でのテンキー非表示（直接キーボード入力）・入力欄フォーカス時の全選択（M_APP_025）
50. 測量タブ: Excel(.xlsx)出力を4シート・生の計算式付きに全面刷新、49章のCSV出力を置き換え（M_APP_025）
51. Excel出力（50章）で計算結果が#VALUE!/#DIV/0!になる致命的バグを発見・修正（シート名修飾漏れ、M_APP_025）
52. Excel出力に「区間別計算方法一覧」（凡例）と「現在区間」セルを追加（M_APP_025）
53. Excel出力を全面的に簡潔化 — 列幅最適化・ラベル簡略化・「区間別計算式一覧」をパラメータ表に変更・誤差計算を「区分決定→前述の式から選ぶ」構造に再設計（左右判定バグ修正含む、M_APP_025）
54. Excel出力の測定誤差計算式を「区分（現在区間）をキーにしたVLOOKUP」に統一 — INDEX/MATCH位置参照から脱却（M_APP_027）
55. 一次資料（秦野市・坂本建設室川第９現場）との突き合わせ、区間別土質の実装、calcTerzaghi()の2つのバグ修正（M_APP_027）
56. 秦野市室川第９の別スパン2件を追加確認 — ③受働土圧NGでも無対策で施工成功したという重要な実地情報、中押しジャッキ段数計算は未実装（M_APP_027）
57. 別現場（水無第6）の一次資料3件を確認 — BC点管種検討の式に未実装の補正係数（η有効長補正）を発見（M_APP_027）
58. 入江土木の支圧壁4資料、渋沢2工区・館町のFJリング資料を調査 — クッション材はコルクでなくPB/FJリングが実務の主流と判明（M_APP_027）
59. 地震時継手抜出照査（レベル1・レベル2地震動）・PB/FJクッション材選定（σ_allowの管種連動自動入力含む）を実装（M_APP_027）
60. 測量タブ: Excel出力「結果一覧」シート・「予測点」機能・曲線を含む埋め込みグラフを新規実装（60.1〜60.11、複数回改訂。最新は60.11章。M_APP_027）
61. 刃口推進の周面抵抗式を選択式に — 下水道協会式（既定）／佐藤・山本改定式（実績ベース補正式、土質7区分のα・β・Ca自動入力付き）（M_APP_028）
62. 線形ビジュアル表示: 測量KP点のラベルを「N本目F/R」に修正、実測線（紫実線）・予測線（紫点線）を追加（M_APP_028）

---

## 1. アプリの概要

`M_APP_005.html` は単一ファイル完結型のSPA（推進工設計計算ツール、JPTA準拠）。

- 入口: `index.html` → 自動的に `M_APP_005.html` にリダイレクト
- タブ構成: 入出力 / 基本情報 / 立坑 / 推進力 / 支圧壁 / 測量
- データは `localStorage`（キー: `curvedJackingProjectDraft` / `curvedJackingProject`）とJSONファイルの両方で保存可能
- PC版・M_APP_004系（曲線推進管理システム）とは**別系統のアプリ**。00〜05番の旧ページ構成はM_APP_016で完全に削除済み（不要な遺物だったため）

## 2. 線形（直線・曲線）の呼び方 — 統一ルール

**「第1直線」「第1曲線」…「最終直線」**という、常にBP（発進側）を起点にした通し番号で統一しています。

```
路線: BP ─第1直線─ 第1曲線 ─第2直線─ 第2曲線 ─ … ─ 最終直線 ─ EP
```

- 「直線k」は「曲線k」の直前（BP側）にある直線
- 曲線が0本なら「第1直線（BP→EP）」の1本のみ
- 以前は基本情報タブが `BP→BC1`、推進力タブが独自に `SP直線`/`EP直線` という別の呼び方をしていて混乱の元だったため、全画面でこの命名に統一した（管長さ最適化アドバイス・管本数集計・区間別推進力計算表・グラフ、すべて共通）

## 3. 推進管仕様タブの入力ルール

- **呼び径・管種・種別を選ぶと、外径D・内径・管厚t・管重量W・許容耐荷力Fa・外圧強さP_extが標準表（`PIPE_DB`）から自動設定され、読み取り専用になる**（ヒューム管・レジン管の場合）
- **鋼管を選んだ場合のみ**、標準表が存在しないため上記6項目が編集可能になる（`onPipeCategoryChange()`が`setPipeAutoFieldsReadonly()`で切替）
- ユーザーが直接触ってよいのは: 呼び径・管種・種別・**有効長l′**・継手種別・クッション材厚S4・**シールド機外径Bs**の7項目のみ
- Bsの初期値は「管外径+30mm（片側15mmクリアランス）」を自動提案するが、自由に上書きでき、JSON保存/復元でも上書き値が保持される（`updateShieldBsDefault()`）
- 管長さ最適化アドバイスで使う規格管長は `[0.60, 0.80, 1.20, 2.43]`（1/4管・1/3管・半管・標準管）の実在する分割管のみ。以前は現実にない長さ（1.00〜4.00mの汎用リスト）を使っていたので修正済み

### 重要: 自動反映のタイミング

外径・管長・重量・許容耐荷力・外圧強さ・Bsのいずれかを変更すると:
1. `calcAlignment()` が自動で走り、基本情報タブの表示（管長さ最適化・管本数集計）が更新される
2. **さらに、既に「推進力計算を実行」を1回でも押していれば `refreshForceIfCalculated()` が `calculate()` を再実行し**、推進力タブの区間表・1本毎の表・グラフも自動更新される（未計算の状態では何もしない＝エラー表示が出ない）

## 4. 推進力計算 — 最重要・要理解

### 4.1 なぜ「到達側→発進側」に積算するのか

`calculate()` 内の `segments` 配列は、**到達側(EP)から発進側(SP/ジャッキ)に向かって**推力を積み上げます。これは物理的に唯一正しい順序です。

- ジャッキの位置（発進側）で必要な推力 = 先端抵抗Fo + 全区間の周面摩擦の合計（曲線を通過するたびに増幅）
- **曲線内の増幅係数Kは、その曲線と発進側ジャッキの間にある区間だけに掛かる**。曲線のさらに到達側（EP寄り）にある区間には掛からない
- そのため「その断面より到達側にある全てを足し上げてから、曲線を通過するたびにK^nを掛ける」＝到達側から発進側へ計算するのが正しい

### 4.2 過去に発見・修正した計算バグ（曲線2本以上のルートに影響）

`reversedCurves.forEach()` ループ内で、**中間の直線区間が、本来無関係な"隣の"曲線のKでも二重に増幅されてしまうバグ**があった（M_APP_005.html誕生時からの既存バグ、このセッションで発見・修正）。

- 症状: 曲線が1本だけなら影響なし。2本以上あると最大推進力が過大に計算される
- 原因: ループ内で「中間直線を先にpush→曲線を後でpush」の順だったため、中間直線が"次に処理される"曲線（本来は無関係）のKにも巻き込まれていた
- 修正: 「曲線を先にpush→その曲線のBP側に隣接する直線を後でpush」に順序を入れ替え（[M_APP_005.html:2633行付近](M_APP_005.html)の`reversedCurves.forEach()`。M_APP_019での大幅な行追加により行番号がM_APP_018時点から大きくずれているので注意）
- 検証方法: 独立に組んだ「各区間の増幅係数＝ジャッキとの間にある曲線のK^nの積」という別ロジックで計算し、F_totalが一致することを確認済み（2曲線・4曲線のテストケースで完全一致）

### 4.3 「構造上の力」と「進行度ベースの力」— 2つの異なる意味のF

同じ区間別データから、**目的が異なる2種類のFの見せ方**がある。混同しないこと。

| 表示 | 使う数値 | 意味 | 傾向 |
|---|---|---|---|
| **BC点外圧管種検討表** | `segments`の生の値（EP→SP積算、変更なし） | 「その継手が完成後に生涯で受ける最大構造力」＝耐荷力照査用 | 発進側が最大、到達側が最小 |
| **区間別推進力計算表・1本毎の推進力計算表・推進力グラフ** | `computeProgressiveSegments()` / `computeProgressiveTrace()`（新規追加） | 「発進坑口から実際に押し込んでいくにつれて、ジャッキに必要な推力がどう増えていくか」＝施工感覚・推進力管理図に対応 | **発進坑口(Fo)が最小、到達坑口が最大＝F_total** |

**なぜ分けたか:** 当初、区間表・グラフも「BC点検討表と同じ構造力（発進側が最大）」で表示していたところ、ユーザーから「発進坑口に向かって進むほど推力が減るのはおかしい、普通は押し込むほど増えるはず」と指摘があった。検証の結果、これは表示の向きの選択ミスであり、「押し込むほど推力が増える」という直感の方が施工感覚として正しい（実際のJPTA「推進力管理図」もこちらの考え方）。ただしBC点検討表（継手強度照査）は「その継手が完成後に受ける最大力」を見る必要があるため、そちらは構造力のままにしてある。

### 4.4 進行度ベースの計算式（`computeProgressiveSegments` / `computeProgressiveTrace`）

```
CumK = 1（発進側からの通過済み曲線の増幅係数の積）
S = 0（発進側から積算した、既に押し込んだ直線・曲線自身の周面摩擦の合計）

直線を通過: S += CumK × fo × L
曲線を通過（区間まとめ）: S += CumK × fo × CL × λ  →  その後 CumK ×= K^n
曲線を1本ずつ（管単位）: CumK ×= K → S += CumK × fo × l'　を管の本数だけ繰り返す

任意の時点での推力: F(x) = S + Fo × CumK
到達坑口（全区間通過後）: F = F_total（区間別推進力計算表の最大推進力と完全一致）
```

**設計上の注意:** 「発進側から単純に走査し、曲線に当たるたびに全体をK倍する」という素朴な実装は**間違い**（発進側寄りの区間まで、まだ通過していない先の曲線のKで誤って増幅してしまう＝4.2のバグと同種の間違いを再現してしまう）。上記のようにCumKとSを分離して管理し、「新しく追加する項だけ現在のCumKで重み付けする」実装が必須。既存の値（S）を後から曲線でさらに掛け算してはいけない。

### 4.5 1本毎の推進力計算表

- 管の本数は**発進側を1本目**として数える（既存の「管本数・目地仕様集計」表は到達側=1本目なので、**向きが逆**。カード内に説明文があったが「見やすさ優先」でユーザー指示により削除済み。将来また聞かれたら口頭で説明すること）
- 直線区間の管: `F = F前 + fo×l`
- 曲線区間の管: `F = F前 + ΔF`（内部はCumK/Sの管単位更新。区間合計の `F前×K^n + fo×CL×λ` と完全に一致することを検証済み）
- CSV出力ボタンあり（`exportPipeForceCSV()`、UTF-8 BOM付き、Excel文字化け対策）
- 距離は「管長 + (クッション材厚S4 − つぶれ代)」を1本ごとに積算する（`pipeCrush`フィールド、つぶれ代=荷重でクッション材が圧縮された後の厚み減少分）

### 4.6 区間ごとの個別仕様（標準仕様 or 個別設定）

各直線・曲線区間は、既定で「推進管仕様」カードの**標準仕様**を使うが、区間ごとに**個別仕様**（管種・種別・管長・クッション材厚・つぶれ代・継手種別）へ切り替えられる。

- **データモデル:** 曲線は`curves[i].spec`、直線は`straightSpecs[idx].spec`（`idx`は0始まりで「第1直線」=0、「第k直線」=k-1、「最終直線」=curves.length。`syncStraightSpecsLength()`で常に`curves.length+1`個に自動調整）。`spec = null`または`{on:false}`なら標準仕様。
- **呼び径（外径）は区間ごとに変えられない**（ユーザー確認済み方針：掘削径は推進全体で一定のため）。ヒューム管・レジン管はどの種別を選んでも外径は`PIPE_DB`上同一なので、種別だけ変えても曲線幾何（TL/CL/K値）には影響しない。**鋼管を選んだ区間のみ**、外径・内径・管厚・重量・Fa・外圧強さを直接手入力できる（標準表が無いため）。
- **解決ロジック:** `resolveSpec(override)` が override（無ければ標準）から `{D, Di, t, lp, W, Fa, Pext, S4, crush, jointAllowMm, category, grade}` を返す。呼び出し側はこれを使うだけで、標準/個別の分岐を意識しなくてよい。
- **UI:** `segSpecFieldsHTML(kind, key, spec)` が曲線ブロック・直線カードの両方で共用のパネルHTMLを生成。`kind`は`'curve'`または`'straight'`、`key`は曲線の`id`または直線の`idx`。トグル・入力変更のハンドラは`onSegSpecToggle` / `onSegSpecCategoryChange` / `onSegSpecChange`。
- **周面摩擦力foは区間ごとに再計算する**（ユーザー確認済み方針）。foの式にはD・Wが入るため、`calculate()`内に`foForSpec(spec, hVal, qTotalVal)`ヘルパーを追加し、区間ごとの`resolveSpec()`結果を渡してfoを個別計算している。**先端抵抗Fo（先端一箇所の値）は標準仕様のみで計算し、全区間共通のまま**（ユーザー確認済み方針）。
- **BC点外圧管種検討表**は、以前は「現場全体で選んだ管種の全種別を横並び比較」する表だったが、区間ごとに管種が変わりうるため、**その曲線で実際に選ばれている管種・種別**を1列で示す形に変更した（`bcResults[].pipeLabel`）。鋼管の場合はQA_TABLE（推進力照査用データ）が無いため判定列は「—（鋼管は手入力Faで別途照査）」となる。
- **外圧耐荷力照査（Mr/M）も区間ごとの表に拡張**（ユーザー確認済み方針、`extCard`/`extTable`）。従来の「計算結果サマリー」内のMr/M表示は**標準仕様1回だけの参考値**として残してあり、区間別の判定は新しい表を見る。
- **管本数・目地仕様集計（`buildPipeSchedule`）** も、以前は現場全体で1つのグローバル管長を使って路線全体を均等に分割していたが、**区間ごとに自分の管長で分割してから路線全体でつなげる**方式に変更した（`pipes`配列を区間ごとに生成→結合）。
- JSON保存/復元・CSV出力とも個別仕様に対応済み（`getProjectData()`の`alignment.straightSpecs`、`curves[].spec`）。旧保存データ（`cushion`のみ保持していたもの）は自動的に個別仕様へ変換して読み込む（後方互換）。

### 4.7 管1本ごとの長さは区間内で均等割り（端数を最後の1本に寄せない）

1本毎の推進力計算表・管本数目地仕様集計とも、区間（直線・曲線）を管の本数に分割する際は

```
numPipes = Math.round(区間長 / 管長)
1本あたりの実長 = 区間長 / numPipes   ← 区間内で均等割り
```

を使うこと。**`Math.ceil` + 最後の1本だけ端数、という実装をしてはいけない**（過去に直線側だけこの誤った実装になっており、曲線に入る直前の管だけ極端に短くなって、グラフのドット間隔が乱れるバグがあった。ユーザー指摘で発覚・修正済み）。曲線側は元から均等割りだったので、直線側をそれに合わせた。

## 5. 推進力グラフ（`drawForceGraph`）

- X軸: 発進坑口(E1, x=0) → 到達坑口(E2, x=maxL)。距離目盛あり
- Y軸: 進行度ベースの推力（4.3節参照）。発進坑口で最小、到達坑口で最大になる
- 色: **曲線=青、直線=緑**（`FORCE_GRAPH_COLOR`）
- BC/EC/E1/E2の位置に破線マーカー＋ラベル（「坑口」は長くて重なるため「E」記号に短縮）
- 管1本ごとに小さな○、5本ごとに塗りつぶした●を描画し、目視で本数を数えられるようにしてある
- 曲線区間は管1本ごとの点をそのまま折れ線でつないでいるため、指数的な増幅カーブが滑らかに表現される（区間の始点・終点を直線で結んでいるだけではない）

## 6. 区間長按分の近似（解消済み・M_APP_019）

**旧問題（M_APP_018以前）:** `calculate()` 内の区間分割（`epStraight` / `intermediateStraight` / `spStraight`）は、基本情報タブで計算済みの正確な区間長（第1直線・第2直線…の実長）を再利用せず、「推進距離全体から曲線長を引いた残りの直線長を、区間の数で均等に按分する」という簡易近似だった。曲線1本なら影響なし、2本以上かつ各直線の実長が不均等な場合にズレが生じていた。

**修正内容（M_APP_019）:**
- `calcAlignment()`（[M_APP_005.html:1254行付近](M_APP_005.html)）が、計算済みの各直線の正確な実長（BP-EP基準）をグローバル変数 `realStraightLengths`（`straightSpecs`と同じ並び：0=第1直線…curves.length=最終直線）に保存するようにした。
- `calculate()`（[M_APP_005.html:2090行付近](M_APP_005.html)）は、`realStraightLengths`の本数が曲線数+1と一致する場合、均等按分ではなくこの実長を使う。第1直線・最終直線は、実長からそれぞれ`launchDist`/`arrivalDist`（立坑内に収まる分）を差し引いた「地山内（実際に推進力がかかる区間）」の長さにする。曲線が0本のとき（直線1本のみ）は両方の除外距離を同じ1本の直線から差し引き、二重計上を避ける。
- `realStraightLengths`が噛み合わない場合（`calcAlignment()`未実行など）は、従来の均等按分ロジックにフォールバックする（`intermediateStraightFallback`として保持）。
- **検証方法:** 曲線2本・意図的に不均等な直線長（第1直線1.60m／第2直線18.86m／最終直線6.46m相当）のテストケースで、`F_total`を「各区間の増幅係数＝ジャッキとの間にある曲線のK^nの積」という独立した検算式と突き合わせ、完全一致（diff=0）を確認済み。フォールバック時・曲線0本時もエラーなく動作することを確認済み。
- 4.6の区間別個別仕様（`straightSpecs[k]`）は元々「位置（第k直線）ベース」で対応付けられていたため、この修正の影響を受けず引き続き正しく機能する。今回変わったのはその区間の**長さ自体**が正確になった点のみ。

## 7. ファイル構成（M_APP_019）

| ファイル | 役割 |
|---|---|
| `index.html` | 入口。`M_APP_005.html`へ自動リダイレクト |
| `M_APP_005.html` | 本体（このドキュメントで説明した全ロジック） |
| `manifest.json` | PWA設定 |
| `service-worker.js` | オフラインキャッシュ（`CACHE_NAME`はM_APP_019での変更のたびに`v1`→`v4`まで更新済み。**次に本体を変更したら必ずv5以降へ上げること**、さもないとユーザーのブラウザに古いキャッシュが残り続ける） |
| `icon.svg` | アイコン |
| `README.txt` | 現場での運用メモ |

## 8. 検証の進め方（このセッションで使った方法）

コード修正のたびに、以下の手順でブラウザ実機検証を行った（Claude Codeの`preview_*`ツール使用）:

1. `.claude/launch.json`（M_APP_004フォルダ内）に各バージョンの静的サーバー設定がまとまっている。新バージョンを作ったら`m_app_0XX_static`をコピーしてポート番号・パスだけ変更して追加すること（M_APP_018時点でポート8645が空き）
2. `localStorage.clear()` → `location.reload()` でまっさらな状態にする
3. `showPage(1); addCurve(); addCurve(); calcAlignment(); showPage(3); calculate();` のように直接JS関数を叩いてテストデータを作る（UIクリックより高速）
4. `lastCalcResult.F_total` や `lastPipeForceRows` を直接読み、独立に組んだ検算式（例: 4.2節の別ロジック）と一致するか突き合わせる
5. `preview_console_logs` でJSエラーがないか必ず確認
6. スクリーンショットで見た目も最終確認

**重要:** 推進力計算のような数値ロジックは、画面が「それらしく」表示されているだけでは正しさの保証にならない。必ず独立した検算式で数値を突き合わせること（このセッションで4.2のバグを発見できたのはこの方法による）。

## 9. 基本情報タブのUI構成（アコーディオン・区間別仕様パネルの置き場所）

基本情報タブ（`#p0`）が縦に長くなりすぎたため、カード単位のアコーディオンにしてある。

- 対象カードは7つ：📋現場情報 / ⚙️推進工法 / 🏗️推進管仕様 / 🌱土質条件 / 📐線形・推進区間 / 📏管長さ最適化アドバイス / 🔧管本数・目地仕様集計
- 各カードの`<div class="card">`に`accordion`クラスを付け、`.card-title`に`onclick="toggleAccordion(this)"`を付けるだけ。**中身のHTMLは一切ラップし直していない**（CSSの`.card.accordion:not(.open) > .card-title ~ *{display:none!important}`で、開いていないカードのタイトル以外の兄弟要素を丸ごと隠しているだけ）。動的に再描画される中身（曲線リスト・線形計算結果など）にも影響しない
- `toggleAccordion()`は、同じ`.page`内の他の`.open`カードを閉じてから、クリックされたカードを開閉する＝**常に1つだけ開く**仕様
- デフォルトは全カード閉（タイトルだけ）。特定カードを初期展開したい要望が出たら`toggleAccordion()`相当の処理を`init()`末尾で呼べばよい

**区間別個別仕様パネル（4.6節）は、この「線形・推進区間」カードの中の`線形計算結果（自動）`エリアに統合済み。** 以前は曲線ブロックと専用の「直線区間の個別仕様」カードに分かれていたが、ユーザー要望で1箇所（各区間の距離表示のすぐ下）にまとめた。曲線ブロック（`renderCurves()`が描画する`#curveList`）は今はR・角度・方向のみで、個別仕様パネルは含まない。

## 10. 重要な教訓：動的DOM再描画とテキスト入力欄

区間別個別仕様パネル（4.6節）の実装で、**2周にわたってテキスト入力のバグを出した**。同じ轍を踏まないよう経緯を残す。

1. **1周目のバグ:** 個別仕様の管長・クッション材厚等を`oninput`で毎キー入力ごとに`calcAlignment()`を呼んでいた。`calcAlignment()`は`#alignmentCalcResult`（=これらの入力欄自身が入っているコンテナ）を`innerHTML`で丸ごと差し替えるため、**入力中の欄自身が破棄・再生成され、フォーカスが失われて2文字目以降が入力できなくなった**。
2. **1周目の対処（不十分だった）:** `setHTMLPreservingFocus()`（差し替え前後でフォーカスidとカーソル位置を保存・復元するヘルパー、`v()`/`n()`の近くに定義済み・汎用的に使える）を導入。ただし対象の入力欄が`type="number"`だったため、**`selectionStart`/`setSelectionRange`が使えず**（多くのブラウザでnumber型は選択範囲API非対応）、カーソル位置の復元だけ失敗し、「1文字目を打つとカーソルが先頭に飛ぶ」別の症状が残った。
3. **2周目の対処:** `type="number"`を`type="text" inputmode="decimal"`に変更（selection APIが使えるようになる）。
4. **最終的な対処（ユーザー提案・現在の実装）:** そもそも**毎キー入力で再描画する設計自体をやめ**、`oninput`を`onchange`（フォーカスが外れた時にだけ発火）に変更した。これにより入力中はDOMに一切触れず、上記1〜3の対症療法が丸ごと不要になった。`setHTMLPreservingFocus()`自体は汎用ヘルパーとして残してあるので、**今後また「入力欄を含む領域を`oninput`で再描画したくなった時」は、まず`onchange`で済まないか検討し、どうしても即時反映が必要な場合だけ`setHTMLPreservingFocus()`＋`type="text" inputmode="decimal"`を使うこと**。

**教訓のまとめ：入力欄を内包するコンテナを`innerHTML`で再描画するハンドラは、`oninput`（毎キー入力）ではなく`onchange`（フォーカスアウト時）にするのが第一選択。** `oninput`で即時反映したい場合でも、`type="number"`は使わない（`type="text" inputmode="decimal|numeric"`にする）。

## 11. M_APP_018以前の作業履歴（新しい順、コミット済み）

M_APP_019での作業（12〜17章）はすべて未コミットのため、ここには含まれない。M_APP_019時点の変更点は17章を参照。

```
4cb3f37 Recalculate segment spec fields on blur instead of every keystroke（M_APP_017）
101be58 Fix cursor jumping to the start when typing in segment spec fields（M_APP_017）
3dd5513 Turn 基本情報 tab cards into a single-open accordion（M_APP_017）
e3c08b6 Fix focus loss while typing in per-segment spec fields（M_APP_017）
9d175cf Pre-fill segment spec fields with the standard spec when toggled on（M_APP_017）
3bb6ee3 Evenly distribute straight-segment pipe lengths to keep pitch uniform（M_APP_017）
195a994 Consolidate per-segment spec panels into the alignment results list（M_APP_017）
587b729 Document per-segment pipe spec override system in handover spec（M_APP_017）
11b5fc6 Add per-segment pipe spec overrides and cushion-aware pipe distance（M_APP_017）
f7a644c Add handover spec document covering M_APP_016 changes（M_APP_017）
34e9013 Remove numbering-convention note from per-pipe force table
8e61a7a Add CSV export for the per-pipe jacking force table
81735e7 Restyle jacking-force graph: curve=blue/straight=green, E1/E2 shaft labels, pipe-count dots
16d4e8e Auto-refresh jacking-force results when pipe spec fields change after a calc
d920ef9 Fix multi-curve force calculation bug; make force build up as jacking progresses
41fcd27 Show tip resistance first, add per-pipe force graph markers and per-pipe force table
483270d Reorder jacking-force segment table to launch-side-first reading order
f2421b6 Unify straight/curve segment naming to 第N直線/第N曲線/最終直線
02b6f78 Lock table-derived pipe fields to read-only, default shield Bs, rename length labels
8e6f62f Fix stale pipe-length advisor and recalc pipe spec fields to real standard lengths
45a9f89 Add survey baseAz auto-calc, clean up legacy project files, improve jacking-force verdict display
d25687a Fix manhole size labels and account for wall thickness in exclusion distance（M_APP_015以前からの既存コミット）
```

`git log` はM_APP_018にもそのまま引き継がれている（`git log --oneline` で確認可能）。

---

## 12. 管本数・目地仕様集計 — 目地開口・継手判定の大幅見直し（M_APP_019）

`buildPipeSchedule()`（管本数・目地仕様集計表）まわりで、ユーザーの実機テストから3件の不具合・設計不足が見つかり、いずれも修正済み。

### 12.1 バグ修正2件（前提となる小さい修正）

- **つぶれ代（`pipeCrush`）がJSON読込で0mmに戻る:** `setProjectData()`が`pipe.cushion`は復元するのに`pipe.crush`を復元し忘れていた（単純な書き漏れ）。`if (d.pipe.crush != null) setV('pipeCrush', d.pipe.crush);`を追加して解消。
- **クッション列が「実際に属する区間の仕様」ではなく「曲線側の仕様」を表示していた:** `buildPipeSchedule()`内で、目地開口S1の計算に使った曲線（`bestSeg`、最も厳しい値を要求する曲線）のクッション値`bestSeg.spec.S4`を、そのままテーブルの「クッション」列にも使い回していた。これだと最終直線に個別のクッション仕様（例: 10mm）を設定していても、曲線側の標準クッション（例: 20mm）が表示されてしまう。**その管が実際に属する区間自身の`spec.S4`**（`pipe.ownS4`として管生成時に保持）を使うよう修正。あわせて、区間ごとにクッション値が異なりうるようになったため、行のグルーピングキーにもクッション値・S1値を含めて、異なる値を持つ区間が誤って1行にまとめられないようにした。

### 12.2 目地開口S1の判定ロジック根本改訂（なぜ変えたか）

**ユーザー指摘の経緯:** 「曲線を通過済みの直線区間なのに、曲線通過時の開口（例: 68.8mm）がそのままS1として表示されている。曲線を通過した後は目地が閉じる方向に戻るはずで、実際の残留開口はクッション材厚S4からつぶれ代を引いた値（例: 20−8=12mm）になるはずだ」という指摘があり、検証の結果その通りだった。

- **旧実装の問題:** `calcS1(D, L, R, S4)`（曲線内の幾何学的開口式）を、曲線内の管にも、曲線を通過済みで今は直線区間に静止している管にも、**同じ式で**適用していた。これは物理的に誤り。曲線通過中は一時的に大きく開くが、通過後は外力がなくなり目地は閉じる方向に戻るため、残留開口は幾何学的開口ではなく**クッション材の圧縮状態（S4−つぶれ代）**で決まる。
- **さらなる気づき:** 「一度も曲線を通過しない直線区間」もS1=0（開口なし）としていたが、これも誤り。クッション材は軸力（推進力そのもの）で常に圧縮されるため、曲線の有無に関わらず**S4−つぶれ代の残留開口が生じる**。
- **最終的な判定式**（[M_APP_005.html:2174行付近](M_APP_005.html)の`buildPipeSchedule()`内）:
  ```
  bestCurveS1 = その管が生涯で経験する最大の幾何学的開口（通過した全曲線のうち最大値。一度も曲線を通らなければ0）
  S1（表示値・推進完了後の残留開口）=
    曲線内に永久に留まる管（inCurve=true）    → bestCurveS1（曲がったまま完成するため、幾何学的開口がそのまま残留値）
    それ以外（直線区間で静止する管、全て）      → max(0, その管自身のS4 − つぶれ代)
  ```

### 12.3 許容開口長と抜出し長の2段階判定（継手選定のロジック）

上記のS1修正だけでは別の問題が残る。「通過済み直線区間の残留開口(12mm)は小さいのでJA型で十分」と判定してしまうと、**実際に曲線を通過する瞬間に一時的に68.8mm開く**という事実を見落とし、JA型では施工中に破損する。ユーザー指摘：「曲線を通過した時に目地が最低68.8mm開いているので、JAでは通過できません」。

継手性能表（JA/JB/JC）には元々2つの限界値がある。
- **許容開口長**（JA:40/JB:50/JC:70mm）＝ 推進中に一時的に（方向修正等で）最大まで開いてもよい開口
- **抜出し長**（JA:30/JB:40/JC:60mm）＝ 推進完了時点で残っていてよい開口の上限（許容開口長より厳しい）

旧実装は`JOINT_SPEC`に抜出し長の数値しか持っておらず、しかもそれを全ケースの判定に流用していた（結果的に「通過中の暴露」を見ていなかった）。修正後は`JOINT_SPEC`に両方を持たせ、継手選定は**両方を満たす最も安いもの**にした。
```js
const JOINT_SPEC = [
  {key:'JA', label:'JA型', pulloutOpen:30, allowOpen:40},
  {key:'JB', label:'JB型', pulloutOpen:40, allowOpen:50},
  {key:'JC', label:'JC型', pulloutOpen:60, allowOpen:70},
];
recommendedSpec = JOINT_SPEC.find(j => bestCurveS1 <= j.allowOpen && S1 <= j.pulloutOpen) || null;
```
（`calcCurveJointCheck()`など、曲線がその位置に永久に留まることを前提にした既存の他チェックは`pulloutOpen`のみで判定すればよく、そちらは数値・挙動とも変更していない。曲線に永久に留まる管は「通過時＝完了後」で同じ値のため、より厳しい`pulloutOpen`だけ見れば十分）

### 12.4 「実際の選択（推奨）」の表記

継手種別は物理的な部品なので、その管が実際に使っている区間仕様（標準仕様なら「推進管仕様」カードの選択、個別仕様ならその区間の選択）で決まる。これを`actualSpec`（`pipe.ownJointAllowMm`から解決）として、計算上必要十分な最安の継手`recommendedSpec`と併記する。
- 一致: そのまま `JC型`
- 実際の選択が推奨より上位（過剰スペック）: `JC型（JA型を推奨）`
- 実際の選択が推奨に届かない（危険）: 赤字で `JA型（要JC型以上）`
- 推奨自体が無い（JCでも超過）: `JA型（JCでも超過）`

## 13. 線形ビジュアル表示（新機能、M_APP_019で追加）

基本情報タブの「線形・推進区間」カードの下に、路線の平面形状を模式的に表示するcanvasベースの新カード（`#alignmentVisualCard`）を追加した。`drawAlignmentVisual()`が本体（[M_APP_005.html:1421行付近](M_APP_005.html)）。

### 13.1 座標系・表示方向

内部計算はBPを原点・前方をx軸として素直に積み上げる（`x=前方, y=右+/左-`）が、**表示だけ90°回転**させ、縦軸=X（上+）・横軸=Y（右+）にしている（ユーザー指定）。`tx(p)=offX+p.y*scale`、`ty(p)=offY-p.x*scale`の2行だけで実現しており、内部の座標計算ロジック自体は回転前のまま素直な形を保っている。

### 13.2 ズーム・パン（ピンチ／ホイール／ドラッグ）

`alignmentView={scale,offsetX,offsetY}`（ユーザー操作分）と`alignmentViewBase={scale,offX,offY}`（バウンディングボックスに合わせた自動フィット分）を分離して保持し、実際の変換は両者を合成する。ズームは常に「操作したアンカー点（マウス位置・ピンチ中心）が画面上で動かないよう」`offsetX/offsetY`を逆算して更新する（`zoomAt()`）。拡大率は0.1〜200倍まで許容（ユーザーが「1/10まで拡大できるように」と要望したため、方眼と組み合わせて細部を確認できる範囲に広げた）。ダブルクリック/ダブルタップと「表示リセット」ボタンで`alignmentView`を初期値に戻せる。

**実装上の注意（他のcanvasにも共通）:** アコーディオンが閉じている間は`.card.accordion:not(.open) > .card-title ~ *{display:none!important}`によりcanvasのoffsetWidth/Heightが0になり、描画してもサイズが確定せず何も表示されない。`toggleAccordion()`にカードを開いた直後の再描画フックを設けている（14.3節参照）。

### 13.3 方眼表示（縮尺に応じた形）

現在の拡大率（px/m）から、間隔が見やすくなる「1・2・5×10ⁿ」の値を自動選択して方眼を描く（`niceGridStep()`）。拡大するほど細かい方眼になり、左下に「方眼: ○m」と表示する。

### 13.4 曲線要素（BP/BC/IP/SP/EC/RO/EP）と作図補助線

各曲線について、標準的な測量の要素点をすべて計算・表示する。
```
BC = 曲線始点、EC = 曲線終点、IP = 交点（back/forward tangentの交点）
SP = 曲線の弧の中点、RO = 曲線の中心（半径起点）
IP = BC + TL×(その直前の方位角方向)
```
作図補助として**BC-RO・RO-EC・BC-IP・IP-ECの4本の破線**を描く（ユーザー指定。標準的な曲線要素図の描き方）。曲線が複数ある場合は各点に`BC1`/`BC2`のように番号を付けて区別する。

### 13.5 曲線要素の注記円（ドラッグ移動・リサイズ）

各曲線のSPの近く（RO と反対方向）に、その曲線の要素（曲線番号R1/R2…・R・IA・CL・SL・TL）を円（実際は楕円）で囲んで表示する。ユーザー要望により、この円は**ドラッグで自由に移動**でき、**右下の小さいハンドルをドラッグすると拡大縮小**できる。位置・サイズは`curveAnnotationState[曲線番号]={worldPos, scale}`に保持し、`worldPos`が設定されていれば既定位置（SP基準の自動配置）より優先する。当たり判定は直近の描画結果`lastCurveAnnotationScreens`（画面座標でのcx/cy/rx/ry/ハンドル位置）を毎回保存しておき、次のポインタ操作開始時に参照する方式。円と円をつなぐ引き出し線（SP→円）は、ユーザー要望により**削除済み**（円だけを独立して表示する）。

### 13.6 測量測定点の重ね描き

測量タブで計算された測定点（`surveyResults`、KP1・KP2…）を、菱形マーカー＋「◯本目 KP1」ラベルで重ねて表示する。ラベル先頭の本数は、標準管長で概算した到達側基準の本数（`Math.round((totalDistance-distFromBP)/lp)`）。判定ランク（OK/注意/NG）で色分け。

**座標系の注意:** 測量タブの設計線は`buildSurveyGeometry()`内で基準方位角`baseAz`（器械点→後視点の実測方位）だけ回転した座標系を使っているが、線形ビジュアル表示は常に`az=0`起点のスキーマ座標。そのため測定点は表示前に`-baseAz`だけ逆回転してから重ねている。`judgeStraight()`/`judgeCurve()`には元々なかった`distFromBP`（BPからの累積距離）フィールドを追加した（既存の`remaining`は区間内のみのローカルな残り距離で、本数推定には使えないため新設。既存フィールドへの影響はない）。

## 14. 全タブのアコーディオン化（M_APP_019）

9章で基本情報タブのみアコーディオン化されていたが、他の5タブ（入出力／立坑／推進力／支圧壁／測量、計33枚のカード）も同じ方式で統一した。

### 14.1 対象・注意点

やり方自体は9章と同じ（`class="card accordion"`＋`card-title`に`onclick="toggleAccordion(this)"`）。`toggleAccordion()`自体は`.closest('.page')`でページを判定するため、ページをまたいだ改修は不要だった。

### 14.2 動的生成カードの開閉状態保持

支圧壁タブの「支圧壁3段階照査結果」「バックロック選定」の2枚は、`calcBacklock()`が呼ばれるたびに`#backlockResult`の`innerHTML`を**丸ごと再生成**している。これらをそのままアコーディオン化すると、**入力するたびに開いたカードが強制的に閉じてしまう**（新しいDOM要素が`open`クラスなしで生成し直されるため）。これを防ぐため、`calcBacklock()`内で再生成前に`.card.accordion.open`のidを読み取っておき、再生成後に同じidの要素へ`open`クラスを再度付与している。**同様の「毎回innerHTML丸ごと差し替え」パターンを持つ箇所を今後アコーディオン化する時は、この対処が必須**。

### 14.3 canvas再描画レジストリ（CANVAS_REFRESH_FNS）

13.2節の問題（閉じたアコーディオン内のcanvasは開いた瞬間に再描画が必要）が、`distDiagram`（立坑タブ）・`forceGraph`（推進力タブ）・`wallPlanView`/`wallSectionView`（支圧壁タブ、16章）でも同様に発生する。個別に対処するのではなく、`toggleAccordion()`に汎用的な仕組みを設けた。
```js
const CANVAS_REFRESH_FNS = {
  alignmentVisual: () => drawAlignmentVisual(),
  distDiagram: () => updateGroundLen(),
  forceGraph: () => { if (lastCalcResult) drawForceGraph(lastCalcResult.segments, lastCalcResult.Fa, lastCalcResult.Fo); },
  wallPlanView: () => drawWallPlanView(),
  wallSectionView: () => drawWallSectionView(),
};
```
カードを開いた直後、そのカード内の`<canvas id="...">`を探し、同名キーの関数があれば呼ぶ。**新しくcanvasを追加するときはここに1行足すだけでよい。**

**実装上の教訓:** 最初は`requestAnimationFrame()`で1フレーム遅らせて呼んでいたが、これは不要だった。`offsetWidth`/`offsetHeight`を読むタイミングでブラウザは同期的にレイアウトを確定させる（forced synchronous layout）ため、`classList.toggle('open', true)`の直後に同期的に呼んでも正しいサイズが取れる。rAFはブラウザタブが非アクティブ（自動テストツールでpaneが表示されていない場合等）だと発火が遅延・停止することがあり、そちらの方が問題になる。**canvasをdisplay:none解除の直後に再描画したい場合は、rAFではなく同期呼び出しでよい。**

## 15. 支圧壁計算 — 受働土圧安定照査の修正（M_APP_019）

### 15.1 何がおかしかったか

`calcBacklock()`の③受働土圧安定照査（`Ep = 0.5×γ×Hw²×Kp×Beff`、`Ep ≥ F`で判定）が、典型的な推進力（F=3000〜5000kN級）に対してほぼ必ずNGになっていた。実際に検証すると、γ=18・φ=30°・Hw=2.5m・Beff=3mでもEp≈506kNにしかならず、Fに対して1桁近く足りない。

**原因:** 旧式は支圧壁**自体の高さ(Hw)の範囲内の土の自重分**しか見ておらず、以下を無視していた。
- **上載荷重（サーチャージ）**: 支圧壁より上にある土被り（土質条件タブの`soilHmin`/`soilHmax`は既に入力されているのに、この計算では未参照だった）による押さえ効果
- **粘着力（`soilC`）**: 粘性土の粘着抵抗

支圧壁は通常、土被りの下（地表からかなり深い位置）に埋設されるため、実際には自重項よりも上載荷重項の方が大きくなることが多く、これを無視すると受働抵抗を大きく過小評価する。

### 15.2 修正後の式・根拠

標準的なRankineの受働土圧式（サーチャージ・粘着力項を含む形）に合わせた。
```
Ep = (a)自重項 + (b)上載荷重項 + (c)粘着力項
(a) = 0.5×γ×Hw²×Kp×Beff
(b) = q×Hw×Kp×Beff　　　　q = γ×(支圧壁天端の土被り)
(c) = 2×C×√Kp×Hw×Beff
支圧壁天端の土被り = H最大 − (Hw−hc)　　（H最大=soilHmax、hc=管中心高さ。負値は0とする＝天端が地表より上に出る異常なケースのガード）
```
`H最大 − (Hw−hc)`という換算は、土質条件タブの土被り`soilHmax`が「地表面〜管中心」の深さという既存の定義（`calculate()`内の周面摩擦計算と同じ解釈）に合わせ、そこから支圧壁天端までの高さ分（`Hw−hc`）を引くことで「地表面〜支圧壁天端」の深さを求めている。

計算結果画面には(a)(b)(c)の内訳と中間値（Kp・土被り・q）をすべてステップごとに表示するようにした（ユーザー要望「計算式をわかりやすく表示」）。`checks[2].formula`に`<br>`区切りの複数行文字列を入れているだけで、他のレンダリングロジックは変更していない。

### 15.3 検証結果

Bw=3.0m, hc=0.8m, F=3500kN, γ=18, φ=30°, H最大=5.0mの条件で、
| Hw | C | Ep（新式） | 判定 |
|---|---|---|---|
| 2.5m | 0 | 1843kN（旧式は506kN） | NG |
| 4.0m | 20 | 3294kN | NG（僅差） |
| 4.5m | 20 | 3565kN | **OK** |

Hwや粘着力を増やすと正しくOK側に転じることを確認済み。

### 15.4 許容支圧応力度 f_a=3000 kN/m² は根拠のないプレースホルダー（未解決・要確認）

ユーザーから「許容支圧応力度（`wallFa`、①基礎反力照査・②偏心曲げ照査で`≤fa`の判定に使う）の3000という数値はどこから来たのか」と質問があり、コードを確認した結果、**計算式や参照テーブルは一切なく、HTMLの`<input id="wallFa" ... value="3000">`にハードコードされた初期値（プレースホルダー）に過ぎない**ことが判明した（[M_APP_005.html:581行](M_APP_005.html)）。

- 他のどの入力（コンクリート強度・配合など）からも自動計算されておらず、単なる仮の数値。
- 一般には支圧壁のコンクリートの設計基準強度Fcに対して許容支圧応力度を定める考え方があるが、**このアプリにはその計算ロジックがそもそも実装されていない**。
- **現状の運用:** ユーザーが自分の現場のコンクリート仕様に基づいた実際の値に、都度手動で入力し直す必要がある。
- **今後の課題（未着手）:** コンクリート強度Fcの入力欄を追加し、そこから許容支圧応力度を計算する式を実装するかどうかは、次回ユーザーに相談すること（今回は「情報提供のみ」で、コード変更はしていない）。

## 16. 支圧壁 寸法図（平面図・縦断面図）（新機能、M_APP_019）

支圧壁タブに「📐 支圧壁 寸法図（平面・断面）」カードを追加（`drawWallPlanView()`/`drawWallSectionView()`、[M_APP_005.html:3070行付近](M_APP_005.html)）。`calcBacklock()`（[M_APP_005.html:3249行付近](M_APP_005.html)）の冒頭（早期returnより前）で毎回呼ぶため、推進力Fを入力していない段階でも寸法だけは確認できる。

- **縦断面図（`wallSectionView`）:** 支圧壁本体（幅Beff×高さHw、基礎コンクリート上面y=0基準）、推進管芯高さ線（破線＋管断面の円）、そして**管芯を基準にした天端まで（Hw−hc）・下端まで（hc）の寸法線**を左側に表示する（ユーザー指定：「推進管芯高さ線を描いて、支圧壁天端、下端までの寸法も」）。

### 16.1 円形立坑の支圧壁は矩形ではなく円弓形（ユーザー提供の参考図で修正）

**初回実装の誤り:** 平面図（`wallPlanView`）を最初に作った時は、円形立坑でも支圧壁を単純な矩形の帯（幅Bw×厚みなし）として描いていた。ユーザーから実際の施工イメージ図（円形立坑内に扇形の赤い支圧壁ブロックが積まれている写真調のイラストと、平面図CAD図）が提供され、**円形立坑の支圧壁は実際には円弓形（弓形、レンズ状の断面）になる**ことが判明した。CAD参考図の寸法関係（立坑径5000mm・弦2800mm・中心厚さ434mm）を検証したところ、標準的な円弓形の幾何（サジッタ公式）と完全に一致した。

- **なぜ円弓形になるか:** 支圧壁はライナープレート内側の円筒面に沿って構築されるため、平らな矩形ではその両端に隙間ができてしまう。実際は「ライナープレート内側の円をある弦（幅）で切り取った円弓形」の形状になる。
- **判明した寸法関係:**
  ```
  ライナープレート内側径 = 立坑径（呼び径） − 50mm（参考図の「青い線」）
  弦Beff = 支圧壁の設計幅（ユーザー入力Bwをライナー内側径以内に収めた値）
  中心厚さ（サジッタ）= R − √(R² − (Beff/2)²)　　R = ライナープレート内側の半径
  ```
  検証1（簡易参考図）: R=2500mm・Beff=2800mmで計算すると厚さ=434mm→参考図の「434」と完全一致。
- **検証2（実際の推進仮設図PDF、より詳細な参考図で追加確認）:** ライナープレート径5000mm・板厚25mm（両側で−50mm）・支圧壁幅3644mmという実例で計算すると、R=(5000−50)/2=2475mm・厚さ=2475−√(2475²−1822²)=799.9mm→図面の「支圧壁厚 800mm」とほぼ完全に一致（設計上の丸め誤差の範囲）。この検証で、**ライナープレート径（外径）ではなくライナープレート内側（有効径）を基準にRを取る必要がある**ことも確認できた（外径のままだと500mm以上ずれた誤った値になる）。あわせて図面の管中心高さ関連寸法（675mm=φ1350の半径、1005mm=基礎コンクリート上面からの管中心高さ）も、アプリの`hc`（管中心高さ）の定義と整合することを確認した。ラベルもこの実際の図面の用語（「ライナープレート径」「支圧壁幅」「支圧壁厚」）に合わせて修正し、断面図側にも支圧壁厚（平面図と同じ値）を注記として追加した。
- **修正内容:** `computeWallBeff()`（[M_APP_005.html:3235行付近](M_APP_005.html)）を、以前の「管外径の高さでの弦長」という誤った基準から、**ライナープレート内側径（立坑径−50mm）を上限とする弦**に変更し、`linerD`（ライナー内側径）と`sagitta`（中心厚さ）も返すようにした。`drawWallPlanView()`の円形立坑分岐は、矩形の帯ではなく実際の円弓形（`R_liner`と弦`Beff`から極座標でサンプリングした多角形）を描画するよう全面的に書き直した。あわせて立坑外径（赤）とライナー内側（青）の2つの円も参考図と同じ配色で表示する。
- **縦断面図への影響:** 断面図（`wallSectionView`）は元々`Bw`をそのまま壁幅として使っていたが、円形立坑では実際の壁幅は`Beff`（ライナー制約後）であるべきなので、`Bw`ではなく`Beff`を使うよう修正した（矩形立坑では従来通りBw=Beffなので挙動は変わらない）。
- **注意:** 支圧壁の**構造計算**（15章の3段階照査）自体は、以前から`Beff`（有効幅）を使っており、この修正は「使っているBeffの値をより正確に（ライナー基準に）した」だけで、照査ロジック自体への影響はない。影響があるのは平面図・断面図という**表示**の正確さのみ。

## 17. M_APP_019での作業履歴まとめ

### 17.1 コミット状況

本ドキュメント作成時点（2026-07-27）で、12〜16章の内容はすべて**未コミット**（`M_APP_005.html`・`service-worker.js`・`仕様書.md`が変更検出される状態）。ユーザーから明示的に「コミットして」と言われるまではコミットしない方針（システム指示）のため、次回このセッションを引き継ぐ場合は`git status`/`git diff`で現状を確認すること。

### 17.2 変更点サマリー（新しい順）

1. 許容支圧応力度f_a=3000kN/m²が根拠のないプレースホルダーであることが判明（コード変更なし、情報提供のみ。15.4節）
2. 円形立坑の支圧壁平面図を円弓形に修正、実際の推進仮設図PDFで数値検証・ラベルも図面の用語に統一（16.1節）
3. 支圧壁 寸法図（平面図・縦断面図）を追加（16章）
4. 支圧壁 受働土圧安定照査に上載荷重・粘着力項を追加、計算式の内訳表示（15章）
5. 全タブ（入出力/立坑/推進力/支圧壁/測量）をアコーディオン化、canvas再描画レジストリ導入（14章）
6. 線形ビジュアル表示: 測量測定点の重ね描き、円からSPへの引き出し線を削除（13.5, 13.6節）
7. 線形ビジュアル表示: 座標軸90°回転（縦=X上+・横=Y右+）、方眼表示、ズーム範囲拡大、BC-RO/RO-EC/BC-IP/IP-EC作図補助線、注記円のドラッグ移動・リサイズ（13章）
8. 線形ビジュアル表示（新機能）を基本情報タブに追加（13章）
9. 管本数・目地仕様集計: 目地開口S1の判定ロジック根本改訂、許容開口長/抜出し長の2段階判定、実際の選択×推奨の併記表示（12.2〜12.4節）
10. つぶれ代の保存消失バグ・クッション列の表示混同バグを修正（12.1節）
11. 区間長按分の近似を解消、`realStraightLengths`による実長の再利用（6章、このセッションの最初の作業）

### 17.3 検証方法

8章の方針（独立した検算式との突き合わせ、`preview_console_logs`でのエラー確認）をこのセッションでも踏襲。特に12章の目地開口ロジックは、複数の`R`・つぶれ代・継手選択の組み合わせでJA/JB/JC判定が期待通り切り替わることを、`calcAlignment()`/`calcBacklock()`を直接呼ぶスクリプトで確認した。線形ビジュアル表示（13章）はcanvasの描画ピクセルデータ（`getImageData`で色の出現数を確認）で描画の有無・要素の存在を検証する方法を新たに使った（スクリーンショットが撮れない実行環境だったため）。

---

## 18. ジャッキ当たり部の局所支圧照査（新機能、M_APP_020）

### 18.1 背景（15.4節の未解決課題への対応）

前バージョン（M_APP_019）の17章末で、「許容支圧応力度 f_a=3000kN/m² は根拠のないプレースホルダー」という未解決課題を記録していた（15.4節）。ユーザーから、コンクリートの応力の基礎（σ=荷重P/断面積A、圧縮応力の説明）の参考資料と共に、「支圧壁の形状から、元押ジャッキが当たる箇所（実際には鉄板を挟んで力を分散させる）を考慮して支圧応力がどれだけ必要か、その応力を発生させるために必要なコンクリート圧縮強度・支圧壁厚さを算出するロジック」の追加依頼があり、これに応える形で実装した。

### 18.2 なぜ「支圧壁全体の応力」と「局所の支圧応力」が別物なのか

既存の①基礎反力照査（15章、`f = F/(Beff×Hw)`）は、推進力Fが支圧壁の**全断面**に均等に分布すると仮定した平均応力。しかし実際には、元押ジャッキは支圧壁に直接当たらず、その間に**鉄板（受圧板）を挟んで力を伝える**。鉄板の当たり面積は支圧壁全体よりずっと小さいため、**鉄板直下のコンクリートは平均応力よりはるかに大きな局所応力を受ける**（これが「支圧応力度」の照査対象）。この2つは目的が異なる別々の照査であり、どちらか一方だけでは不十分。

### 18.3 計算ロジック

`calcLocalBearing()`は[M_APP_005.html:3346行付近](M_APP_005.html)、`calcBacklock()`から毎回呼ばれる（[M_APP_005.html:3395行付近](M_APP_005.html)冒頭）。

新規入力（支圧壁タブに「🔨 ジャッキ当たり部の局所支圧照査」カードを追加）:
- `jackPlateW`/`jackPlateH`: ジャッキ受圧鉄板の幅・高さ [m]（初期値0.5m）
- `wallFc`: 採用コンクリート設計基準強度 F_c [N/mm²]（初期値24）

```
A1（鉄板当たり面積） = b_p × h_p
A2（支圧壁断面積、荷重が最終的に分散しきる先と仮定） = Beff × Hw
必要支圧応力度　σ = F / A1

支圧割増し係数 = min(√(A2/A1), 2)　　※コンクリートが載荷面より広い断面に囲まれていると
                                        局所の支圧強度が割り増される、という一般的な考え方
許容支圧応力度　fa = 0.25 × Fc × 支圧割増し係数　　（Fcの単位はN/mm²→kN/m²に変換して計算）
判定: σ ≤ fa

必要コンクリート強度　Fc_required = σ / (0.25 × 支圧割増し係数)
必要支圧壁厚さ　t = max((Beff − b_p)/2, (Hw − h_p)/2)　　（45°分散想定：鉄板の端から
                                                            Beff×Hwの端まで45°で広がりきる厚さ）
```

**式の根拠・注意点:**
- `0.25×Fc×min(√(A2/A1),2)`は、コンクリートの局所支圧強度の割増しに関する一般的な目安式（コンクリートの許容応力度は概ねFc/4程度、載荷面が周囲のコンクリートに拘束されると強度が上がる、という考え方をシンプルにまとめたもの）。**特定の一つの規準（道路橋示方書・JPTA指針等）に完全準拠したものではなく、目安値**である旨を計算結果画面にも明記した。実際の設計では適用する基準の該当条項を確認すること。
- 「必要支圧壁厚さ」の45°分散仮定は、荷重がコンクリート内部を斜め45°で広がっていくという簡易的な考え方（厳密な2方向の応力分散解析ではない）。幅方向・高さ方向それぞれ独立に計算し、大きい方を採用している。
- A1がA2以上（鉄板が支圧壁と同じかそれ以上に大きい）場合は`Math.max(A1, A2)`のガードで割増し係数が1に収束し、単純平均応力の照査（①基礎反力照査）と同じ扱いになる。

### 18.4 検証結果

Bw=3.0m, Hw=2.5m, hc=1.0m, F=3168kN, 鉄板0.5m×0.5m, Fc=24N/mm²の条件で:
```
A1=0.250m², A2=7.50m²
σ = 3168/0.250 = 12672 kN/m²
割増し係数 = min(√(7.5/0.25),2) = 2.00（上限に達している）
fa(Fc=24) = 0.25×24000×2.00 = 12000 kN/m² → σ(12672) > fa(12000) → NG
必要Fc = 12672/(0.25×2.00)/1000 = 25.3 N/mm²（採用24に対しわずかに不足）
必要厚さ = max((3.0-0.5)/2, (2.5-0.5)/2) = max(1.25, 1.0) = 1.25m
```
Fcを30N/mm²に上げるとOKに転じることを確認済み。JSON保存/復元（`getProjectData()`/`setProjectData()`の`wall.jackPlateW`/`jackPlateH`/`fc`）も動作確認済み。

## 19. M_APP_020での作業履歴まとめ

### 19.1 コミット状況

M_APP_020での変更（18章の内容。M_APP_019セッションの未コミット分もまとめて含む）は、ユーザーから明示的に「commitして」と指示があったため、コミット`aeb1a23`としてローカルにコミット済み（push はこれまで通り明示的な指示があるまで行わない）。

### 19.2 変更点サマリー

1. ジャッキ当たり部の局所支圧照査（新機能）を支圧壁タブに追加（18章）。前バージョンで見つかった「許容支圧応力度f_aが根拠のないプレースホルダー」という課題（15.4節）への対応。

## 20. 立坑形状（発進/到達立坑）が保存後にUIへ反映されないバグの修正

### 20.1 症状・原因

ユーザーから「立坑形状（矩形立坑・円形立坑等）が、保存して読み込むと矩形立坑に戻ってしまう」と報告があった。調査の結果、**支圧壁タブの立坑種別（`wallShaftType`）は正しく復元されていたが、立坑タブ（発進立坑・到達立坑）側の種別（`shaftTypes.launch`/`shaftTypes.arrival`）は復元されていなかった**ことが判明。

`setProjectData()`の`d.shaft`復元処理が、`shaftTypes.launch = sh.launchType||'rect';`のように**内部変数を直接書き換えるだけ**で、UIを更新する`setShaftType(side, type, el)`関数を一切呼んでいなかった。このため:
- 内部変数`shaftTypes.launch`自体は正しく`'circle'`等に復元されていた（`calcShaftDist()`の計算結果は実は正しかった）
- しかし**選択中チップのハイライト表示**（`.shaft-chip.active`）と**入力欄の表示切替**（矩形/円形の入力欄のdisplay:none切替）が更新されないため、**画面上は常に矩形立坑（初期状態のまま）に見えていた**

再現テスト: `setShaftType('launch','circle',el)`→保存→リロード後、`shaftTypes.launch`は`"circle"`のままなのに、アクティブなチップは`"rect"`、円形入力欄は非表示のままであることを確認した。

支圧壁タブ側（`setWallShaft()`経由で正しく復元）は最初から問題なかった（既存コードが既にこのパターンで実装されていたため）。

### 20.2 修正内容

`setProjectData()`（[M_APP_005.html:3694行付近](M_APP_005.html)の`d.shaft`ブロック）を、支圧壁タブの`wallShaftType`復元と同じパターンに統一した。`shaftTypes[side] = type`という直接代入をやめ、対応するチップ要素を`querySelector`で探して`setShaftType(side, type, el)`を呼ぶことで、UIも含めて正しく復元されるようにした。

```js
const launchType = sh.launchType || 'rect';
const launchEl = document.querySelector(`#launchShapeChips [data-val="${launchType}"]`);
if (launchEl) setShaftType('launch', launchType, launchEl);
// arrival側も同様
```

修正後、`保存→リロード`で発進側を円形立坑にした場合、アクティブなチップ・入力欄の表示とも正しく`circle`側に切り替わることを確認済み。

## 21. 立坑種別に「小判型立坑」を追加

ユーザー要望により、立坑タブ（発進/到達立坑）・支圧壁タブの両方の立坑種別選択に「小判型立坑」（矩形部＋両端半円の組合せ形状、いわゆるスタジアム型）を追加した。

- **既存の矩形立坑用の入力欄をそのまま流用**（内法幅B・内法長L・オフセット等、新規フィールドは追加していない）。小判型は「矩形部＋半円端部」だが、推進方向の除外距離計算（`calcShaftDist()`）・支圧壁の有効幅計算（`computeWallBeff()`）とも、**半円端部の形状差は考慮せず、矩形立坑と全く同じ式（全長L/2＋オフセット、有効幅=内法幅B）を流用する**方針とした。半円端部はどちらも「推進方向に対して直線部が支配的」という前提に基づく簡略化であり、この前提を明記した（`wallTypeNote`のオーバル説明文にも明記）。
- **支圧壁タブの平面図（`drawWallPlanView()`）だけは、実際の小判型形状（矩形＋半円弧2つ）を`ctx.arc()`で描画するよう対応**。支圧壁本体（Bw/Beffの帯）の描き方自体は矩形立坑と同じ。
- 立坑タブ側（`launchShapeChips`/`arrivalShapeChips`）・支圧壁タブ側（`wallShaftChips`）両方にチップを追加、`setShaftType()`/`calcShaftDist()`/`setWallShaft()`/`computeWallBeff()`の各分岐に`'oval'`を追加。
- 全立坑種別（矩形/円形/オープン/小判型、到達側は+マンホール、支圧壁側は+鋼製ケーシング）を一通り切り替えるテストで、計算結果・保存復元ともエラーなく動作することを確認済み（20章の保存復元バグ修正が、この小判型追加にもそのまま効いている）。

## 22. 本ラウンドのコミット状況

20章・21章の内容は、ユーザーから明示的に「commitして」と指示があったため、コミット`7bf6cbf`としてローカルにコミット済み（18章の内容は前回のコミット`aeb1a23`）。pushは引き続き明示的な指示があるまで行わない。

## 23. 許容支圧応力度f_a（15.4節）への対応 — 自動計算はせず注釈のみ追加

15.4節・7章冒頭で挙げていた未解決課題（`wallFa`の初期値3000kN/m²が根拠のないプレースホルダー）について、ユーザーに対応方針を確認した。

- **検討した案:** 18章で追加済みの`wallFc`（採用コンクリート設計基準強度）を流用し、`fa=0.25×Fc`（局所支圧照査と異なり割増し係数なし＝全断面平均応力とみなす）で自動算出する案を提示。
- **ユーザーの判断:** 自動計算は導入せず、**引き続き手入力のまま**とする方針を選択。理由の深掘りはしていないが、「fa」（許容支圧応力度）は本来、採用基準（道路橋示方書等）や現場条件によって決め方が変わりうる値であり、`0.25×Fc`という簡易式に固定してしまうことへの慎重さと推測される。
- **実施内容:** `wallFa`入力欄（[M_APP_005.html:584行](M_APP_005.html)）の直後に`.note-box`（オレンジ系の注意喚起スタイル、`wallTypeNote`等で既に使われているクラスを流用）を追加し、「初期値3000kN/m²は根拠のないプレースホルダーであること」「現場のコンクリート仕様に基づき手動設定が必要なこと」「局所支圧照査の`wallFc`とは別の値で、自動算出の仕組みは無いこと」を明記した。計算ロジック・JSON保存構造への変更は一切なし（表示のみの変更）。
- **今後また聞かれたら:** ユーザーが将来、自動算出（`0.25×Fc`案、または別の基準式）を導入したくなった場合は、本節の検討経緯（案自体は妥当と判断されつつ見送られたこと）を踏まえて再提案すること。
- **サービスワーカー:** `CACHE_NAME`を`m-app-005-v8`→`v9`に更新済み。

---

## 24. 推進力計算が実際のPDF計算書と大きく乖離するバグを3件発見・修正、PDF準拠の計算書を新設

### 24.1 発端

ユーザーが実際の現場（館町雨水／新幸技建／φ1350ヒューム管、`推力計算1350.pdf`という別の計算ソフトで作成された下水道協会式の計算書）と、本アプリの計算結果を突き合わせたところ、同一条件で**アプリ側が+74.6%（3849kN vs PDFの2205kN）過大**という大きな乖離が見つかった。

### 24.2 検証方法

PDFに記載された全入力値（管仕様・土質・線形）をM_APP_020の推進力タブに同一条件で投入し、`calculate()`を実行。内部の各計算式をPDFの計算式と1行ずつ突き合わせ、疑わしい式だけを一時的にPDFの正式な式に差し替えて再計算する、という「要因ごとの寄与度を分離する」方法で原因を切り分けた（ブラウザのJSコンソールから直接関数を差し替えて実行）。

### 24.3 見つかった3つのバグ（すべて修正済み）

1. **`calcCurveK()`の曲線抵抗増加率係数kが、土質を無視した固定値`0.5`だった**（[M_APP_005.html](M_APP_005.html)、コメント「JPTA定数」は誤り）。正しくは下水道協会式`k=tan(φ/2)`（φ≤15°の場合はφ=15°とする床処理）。管本数分（本件33本）複利で拡大されるため、乖離全体の約51%を占める最大要因だった。`calcCurveK`に`phi_soil`引数を追加し、呼び出し側（`calculate()`内の曲線ループ）にも渡すよう修正。
2. **`calcFoFo()`の刃口推進(`blade`)分岐で、先端抵抗F0が「管外径D」「実測N値」を使っていた**。正しくは下水道協会式`F0=10×1.32π×Bs×N′`（先導体外径・貫入抵抗設計値）。乖離の約43%を占めた。土質条件カードに**N′（貫入抵抗設計値）入力欄を新設**（既定値はN値と同じ。切羽心抜き工法等でN値実測より低減する場合に上書きする）。
3. **`calcTerzaghi()`の緩み幅Be・側方土圧係数・活荷重pが、下水道協会式と異なる簡易式（Be=1.5D+0.4固定、Jaky式K0=1-sinφ、活荷重=100/((0.5+H)(0.2+H))）になっていた**。標準式（Be=(Bc+0.1)(1+sin(45°-φ/2))/cos(45°-φ/2)、K=1固定、T-25後輪荷重式）に置き換え。乖離の約6%。
   - **この修正作業中に、Ce（サイロ係数）の式で`Be`の掛け算を書き忘れる二次バグを作り込み、ブラウザでの再検証で即座に発覚・修正した**（正: `Ce=(Be/(2Kμ))×(1-e^(-2KμH/Be))`）。**教訓: 数式修正は必ずブラウザで既知の正解値と突き合わせて検証すること**（8章の方針通り）。

修正後、3箇所すべてをPDFの式に揃えた状態で再計算した結果、**F_total=2205.75kN（PDF: 2205kN、誤差0.03%）と完全に一致**することを確認した。

### 24.4 元押ジャッキ仕様の入力・判定を追加

推進力タブに「🔧 元押ジャッキ仕様」カードを新設（[M_APP_005.html](M_APP_005.html)、`jackCapUnit`＝ジャッキ1本の能力[kN]、`jackCount`＝使用台数）。計算書8節でF_total（総推力）と「能力×台数」の総ジャッキ能力を比較しOK/NG判定する。JSON保存/復元にも対応（`getProjectData()`/`setProjectData()`の`jack`キー）。

### 24.5 PDF準拠の8セクション計算書（計画書添付用）を新設

推進力タブの計算結果に「📄 計算書（推進力の設計）」カード（`renderReport()`関数）を新設し、実際のPDF計算書と同じ構成・順序で表示するようにした。

1. 管の仕様　2. 土質条件　3. 施工条件（+施工概略図SVG）　4. 管にかかる等分布荷重（+断面模式図SVG）　5. 推進力の計算　6. 区間推進力　7. 管の許容耐荷力の検討　8. 推進設備の検討（元押ジャッキ）

- **7節の許容耐荷力比較表**: ヒューム管の場合、1種(50N)/2種(70N)/3種(90N)の3グレードのFrを並べて表示する。PIPE_DBには1種・2種のFa値しか無いが、Fr=1000σmean×Aの関係から`Fr(90N) = Fr(50N) × (22500/13000)`で計算できることを検算で確認し、90N用のテーブルを追加せずに済ませた。
- **6節の区間推進力**: 既存の「区間別推進力計算表」（施工感覚に合わせたSP→EP進行度ベース表示、4.3節参照）とは別に、`segments`配列（元々EP→SP構造計算順で保持している生データ）をそのままPDFのF1/F narrativeと同じ順序・意味で表示する。**4.3節の設計判断（進行度ベース表示への変更）は変更していない**、単にPDF準拠の計算書内だけ、元々ある構造計算順の生データを別の見せ方で流用しただけ。
- **印刷対応**: `#reportCard`のみを印刷対象にする`@media print`ルールと、「🖨️ 計画書添付用に印刷/PDF保存」ボタン（`printReport()`、`document.body`に`printing-report`クラスを付けて`window.print()`を呼ぶだけ）を追加。既存の「タブ内に隠れたカードはaccordionで閉じている」CSSを`body.printing-report`スコープで上書きし、印刷時は開閉状態に関係なくreportCard全体が見える。
- 2つのSVG図（`svgConstructionSchematic()`＝発進〜曲線〜到達の概略図、`svgLoadCrossSection()`＝Be/Bt/すべり面の断面模式図）は、実際の計算値（Be・Bt・曲線長等）から比例計算した簡易な模式図（縦方向は実寸比例ではない旨を明記）。
- 既存の詳細カード（計算根拠・区間別推進力計算表・1本毎の表・BC点・外圧耐荷力・グラフ）はすべて残し、タイトルに「詳細:」を付けて計算書カードの下に「付録」として維持した（何も削除していない）。

### 24.6 検証結果

- PDF記載の全入力値で同一条件を再現し、F_total=2205.75kN（PDF比+0.03%）で一致（24.3節）。
- デフォルト状態・鋼管カテゴリ・曲線0本（直線のみ）のいずれでも`calculate()`がエラーなく完走し、計算書にNaNが出ないことを確認済み。
- JSON保存→復元のラウンドトリップで`soilNprime`・`jack.capUnit`・`jack.count`が正しく戻ることを確認済み。
- `service-worker.js`の`CACHE_NAME`を`v9`→`v10`に更新済み。

---

## 25. 計算書（PDF保存）が1ページで切れる・図解が不十分だった問題への対応

### 25.1 発端

24章の計算書機能をユーザーが実際に印刷/PDF保存してみたところ、**1ページしか出力されず、しかも等分布荷重（4節）の断面図SVGより先で切れていて、5〜8節が丸ごと欠落**していた。あわせて、ユーザーが別の実案件（相模原市、φ1650・別の推進力計算Excel「刃口曲線推力検討002.xlsx」）を示し、そのExcelの断面図（Be=上端幅・H=右側の高さ・45°+φ/2=左下角度・すべり面=斜辺の引き出し線、上下左右に一点鎖線の中心線という描き方）に合わせたイラストにすること、計算結果に途中式（数値代入）を付けること、各検討事項の合否判定をまとめて分かるようにすることが要望された。

### 25.2 「1ページしか出ない」の原因

このアプリはPWA（モバイルアプリ的UI）のため、`html,body{height:100%;overflow:hidden}`で画面を固定し、`#pageArea`だけを`overflow-y:auto`で内部スクロールさせるレイアウトになっている（9章参照）。`window.print()`はこの**画面に見えている1ビューポート分しか用紙に流し込めず**、スクロールで隠れている続きのカード（5〜8節や断面図）は最初から存在しないものとして扱われていた。

**対応:** `printReport()`が`<html>`と`<body>`の両方に`printing-report`クラスを付与し、`@media print`側で`html.printing-report, body.printing-report{height:auto!important;overflow:visible!important}`と`#pageArea`・`.page`の高さ制約もすべて解除するようにした（[M_APP_005.html](M_APP_005.html)のstyle内、`printReport()`関数）。ブラウザのJSで同じCSSを一時適用して`document.body.scrollHeight`を比較する方法で検証し、720px→3548pxまで展開されることを確認した（実際のprint media適用は`window.print()`時のみ発火するため、コンソールでの`getComputedStyle`では直接確認できないことに注意。効果を確かめたい場合はこの「同じ宣言を一時的な`<style>`で注入してscrollHeightを比較する」方法を使うこと）。

### 25.3 等分布荷重の断面図をExcelの描き方に合わせて再設計

`svgLoadCrossSection()`を全面的に描き直した（[M_APP_005.html](M_APP_005.html)）。
- 上端にBe寸法線、下端にBt寸法線（旧版から継続）
- **新規:** 右側にH（土被り、模式的）の寸法線を追加
- **新規:** 縦方向の対称中心線・横方向の管中心線を一点鎖線で追加
- **新規:** 左下角に「45°+φ/2」の角度マーク（円弧）。Beの計算式自体は`45°-φ/2`（鉛直からの半角）を使うが、現場の計算書の慣習としては水平基準からの角度`45°+φ/2`で図示するため、**式の変数はそのまま、図の角度表記だけ`45°+φ/2`に合わせた**（数学的に等価、90°-（45°-φ/2）=45°+φ/2）。
- **新規:** 「すべり面」の引き出し線を右斜辺に向けて追加

### 25.4 計算過程（途中式）の追加

`stepRow()`/`calcSteps()`という汎用ヘルパーを追加し、「項目名・数値を代入した式・結果」を1行ずつ並べる`.calc-steps`ブロックを導入した。
- **4節（等分布荷重）:** Bt・すべり角・Be・μ・Ce・w・衝撃係数i・活荷重p・qの9行
- **5節（推進力の計算）:** F0・μ′・τa・fの4行
- **6節（区間推進力）:** 曲線ごとにk・Kx・λxの3行×曲線数
- これらの中間値（`mu`・`kmu`・`slipHalfDeg`・`impactI`・`P`・`a`・`c`・`betaLive`）は`calcTerzaghi()`の戻り値に追加しただけで、**計算結果の値自体は一切変えていない**（表示のための情報追加のみ）。

### 25.5 総合判定（合否一覧）の追加

計算書の総合ヘッダー直下（1節より前）に、①推進力（F≤Fr）②外圧耐荷力（Mr/M≥1.2、**従来レポートに掲載していなかった項目を追加**）③推進設備（F≤ジャッキ総能力）の3項目をまとめた合否一覧`s0`を追加した。全項目OKなら緑のチェック、1つでもNGなら赤の✕で見出し自体の色も変わる。

### 25.6 検証

- 館町雨水（φ1350、24章のテストケース）を再実行し、F_total=2205.755kN（PDF比変わらず一致）を再確認 — 今回の変更で既存の数値が変わっていないことを確認。
- 相模原市案件相当（φ1650、R=40m・IA=56.522°・左曲線、φ=11°、γ=13、C=20、N=5、N′=1）を新規に投入し、エラーなし・NaNなし・総合判定表示・計算過程2箇所・SVG2点の出力を確認。
- `document.body.scrollHeight`比較による印刷展開の検証（25.2節）。
- `service-worker.js`の`CACHE_NAME`を`v10`→`v11`に更新済み。

---

## 26. 許容支圧応力度f_aの単位をkN/m²→N/mm²に変更、NG時の警告表示を追加

### 26.1 発端

ユーザーから「許容支圧応力度はコンクリートの設計基準強度のことか。そうであれば単位はN/mm²のはず（kN/m²ではおかしいのでは）」という質問があった。回答として、①f_aはFcそのものではなくFcから安全率を見込んで導く**許容値**であること、②kN/m²とN/mm²は同じ物理量の単位違いに過ぎず`1N/mm²=1000kN/m²`であること、③現状の初期値3000kN/m²(=3N/mm²)は根拠不明という説明をした上で対応方針を確認したところ、次の4点の実施が決まった。

1. `wallFa`の単位表示を`kN/m²`→`N/mm²`に変更
2. 初期値を`3000`(kN/m²)→`24`(N/mm²、コンクリートFcでよく使われる値をそのまま仮置き)に変更
3. 引き続きユーザーが自由に変更できる仕様のまま
4. ①基礎反力照査・②偏心曲げ照査がNGの場合に警告表示を追加

### 26.2 実装内容

- `wallFa`入力欄のラベル・単位・初期値を`N/mm²`・`24`に変更（[M_APP_005.html:648行](M_APP_005.html)）。注釈も「初期値24N/mm²はFcの目安値の仮置きであり根拠のある許容応力度ではない」旨に更新。
- `calcBacklock()`内で`faInput`（N/mm²、ユーザー入力）と、①②の計算（f・σmaxはkN/m²）で使う`fa = faInput×1000`（kN/m²換算）を分離。判定式自体（`f≤fa`等）は変更していない。
- ①②の照査結果表示に、N/mm²表示とkN/m²換算の両方を併記。
- **NG時の警告表示**: `wallCheckCard`の先頭に、①または②がNGのときだけ`ng-box`で「f_a（現在○○N/mm²）が実際の値より大きすぎないか確認するか、支圧壁の幅Bw・高さHwを見直してください」という警告を追加（[M_APP_005.html:3944行付近](M_APP_005.html)）。
- **後方互換（重要）**: 旧バージョンで保存されたJSONの`wall.fa`はkN/m²形式（例: 3000）のまま残っている。単位の意味が変わったため、`setProjectData()`側で**200を超える値は旧kN/m²形式とみなして÷1000で変換**する処理を追加した（コンクリートの許容支圧応力度がどんな基準でもN/mm²で200を超えることは実務上ありえないため、この閾値で安全に判別できる）。新形式（すでにN/mm²、24等の小さい値）はそのまま使う。この変換をしないと、旧プロジェクトを読み込んだ際に`fa=3000`が「3000N/mm²」と誤解釈され、実質的にどんな推進力でも①②が必ずOKになってしまう**安全上重要なバグ**になるところだった。

### 26.3 検証

- 既定値24N/mm²、F=3168kN、Bw=3.0、Hw=2.0で①基礎反力照査（f=528kN/m²=0.528N/mm²）がOKになることを確認。
- 意図的に`fa=0.1N/mm²`（=100kN/m²）に下げてNGを再現し、警告ボックスが正しいメッセージで表示されることを確認。
- 旧形式（`fa:3000`）を`setProjectData()`に渡すと`3`（N/mm²）に変換され、新形式（`fa:30`）はそのまま`30`で復元されることを確認。
- `service-worker.js`の`CACHE_NAME`を`v11`→`v12`に更新済み。

## 27. 支圧壁の水準高（標高）入力・多段ジャッキ配置・局所厚さ計算を追加

### 27.1 発端

ユーザーが実際の施工図（元押し装置の側面図・平面図、油圧ジャッキ100t×1650stを曲線造成用に多段配置している例）を示し、「通常図面には水準高が記載されるが、この図面にはない」として、支圧壁の構造を指定する際に次の値を入力できるようにしてほしいと依頼があった。

- ジャッキ据付幅（1段目=2基、2段目=4基、3段目=6基…とそれぞれ数値が必要）
- 基礎コンクリート水準高
- 管底水準高（これから管芯高さも計算できる）
- 支圧壁天端水準高
- 支圧壁厚さ（平面から見て通常一番厚いところ）
- 立坑中心と推進中心のオフセット（通常0）
- 円形立坑はジャッキ据付位置によって支圧壁の厚みが変わるのでこれを計算すること
- 支圧壁前面には応力分散用の鉄板が入っている前提

### 27.2 仕様確認

実装前にユーザーに3点確認した。
1. 水準高モードは既存のHw/hc直接入力と**共存**させ、切り替えられるようにする（既存プロジェクトとの互換性を壊さない）。
2. 多段ジャッキの据付幅入力は、円形立坑の**各段の局所支圧壁厚さを計算・表示するためだけ**に使う（構造照査（①②③やジャッキ当たり部の局所支圧照査）には組み込まない）。
3. 段数は固定ではなく、**推進力タブの元押ジャッキ使用台数（jackCount）÷2**で自動決定する（1段目2基・2段目4基…と段ごとに2基ずつ増える配置が前提）。

### 27.3 実装内容

**水準高モード**（[M_APP_005.html](M_APP_005.html)、「🧱 支圧壁の諸元」カード）
- `直接指定`／`水準高で指定`の2択チップ（`setWallLevelMode()`）。水準高モードでは`wallHw`・`wallHc`が自動計算・読取専用になる。
- 入力: 基礎コンクリート天端水準高・管底水準高・支圧壁天端水準高・立坑中心と推進中心のオフセット（既定0）。
- 換算式（`updateWallLevelDerived()`）: `Hw = 支圧壁天端水準高 − 基礎コンクリート天端水準高`、`hc = (管底水準高 − 基礎コンクリート天端水準高) + 管外径/2`。既存の「基礎コンクリート上面(y=0)基準」という座標系定義（15章）と整合させただけで、他の計算式は一切変更していない。

**支圧壁厚さ**
- 円形立坑（`circle`/`casing`）: 幾何学的に一意に決まる（16章のサジッタ計算）ため`wallThickness`は自動計算・読取専用にした。
- 矩形・小判型・オープン: 厚さは形状から一意に決まらないため、設計値を手入力する欄のまま。

**多段ジャッキ配置（曲線造成用）**（新カード`jackStagesCard`、`renderJackStages()`）
- 段数 = `Math.floor(推進力タブのjackCount / 2)`。段nの基数は`2n`基（1段目2基・2段目4基…）で固定表示、据付幅（その段のジャッキの中心対称スパン）だけを各段ごとに入力する。
- 円形立坑の場合、各段の局所厚さを幾何学的に計算する: 弦（支圧壁前面）から円中心までの距離`hCenter = R − sagitta`（Rはライナー半径、sagittaは16章の中心厚さ）を求め、各段のジャッキ位置のx座標（`据付幅/2 + オフセット`の絶対値）における局所厚さ`= √(R²−x²) − hCenter`を算出する。中心（x=0）で最大（=sagittaと一致）、弦の端（x=Beff/2）でゼロになることを確認済み（27.4節）。
- 矩形・小判型等では局所厚さは計算せず「—」表示（27.2節の方針通り、構造照査には使わない参考表示のみ）。
- `jackStageWidths`（段ごとの据付幅の配列）はグローバル変数で保持し、段数が変わっても既存の入力値をなるべく保持する（配列の`length`をstageCountに合わせて伸縮）。推進力タブの`jackCount`のoninputに`renderJackStages(computeWallBeff())`を追加し、タブをまたいでも段数が即座に更新されるようにした。

**「前面に鉄板が入っている前提」について**: この前提は18章の「🔨 ジャッキ当たり部の局所支圧照査」（`jackPlateW`/`jackPlateH`）に既に組み込まれている。今回のリクエストへの対応として新たな実装は行っていない（既存機能で満たされているため）。

### 27.4 検証

- 矩形立坑・jackCount=4（既定）で2段（2基・4基）の表が出て、局所厚さ列は「—」になることを確認。
- 円形立坑（立坑内径5.0m）に切り替えると、`wallThickness`が読取専用になり、サジッタ計算値（0.506m）が自動入力されることを確認。据付幅0の段は中心厚さと同じ0.506mになることも確認。
- 据付幅を1.0m・3.0mに変えると、局所厚さがそれぞれ0.455m・0.000m（Beff=3.0mの端でちょうどゼロ）になり、中心から離れるほど薄くなる想定通りの挙動を確認。
- `jackCount`を6に変更すると、支圧壁タブを開かなくても3段目（6基）が即座に表に追加されることを確認。
- 水準高モードで基礎コンクリート水準高0・管底水準高0.5・支圧壁天端水準高2.5・管外径1.6mを入力すると、Hw=2.500m・hc=1.300m（=0.5+1.6/2）が正しく自動計算されることを確認。
- JSON保存→復元のラウンドトリップで、水準高モード・3つの水準高・オフセット・段別据付幅配列がすべて正しく戻ることを確認。
- `service-worker.js`の`CACHE_NAME`を`v12`→`v13`に更新済み。

## 28. 多段ジャッキ配置を実際の配置図の仕様（片側幅・容量別ジャッキ径・推進定規）に拡張

### 28.1 発端

27章で追加した多段ジャッキ配置に対し、ユーザーがより詳細な参考図（曲線造成用ジャッキの配置基準図、円形立坑断面に1〜3段目のジャッキ位置・推進定規(H鋼)・寸法を記入したもの）を提示し、「これを参考にして、ユーザーに決めさせる設定にしておいて」と依頼があった。図面のポイント：
- ジャッキ幅は推進中央線からの**片側幅**で指定する（27章の「中心対称スパン」ではなく直接オフセット）
- ジャッキの径は容量で決まる目安値がある（1000kN=200mm、1500kN=250mm、2000kN=300mm）
- 推進定規（H鋼）は管底より50mm下がりに敷設し、推進定規幅はH鋼のサイズと下がり量で決まる
- 2段目・3段目の配置ルール（推進定規内側とジャッキ内側を合わせる、3段目は2段目と同じ幅で上に乗る）は現場・機種で異なるため、自動計算はせず数値をユーザーが確認しながら入力する

### 28.2 実装内容

- **ジャッキ幅の意味を変更**: 27章では「その段のジャッキの中心対称スパン」を入力し内部で÷2していたが、図面の描き方（推進中央線からの片側幅）に合わせて**入力値をそのままオフセットxとして使う**よう`renderJackStages()`を変更（`x = Math.abs(w + offset)`、以前は`w/2 + offset`だった）。
- **段ごとにジャッキ容量[kN]の入力欄を追加**し、`jackDiameterFromCapacity()`（1000kN=200mm・1500kN=250mm・2000kN=300mm、500kN刻みで50mm増える線形の目安値）でジャッキ径を自動表示する（読取専用の参考値）。既定値は推進力タブの`jackCapUnit`を初期値として使う。
- **推進定規（H鋼）関連の全体設定**として「管底からの下がり量[mm]」（既定50）・「推進定規幅[mm]」（既定200）を追加。これらは局所厚さ計算には使わず、計算書・現場資料としての記録用（H鋼規格表の実装や、推進定規幅の自動算出はユーザー判断に委ねる方針とし、値の直接入力のみとした）。
- **「2段目の値を3段目以降にコピー」ボタン**（`copyStage2ToUpper()`）を追加。図面の「3段目は2段目と同じ幅」というルールを強制はせず、ワンクリックで反映できる補助操作とした（自動化しすぎない方針は27.2節の確認事項と一貫）。
- `jackStageCaps`（段ごとのジャッキ容量配列）を新設し、`jackStageWidths`と同様に段数増減時に伸縮・既存値保持。JSON保存/復元にも対応（`wall.jackStageCaps`・`wall.railDrop`・`wall.railWidth`）。

### 28.3 検証

- 1段目容量1000kN→ジャッキ径200mm、2段目容量2000kN→300mmと正しく表示されることを確認。
- 円形立坑（ライナー内側径4.95m）で1段目ジャッキ幅0.4m→局所厚さ0.474m、2段目ジャッキ幅1.0m→0.295mと、片側幅を直接オフセットとして使った計算が意図通りであることを確認（旧仕様の÷2ロジックは撤廃済み）。
- 「2段目の値を3段目以降にコピー」ボタンで3段目の幅（・当時は容量も）が2段目と同じ値になることを確認。
- JSON保存→復元で`railDrop`・`railWidth`・段別配列が正しく戻ることを確認。
- `service-worker.js`の`CACHE_NAME`を`v13`→`v14`に更新済み。

## 29. ジャッキ能力を全段共通に簡略化（jackStageCapsを撤廃）

28章で段ごとに独立入力できるようにした「ジャッキ容量」について、ユーザーから「ジャッキの能力は全段同じとします」という指示があり、即座に簡略化した。

- 段ごとの`jackStageCaps`配列・入力欄を削除し、**全段共通で「⚡推進力」タブの`jackCapUnit`（ジャッキ1本の能力）をそのまま使う**方式に変更（[M_APP_005.html](M_APP_005.html)、`renderJackStages()`）。
- ジャッキ径の目安表示（`jackDiameterFromCapacity()`）はテーブルの上に1回だけ「ジャッキ能力は全段共通: ○○kN（ジャッキ径目安 ○○mm）」という形で表示する。
- `copyStage2ToUpper()`から容量コピー処理を削除（幅だけコピーすればよくなったため）。
- JSON保存/復元から`jackStageCaps`関連の読み書きを削除。
- 検証: 円形立坑・jackCapUnit=2000kNで「ジャッキ能力は全段共通: 2000 kN（ジャッキ径目安 300 mm）」と表示され、保存JSONに`jackStageCaps`キーが含まれないこと、段別ジャッキ幅の保存復元は引き続き正しく動作することを確認。
- `service-worker.js`の`CACHE_NAME`を`v14`→`v15`に更新済み。

## 30. 多段ジャッキ配置を刃口推進工法専用に限定

ユーザーから「ジャッキ位置を指定する仕様は刃口推進工のみとしてください」と指示があった。曲線造成用の多段ジャッキを支圧壁に直接据え付けるこの構成は刃口推進（手掘り式）特有のもので、泥水・泥土圧・泥濃推進はシールド機内の中折れ機構等で曲線を作るため該当しない、という判断に基づく。

- `renderJackStages()`の先頭で`currentMethod !== 'blade'`ならテーブルの代わりに「この機能は刃口推進工法専用です」という案内を表示するようにした（[M_APP_005.html](M_APP_005.html)）。
- `setMethod()`（基本情報タブの推進工法切替）の末尾に`renderJackStages(computeWallBeff())`を追加し、支圧壁タブを開かなくても工法切替と同時に表示が切り替わるようにした。
- `jackStageWidths`配列自体は工法を切り替えても破棄しない（刃口推進に戻せば入力済みの値がそのまま復元される）。
- 検証: 刃口推進でテーブル表示→泥水推進に切替で案内メッセージに変化→刃口推進に戻すと直前の入力値（段別ジャッキ幅）を保持したままテーブルが復元されることを確認。
- `service-worker.js`の`CACHE_NAME`を`v15`→`v16`に更新済み。

## 31. 計算書「3.施工条件」の図を、線形ビジュアル表示と同じ描画に統一

ユーザーから「（計算書の施工概略図を）折角なので線形ビジュアル表示と同じにして」と指示があった。従来は`svgConstructionSchematic()`という簡易な模式図（直線=まっすぐな線、曲線=山なりのベジエ曲線というだけの簡略表現）を独自に描いていたが、基本情報タブに既にある「📈線形ビジュアル表示」（`drawAlignmentVisual()`、13章）の方がBC/IP/SP/EC/RO等の測量要素点・正確な曲線形状・作図補助線・注記円まで含む本格的な図なので、コードを重複させずそちらを再利用する形にした。

### 31.1 実装内容

- `drawAlignmentVisual()`を`drawAlignmentVisual(targetId, viewOverride)`という汎用シグネチャに変更した（[M_APP_005.html](M_APP_005.html)）。引数省略時は従来通り`#alignmentVisual`（基本情報タブのインタラクティブ表示）を描画し、グローバルなズーム/パン状態（`alignmentViewBase`）・注記円のドラッグ当たり判定（`lastCurveAnnotationScreens`）を更新する。`targetId`に別のcanvas idを渡すと、そのcanvasに同じジオメトリ・同じ描画スタイルで静的表示するだけで、上記グローバル状態は一切書き換えない（`isInteractive`フラグで分岐）。これにより関数を複製せずに再利用でき、既存のインタラクティブ表示（ズーム・パン・注記円ドラッグ）には一切影響しない。
- 計算書「3. 施工条件」に`<canvas id="reportAlignmentVisual">`を新設し、`drawAlignmentVisual('reportAlignmentVisual', {scale:1,offsetX:0,offsetY:0})`（常に等倍の自動フィット表示、ユーザーのズーム/パン状態とは独立）で描画するようにした。旧`svgConstructionSchematic()`は完全に削除。
- `CANVAS_REFRESH_FNS`に`reportAlignmentVisual`を追加し、14.3節の既存パターン通り「アコーディオンを開いた瞬間に再描画」に対応。`renderReport()`内でも、reportCardが既に開いている場合は即座に描画する分岐を追加。`printReport()`でも、printing-report適用直後（レイアウト幅が変わるタイミング）に明示的に再描画してから`window.print()`を呼ぶようにした。

### 31.2 検証

- 推進力タブをアクティブにした状態で計算書カードを開くと、線形ビジュアル表示と同じ図（曲線=青・直線=緑、BC/IP/SP/EC/RO/BP/EP、R/IA/CL/SL/TLの注記円）が正しく描画されることを確認（`getImageData`で実際に描画ピクセルが存在することを確認）。
- タブを切り替えずに計算書を開こうとするとcanvasのoffsetWidthが0のまま（ページ自体が非表示のため）で描画されない、という**テストスクリプト側の見落とし**を経て、実際のユーザー操作（該当タブを表示した状態でアコーディオンを開く）を再現した手順で正しく描画されることを確認した。
- 基本情報タブのインタラクティブ表示側は、ズーム操作後も`alignmentViewBase`（自動フィット基準値）が書き換わらないこと（回帰なし）を確認。
- `service-worker.js`の`CACHE_NAME`を`v16`→`v17`に更新済み。

## 32. 計算書の「計算過程」で項目名と数値が重なって表示されるCSSバグを修正

ユーザーから、計算書の途中式（4節等分布荷重・5節推進力の計算・6節区間推進力）で「Be＝…」等の行の文字が重なって読めない、というスクリーンショット付きの報告があった。

### 32.1 原因

`.calc-step`（`stepRow()`が生成する1行）はCSS Gridで「項目名（9em固定幅）・数値代入した式・結果」を横3列に並べていたが、`.cs-label`に`white-space:nowrap`を指定していたため、項目名が長い式（例:「Be = Bt(1+sin(45°-φ/2))/cos(45°-φ/2)」）は折り返されず、9em列の幅を超えてそのまま右へはみ出し、隣の「数値代入した式」列の文字と重なって描画されていた。CSS Gridは既定では列の境界で内容をクリップしないため、意図せず視覚的に重なってしまう。

### 32.2 修正内容

`.calc-step`を3列グリッドから「項目名を独立した行にし、その下に数値代入した式と結果を並べる」2段構成に変更した（[M_APP_005.html](M_APP_005.html)のCSS、`stepRow()`）。項目名がどれだけ長くても専用の行を使い切って折り返せるため、隣接する内容と重なることがなくなる。この構造変更で影響を受けるのは`.calc-step`を使う全ての箇所（4節Be/Ce/w/衝撃係数/活荷重、5節F0/μ′/τa/f、6節の各曲線k/Kx/λx）に及ぶが、生成関数`stepRow()`を直しただけで呼び出し側のコードは一切変更していない。

### 32.3 検証

`getBoundingClientRect()`で項目名行と式+結果行の実際の描画位置を計測し、項目名行の下端が式+結果行の上端より上にある（重なっていない）ことを確認。最も長い項目名「Be = Bt(1+sin(45°-φ/2))/cos(45°-φ/2)」を含む全行で崩れがないことをテキスト抽出でも確認済み。

- `service-worker.js`の`CACHE_NAME`を`v17`→`v18`に更新済み。

## 33. 「多段ジャッキ」「曲線造成用」という用語が誤りだったため全面訂正

27〜30章で作った支圧壁タブの段別ジャッキ配置機能について、ユーザーから用語・目的の説明が根本的に誤っていると指摘があった。

### 33.1 何が誤りだったか

- **「多段ジャッキ」は既に業界で別の意味を持つ言葉**: 太い油圧シリンダの中に一回り細い油圧シリンダを入れ子にして、ジャッキストロークを伸ばす製品を指す（テレスコピック構造）。本機能（段ごとにジャッキを分けて据え付ける配置）とは無関係で、この呼び方は誤解を招く。
- **「曲線造成用」は完全に誤り**: この配置は曲線とは一切関係ない。実際の目的は、**最下段のジャッキ1段だけで管を押すと、押輪（プレスリング）を介して推力が管の下部に偏って伝わってしまうため、複数段のジャッキを使い、押輪・ストラットを通してなるべく均一に推力が伝わるようにする**という、推力分散のための配置（直進時も含め常時使う）。

### 33.2 追加で得た現場知識（コメント・説明文に反映）

- 刃口推進はジャッキストローク40〜50cm程度で押し切ると、次のストラット（厚さ40cm程度）を降ろして押輪とジャッキの間に据え付け、再びジャッキを伸ばして押す動作を繰り返す。
- 刃口推進は切羽で人力（大口径では電動バックホウの場合あり）で掘削した土砂を車輪付きバケットに積み、発進立坑のウインチでワイヤーを巻き取って引き上げ、クレーンで揚重する必要がある。**このためストラット・押輪は上部が開いた馬蹄形になる**（円形だとバケットを真上に引き上げられないため）。
- 泥濃推進等、バケットの出し入れが不要な工法は押輪が円形になり、ジャッキは管高さ中心の左右に据え付けられることが多い（本UIは刃口推進専用のままとし、この単純な構成のUI化は今回は行っていない）。

### 33.3 修正内容

- カードタイトルを「🔩 多段ジャッキ配置（曲線造成用）」→「🔩 ジャッキ配置（推力分散用）」に変更。
- カード内の説明文・関連するJSコメント（`renderJackStages()`のヘッダコメント、刃口推進限定の理由コメント、`setWallLevelMode`まわりの注記等）をすべて上記の正しい説明に書き換えた（[M_APP_005.html](M_APP_005.html)）。「曲線とは関係ない」旨と、馬蹄形／円形という押輪形状の違いも明記した。
- 内部の変数名・関数名（`jackStageWidths`・`renderJackStages()`・`copyStage2ToUpper()`等）は「段（row/tier）」という意味では誤りではないため変更していない。誤解を招くのは「多段ジャッキ」という複合語と「曲線造成用」という誤った目的説明だけだったため、そこだけを是正した。
- 刃口推進工法限定（30章）の方針自体は変更していない（ユーザーから明示的な変更依頼はなかったため）。

### 33.4 検証

カードタイトル・説明文に「多段ジャッキ」「曲線造成用」の文字列が残っていないことをブラウザで確認済み。

- `service-worker.js`の`CACHE_NAME`を`v18`→`v19`に更新済み。

## 34. 推進定規のH形鋼規格選択・段別設置高さ・リアルタイム断面図を追加

ユーザーから、支圧壁タブの「ジャッキ配置（推力分散用）」カードについて3つの追加要望があった。

1. 推進定規に使うH鋼をH-200/250/300/350/400から選ぶ形式にする
2. 管外径・内径・基礎の断面図を描き、指定した下がり量の位置から選んだH鋼を、管外径に接する位置に左右の定規として描く。これをジャッキ位置の入力に合わせてリアルタイムに動かす
3. 各段の設置高さHを指定できるようにする（1段目=定規下面にジャッキ底面が来る(H=0)、2段目=定規上面にジャッキ底面が来る(H=H鋼の成)、3段目=2段目の上面に乗る(H=H鋼の成+ジャッキ外径)を初期値とする）

### 34.1 H形鋼規格選択

`railWidth`（幅の直接数値入力）を`railSize`という規格選択セレクト（H-200/250/300/350/400）に置き換えた。`H_BEAM_SIZES`テーブル（[M_APP_005.html](M_APP_005.html)）に成（H）・幅（B）とも規格と同寸（正方形断面シリーズ）の値を登録している。旧保存データの`railWidth`（数値）しか無い場合は、最も近い規格に丸めて後方互換をとる（`setProjectData()`）。

### 34.2 段別「設置高さ H」入力

`jackStageHeights`配列を新設し、段数が増える（`jackCount`÷2が増加する）たびに、ユーザー指定の初期値規則で新しい段の既定値を埋める（[M_APP_005.html](M_APP_005.html)の`renderJackStages()`）。
```
1段目: H = 0（定規下面にジャッキ底面）
2段目: H = railH（選んだH形鋼の成。定規上面にジャッキ底面）
3段目以降: H = 前段のH + ジャッキ外径（前段の上面に次段が乗る）
```
既存の`jackStageWidths`と同様、一度描画された段の値はユーザーが上書きした後も保持される（配列の長さを段数に合わせて伸縮するだけで、既存インデックスの値は書き換えない）。`copyStage2ToUpper()`（2段目の値を3段目以降にコピー）も、高さについては単純コピーではなく「前段の上に積み上げる」既定則を保つよう、段ごとにジャッキ外径分を足し込むようにした。

### 34.3 リアルタイム断面図（`drawJackPositionDiagram()`）

新しいcanvas（`#jackPositionDiagram`）に、管中心を原点とした座標系で以下を描画する。
- 管外径・内径（同心円）、基礎コンクリート（矩形）
- 左右の定規（H形鋼）: 「管底から指定した下がり量」を定規の**下面**の高さとし（`railBottomY = -Ro - railDrop`）、選んだH形鋼の成の分だけ上に立ち上げた**上面の内側角**が管外径の円に接する位置（`xTouch = √(Ro²-railTopY²)`）を定規の内側端とし、そこから外側に幅Bの矩形を描く。管底そのものから下がった位置に定規の上面を置くと円の外に出てしまい幾何学的に接しえないため、「定規の下面が下がり量の位置、そこから成の分だけ立ち上げた上面が管に接する」という解釈で初めて矛盾なく成立することを検算で確認した。
- 各段のジャッキ（円、左右対称2個）: 中心Y = 定規下面 + その段の設置高さH + ジャッキ半径、中心X = ±（ジャッキ幅の片側オフセット＋立坑中心オフセット）。ジャッキ径は「⚡推進力」タブのジャッキ1本の能力から`jackDiameterFromCapacity()`で求めた目安値を使う（全段共通、29章の方針通り）。
- **リアルタイム更新**: 段テーブルのジャッキ幅・設置高さ入力欄に`data-jack-field`/`data-jack-idx`属性を付け、`oninput`で（テーブル全体のinnerHTML再構築ではなく）`drawJackPositionDiagram()`だけを呼ぶようにした。この関数は配列（`onchange`時にのみ更新）ではなく、**テーブル内の入力欄の現在値を直接読み取って**描画するため、入力中の値がまだ配列に反映されていなくても図がリアルタイムに追従する。10章の教訓（`oninput`+innerHTML再描画で入力欄がフォーカスを失う問題）を踏まえ、`oninput`ではテーブルのDOM自体は一切書き換えていない。表の再構築・支圧壁厚さの再計算は従来通り`onchange`（フォーカスアウト時）の`calcBacklock()`に任せている。

### 34.4 検証

- 新規プロジェクトでjackCount=6・jackCapUnit=1500・H-200を設定し、`jackStageHeights`の既定値が`[0, 0.2, 0.45]`（=[0, railH, railH+jackDia]）と期待通りになることを確認。
- 段テーブルの幅・高さ入力欄に`input`イベントを発火させ、`getImageData`で断面図canvasの描画内容が即座に変化する（リアルタイム反映）ことを確認。
- 円形立坑への切替後もNaN・エラーなく描画されることを確認。
- JSON保存→復元で新形式（`railSize`直接）・旧形式（`railWidth`数値→最近傍規格への丸め）とも正しく復元されることを確認。
- `CANVAS_REFRESH_FNS`に`jackPositionDiagram`を登録済み（14.3節のパターン通り）。
- `service-worker.js`の`CACHE_NAME`を`v19`→`v20`に更新済み。

## 35. 円形立坑は「厚さ入力→幅を逆算」する向きに変更

ユーザーから「円形立坑の場合、支圧壁厚を指定して、計算で幅を出す使用（仕様）にして」と依頼があった。あわせて「ライナープレート内径は呼径より50mm小さい前提」（16章で既に実装済みの`linerD = wallCircleD - 0.05`）を維持するよう念押しがあった。

### 35.1 何を変えたか

`computeWallBeff()`（[M_APP_005.html](M_APP_005.html)）を、円形立坑（`circle`/`casing`）の場合だけ入出力の向きを逆転させた。

- **旧: 幅→厚さ**　`Beff = min(Bw, linerD)` を決めてから `sagitta = R - √(R²-(Beff/2)²)` を計算していた。
- **新: 厚さ→幅**　`wallThickness`（ユーザー入力、0〜Rにクランプ）から `Beff = 2√(t(2R-t))`（サジッタ式の逆算、`R = linerD/2`）で弦幅を求める。

矩形・小判型・オープン立坑は従来通り（幅を入力し、厚さは幾何学的制約が無いため別途手入力）で変更していない。

### 35.2 UI側の対応

`calcBacklock()`内の読取専用切替ロジックを、円形立坑では**`wallBw`が読取専用（自動計算）・`wallThickness`が入力**に、それ以外では**従来通り`wallBw`が入力・`wallThickness`は独立した手入力**になるよう入れ替えた。円形立坑選択時は`wallThickness`の下の注記に、計算されたBeffの値と「ライナープレート内側径=呼び径−50mm」という前提を明記している。

### 35.3 検証

- 立坑径5.0m・厚さ0.5mで、R=(5.0-0.05)/2=2.475mから逆算したBeff=2.983mが`wallBw`に正しく反映されることを確認（手計算値と一致）。
- 矩形立坑に切り替えると`wallBw`が編集可能に戻り、独立入力できることを確認（回帰なし）。
- 厚さに半径を大きく超える値（100m）を入れてもクランプが効き、Beffが直径（4.95m）を超えず、NaNが出ないことを確認。
- JSON保存→復元で、厚さの入力値・復元後に再計算されたBw双方が正しく一致することを確認。
- `service-worker.js`の`CACHE_NAME`を`v20`→`v21`に更新済み。

## 36. バックロック（分割式プレキャスト支圧壁）を実カタログ準拠で追加、現場打ちの代わりに使用する前提に

> **※このBeff算出方法は後に47.11章で再び変更されています。** 円形立坑・バックロックのBeff（有効幅）算出方法は本章→37.1章→38章→47.7章→47.10章→47.11章の順に計6回改訂されており、**現状の正しい仕様は47.11章**です。本章の記述は経緯として残していますが、現状とは異なります。

ユーザーから、廃材削減の観点から支圧壁を現場打ちコンクリートではなく「バックロック」（秩父コンクリート工業、プレキャストブロックを積み重ねライナープレートとの隙間に間詰めコンクリートを充填する工法、NETIS番号KT-160010-A）を使用する前提で計算するよう依頼があり、カタログPDF（`バックロックカタログ.pdf`）と寸法計算書（`反力面積.xlsx`）を参照資料として提示された。

### 36.1 参照資料から読み取った内容

**カタログ**: バックロックには2形状ある。
- **スクウェア形**（矩形立坑向け）: BR-SS/S/M/L/LL、対象呼び径φ800〜φ3000（隣接型番の範囲は重複）、幅2.4〜4.6m・厚さ0.6/0.8m・高さ0.5m/ピース。
- **テーパー形**（円形＝ライナープレート立坑向け、「間詰め材料を大きく減少させることができる」台形断面）: BR-SST/ST/MT/LT、幅1(下端)・幅2(上端)・厚さ0.5〜0.8m・高さ0.5m/ピース。

**寸法計算書（反力面積.xlsx）**: 円形立坑でテーパー形を使う場合の「間詰めコンクリート数量計算」。ライナー内側半径R・支圧壁厚さTから中心角θ・弦幅Wを求め（このW算出式は35章で実装済みのBeff逆算式と数学的に同一であることを検算で確認）、弓形断面積からバックロック台形ブロック自身の断面積を差し引いた残りが間詰めコンクリートの必要断面積になる、という計算。R=2.5m・T=0.572mの参照例で、アプリの実装値と算出値が完全一致することを確認済み（36.4節）。

### 36.2 実装内容

- `BACKROCK_SQUARE`・`BACKROCK_TAPER`（[M_APP_005.html](M_APP_005.html)）にカタログの型番・寸法をそのまま定義。呼び径から`selectBackrock()`で該当型番を選ぶ（範囲重複時は最小の型番を優先）。
- 支圧壁タブに新カード「🧱 バックロック（分割式プレキャスト支圧壁）」を追加。
  - 「バックロックを使用／現場打ち（手入力）」の切替（既定: **使用する**＝ユーザー確認済み方針）。
  - 「自動（立坑形状で判定）／スクウェア形／テーパー形」の形状タイプ切替（既定: 自動＝円形→テーパー、それ以外→スクウェア）。ユーザーが手動でタイプを上書きできる（矩形立坑でテーパー形を選ぶ等の非標準組合せも可能。その場合は警告文を表示）。
  - 選定結果（型番・ピース数・重量）と、テーパー形×円形立坑の場合は間詰めコンクリート数量の計算過程を表示。
- **寸法の反映方法**: `applyBackrockSelection()`が、立坑形状（`wallShaftType`）を優先して判定する（選んだ形状タイプではない点に注意）。
  - 常に`wallThickness`＝選定型番のT（厚さ）を反映。
  - 矩形等（非円形）は`wallBw`＝選定型番の幅（スクウェア形はW、テーパー形を強制選択した場合はW1で代用）を反映。
  - 円形・ケーシングは`wallBw`を直接上書きしない（35章の通り、Beffは常に厚さから幾何学的に逆算される値が正のため）。**この同期漏れが実装中に一度バグとして発生**（バックロック使用時、円形立坑でwallBwの表示がBeffと食い違うことがあった）。原因は「`!backrockUse`のときだけbwEl.valueをBeffに同期する」という条件分岐が残っていたため。円形立坑では使用有無によらず常に同期する形に修正して解消した。
- 旧来の仮想テーブル`BL_TABLE`（実在しない型番"BL-400"〜"BL-2000"を推進力Fだけで選ぶ簡易ロジック）は完全に削除し、「🔩バックロック選定」カードは「✅総合判定」（3段階照査のOK/NGのみ）に置き換えた。
- JSON保存/復元に`backrockUse`・`backrockType`を追加（未保存の旧プロジェクトは`backrockUse=true`扱いとし、新方針に自然移行する）。

### 36.3 間詰めコンクリート数量計算（`renderBackrockGapConcrete()`）

反力面積.xlsxの式をそのまま実装。テーパー形かつ円形立坑の場合のみ表示する。
```
θ = 2×acos((R-T)/R)　[度]
W = 2√(T(2R-T))　（=Beffと同一式）
A1 = R²π(θ/360) − W(R-T)/2　… 弓形断面積
A2 = (W1+W2)/2×(T-リップ) + リップ×W1　… バックロック台形ブロックの断面積（リップ=底部鋼材リップ高さ90mm、カタログ図の固定寸法として全サイズ共通で採用）
A  = A1 − A2　… 間詰めコンクリート断面積
V  = A × 実高さ（ピース数×0.5m）
```
Aが負（バックロックが弓形断面より大きい）場合は組合せ見直しを促す警告を表示する。

### 36.4 検証

- 反力面積.xlsxと同一条件（R=2.5m・T=0.572m）で、θ・W・A1をアプリの式で独立に計算し、Excelのキャッシュ値と小数点以下ほぼ完全に一致することを確認（θ=79.076…°、W=3.183m、A1=1.2446m²）。
- φ1350・円形立坑でBR-ST（幅1=2.8/幅2=1.6/厚さ0.6）が自動選定され、wallThickness=0.6→Beff=3.231mに正しく反映されることを確認。
- φ900・矩形立坑でBR-SS（幅2.4/厚さ0.6）が自動選定され、wallBw=2.4に正しく反映されることを確認。
- 形状タイプの手動上書き（矩形+テーパー、円形+スクウェア）で警告文が表示され、数値もフォールバックロジック通りになることを確認。
- JSON保存→復元で`backrockUse`・`backrockType`・選定結果が正しく戻ることを確認。
- `service-worker.js`の`CACHE_NAME`を`v21`→`v22`に更新済み。

## 37. 実際の承認済み計算書（BR-SST４台1350計算書.pdf）と突き合わせてバックロック計算を全面見直し

ユーザーから、36章で実装したバックロック機能について「バックロックの計算書を参考にしてもう一度全体を見直して」と依頼があり、同一現場（館町雨水・新幸技建・木下、φ1350mm・BR-SST×4台）の**実際に承認された計算書PDF**（`BR-SST４台1350計算書.pdf`）が提示された。36章はカタログとExcel寸法計算書という「参考資料」からの実装だったが、本章はそれを実際の設計実務の結果と直接照合する検証フェーズにあたる。

### 37.1 発見1: 円形立坑でもカタログの幅をそのまま使う（35章のBeff逆算をバックロック使用時は適用しない）

> **※これも後に47.11章で再び変更されています。現状の正しい仕様は47.11章を参照。**

35章では「円形立坑は厚さ入力→Beffを幾何学的に逆算」という仕様にしていたが、これは**現場打ちコンクリートを想定した仕様**だった。実際のBR-SST計算書を見ると、円形立坑であってもバックロック使用時は**カタログの型番が持つ幅（W1/W2）をそのまま`Bw`として使用**しており、厚さから幅を逆算してはいない（プレキャストブロックは寸法が製品として固定されているため、当然といえば当然）。

- `applyBackrockSelection()`: 円形立坑のときに`wallBw`の反映をスキップしていた分岐を削除し、円形立坑でも矩形と同様に常に`wallBw = model.W1`（テーパー形）/`model.W`（スクウェア形）を反映するよう変更。
- `computeWallBeff()`: 円形/ケーシング立坑の分岐内で`!backrockUse`のときだけ35章のサジッタ逆算（厚さ→Beff）を行い、`backrockUse`のときは`Beff = min(Bw, maxBw)`（カタログ幅をそのまま採用、立坑内径でクランプするだけ）に変更。現場打ち（`backrockUse=false`）の挙動は35章のまま維持。

### 37.2 発見2: 受働土圧安定照査（③）に安全率的な係数αが必要

参照計算書の③照査は、単純な「受働土圧 Ep ≥ 推進力 F」ではなく、Ep側に係数（実務では1.5〜2.5程度、地盤条件や施工者の判断による安全側の割り増し）を掛けていた。

- 支圧壁の諸元入力に`wallEpAlpha`（範囲1.5〜2.5、既定値2.5）を追加。
- `calcBacklock()`の③照査式を`Ep = α × ((a)自重項+(b)上載荷重項+(c)粘着力項)`に変更（旧: α無し、単純合算のみ）。
- 検証: α=2.5、φ1350の試算で `Ep = 2.50×1080 = 2700 kN ≥ F=1500 kN` → OK。α無し版（=1.0相当）だと1080 kNとなり不足するケースも起こり得るため、この係数の有無は照査結果を左右する重要な項目だった。

### 37.3 発見3: プレキャストブロック自体の曲げ照査（④）が丸ごと欠落していた

36章までの実装は「支圧壁がどれだけの反力・偏心・受働土圧に耐えるか」という**地盤・支持力側**の3照査（①②③）のみで、**バックロックのプレキャストコンクリートブロック自体が、ジャッキの集中荷重で曲げ破壊しないか**という照査が存在しなかった。参照計算書はこれを④として明示的に計算しており、鉄筋コンクリート梁の許容応力度設計（複鉄筋矩形梁の弾性理論）をプレキャストブロックの鋼材被覆部分を「鉄筋」とみなして適用していた。新規に追加：

```
q = F/(Bo×H0)                         等分布荷重（Bo=支圧壁全幅、H0=高さ）
L = 2×片側ジャッキ幅、L1=L2=(Bo-L)/2   ジャッキ反力の作用区間
Ra = q/(2L)×(L1+L+L2)×(L1+L-L2)        反力
Mc(中央) = Ra²/(2q) - L1×Ra
Ma = Mb = -q/2×L1²                     支点付近の負モーメント
Mapplied = max(|Mc|,|Ma|,|Mb|)

b=1000mm, H=wallThickness×1000mm, t=鋼材部厚さ(既定11mm), cover=5.5mm
d=H-cover, dp=cover, As=t×b, p=pp=As/(b×d), n=7（換算係数）
k = √(2n(p+pp・dp/d) + n²(p+pp)²) - n(p+pp)
Lc = k/2(1-k/3) + n・pp/k・(k-dp/d)(1-dp/d)
j = (1-dp/d) - k²/(2n・p(1-k))・(k/3-dp/d)
v = j・p/Lc
σca = Fc/3 = 60/3 = 20 N/mm²（バックロックのコンクリート強度Fc=60N/mm²は全型番共通の固定値）
σsa = 鋼材の許容引張応力度（既定140N/mm²、入力可）
Mrc = σca・b・d²・Lc、Mrs = σsa・b・d²・Lc・v
抵抗モーメント M = min(Mrc, Mrs) ≥ Mapplied → OK
```

- 支圧壁の諸元入力に`wallSteelT`（鋼材部厚さt、既定11mm）・`wallSteelSa`（鋼材許容引張応力度σsa、既定140N/mm²）を追加。
- `backrockUse=true`のときだけ④照査を追加し、カードタイトルを「支圧壁3段階照査結果」→「支圧壁4段階照査結果」に動的変更。④がNGのときは専用の警告文（ジャッキ据付位置を端に寄せる／鋼材厚さや壁厚を見直す旨）を表示。
- **検証（重要）**: 独立にブラウザのJSコンソールで同じ式を再計算し、参照PDFの印字値とほぼ完全一致することを確認した：Ra=653.39(参照653.3)、Mc=93.17(93.2)、Ma=-55.82(-55.8)、Mb=-11.03(-11.0)、p=0.02224(0.0222)、k=0.33035(0.3303)、x=163.36(163.36)、Lc=0.29578(0.2958)、j=0.93707(0.9371)、v=0.07047(0.0705)、σca=20(完全一致)、Mrc=1446.56(1447)、Mrs=713.61(714)。formula文字列・限界値表示も含めて実装後にブラウザで再現し、同じ値（Mrc=1447、Mrs=714、σca=20）が出ることを確認済み（37.5節）。

### 37.4 発見4: カタログ範囲外の型番を採用する実務（手動型番選択機能の追加）

φ1350mmは、カタログ上はBR-ST（対象φ1100〜1800）が該当するはずだが、参照計算書は実際にはBR-SST（対象φ800〜1200、φ1350はその範囲外）を採用していた。これは「カタログの対象範囲はあくまで目安であり、③④のような個別の構造計算で安全性を確認できれば、より小型・安価な型番を範囲外で採用してよい」という実務判断であり、アプリの自動選定ロジック（範囲マッチのみ）では再現できなかった。

- 支圧壁タブに型番選択の`<select id="backrockModelSelect">`を追加（「自動選定（呼び径の対象範囲から判定）」＋カタログ全型番）。`backrockModelOverride`（既定空文字＝自動）というグローバル変数で管理。
- `setBackrockModelOverride(name)`・`refreshBackrockModelOptions()`を新規追加。後者は`backrockType`（自動判定後の実効タイプ）が変わるたびに（`setBackrockType()`・`setWallShaft()`から呼び出し）選択肢を作り直し、現在のオーバーライドが新しいテーブルに存在すれば維持、存在しなければ自動選定に戻す。
- `applyBackrockSelection()`: `backrockModelOverride`が設定されていれば自動選定より優先。手動選択が自身の対象範囲外のとき（`outsideOwnRange`）は、自動選定の範囲外警告（赤・「メーカーに確認してください」）とは別に、注意喚起色（`var(--warn)`）で「③④照査で安全性を個別に確認してください」という趣旨の文言を表示する（実例として本計算書を明記）。
- JSON保存/復元（`getProjectData()`/`setProjectData()`）に`backrockModelOverride`を追加。

### 37.5 ブラウザでのエンドツーエンド検証

`localStorage.clear()`で初期化した状態から、参照計算書と同じ条件（呼び径φ1350、円形立坑、バックロック使用、テーパー形、型番を手動でBR-SSTに上書き）を再現し確認した。

- 型番BR-SSTが手動指定として選定され、`wallBw=2`・`wallThickness=0.5`（カタログのW1=2.0m・T=0.5mそのまま）が反映され、35章のサジッタ逆算は行われないことを確認。
- 範囲外警告文（「呼び径φ1350mmはBR-SSTの対象範囲外での採用です…実例: BR-SST４台1350計算書.pdf も同様の範囲外採用」）が注意喚起色で正しく表示されることを確認。
- 試算値（Hw=2.0m・hc=1.0m・F=1500kN・fa=0.6N/mm²・γ=18・φ=30°）で①②③④すべてOKとなり、④の途中式・Mrc/Mrsの値が37.3節の独立検算と一致することを確認（NaN・console error無し）。
- `getProjectData()`→`setProjectData()`のラウンドトリップで`backrockModelOverride='BR-SST'`が正しく復元され、`wallBw`/`wallThickness`もBR-SSTの値に再反映されることを確認。
- `backrockModelSelect`のDOM値が復元後も`'BR-SST'`のままであることを確認。
- `service-worker.js`の`CACHE_NAME`を`v22`→`v23`に更新済み。

## 38. バックロックの据付位置（角当たり）計算・補強リング・推進中心線オフセットへの対応

ユーザーから、円形立坑（ライナープレート）内でのバックロック（テーパー形）の実際の据付方法について、手書き参考図（φ5000立坑・BR-ST相当・R=2475mm）付きで説明があった。要点：

1. ハッチング部分（トラペゾイド＝台形ブロックと円弧の間の隙間）はすべて間詰めコンクリートを打設する。
2. バックロックは、幅短辺(W2、ライナー側の狭い方の辺)の角がライナープレート内面に当たる位置に設置する。
3. ライナープレートに補強リング（H鋼）がある場合は、そのH鋼サイズを指定できるようにし、「ライナープレート呼び径から求めた半径 − H鋼せいの半分」の位置に幅短辺の角が当たるという考え方で計算する。
4. バックロックは推進方向線に対して必ず直角に据え付けられる（管を押す方向に反力を有効に発揮するため）。これは立坑中心と推進線がオフセットしていても変わらない。ずれるのは支圧壁の中心ではなく、推進中心線・ジャッキの位置の方である。

### 38.1 それまでの実装との違い

36〜37章までの実装は、円形立坑でのバックロック（テーパー形）使用時、`wallThickness`（カタログのT値、例: BR-STならT=0.6m）をそのまま「支圧壁厚さ・サジッタ」として扱っていた。これは「台形ブロックがカタログの厚さ分だけそのままライナー面に密着している」という簡略化した仮定であり、台形ブロックの幅短辺(W2)自体が持つ有限の幅を無視していた（W2=0の点として扱っていたのと同じ）。ユーザー提供の参考図から、実際には**台形ブロックの前面（W1側）は、幅短辺(W2)の角がライナー内面に接するところまでしか押し込めない**ことが分かった。これは幾何学的に、常に旧実装の`hCenter=R-T`より前面が中心寄りになる（＝ライナーとの間により大きな間詰め空間ができる）方向の補正になる。

### 38.2 角当たり位置の計算（`computeBackrockPlacement()`）

新規関数（[M_APP_005.html](M_APP_005.html)）:
```
yFar = |offset| + W2/2　… 立坑中心から見て遠い方の、幅短辺の角の側方位置
hFar = √(R_eff² - yFar²)　… その角の、立坑中心からの奥行位置（角がR_effの円周上にある）
hCenter = hFar - T　… 幅広辺(W1、前面)の、立坑中心からの奥行位置
```
`offset`は`wallCenterOffset`（既存、立坑中心と推進中心のオフセット）。台形ブロックは推進中心線に対して左右対称に据え付けられる（立坑中心に対しては非対称になりうる）という考え方（本章冒頭4.）を反映し、`offset`を側方位置に加える。角がライナーの外に出てしまう場合（`yFar >= R_eff`）は`ok:false`を返し、UI側で警告を表示する。

### 38.3 補強リング

支圧壁タブの円形立坑入力に「ライナープレート補強リング（なし／あり）」の切替と「H鋼サイズ（せい）[mm]」入力を追加。`effectiveLinerRadius(R)`が、リングありの場合`R_eff = R - H/2`を返す（本章冒頭3.のユーザー指定の考え方をそのまま実装。Hはmm入力をmに変換）。この`R_eff`は、角当たり計算だけでなく、`renderJackStages()`の局所支圧壁厚さ計算・`renderBackrockGapConcrete()`の間詰め数量計算など、円形立坑のバックロック関連計算すべてで統一して使う（現場打ち・35章のサジッタ逆算にも`R_eff`を適用し、リングの有無に関わらず一貫させた）。

### 38.4 `computeWallBeff()`の変更

円形立坑・バックロック使用・テーパー形の場合、`sagitta`・`hCenter`を`computeBackrockPlacement()`の結果から求めるよう変更（従来の`sagitta = wallThickness`を置き換え）。`backrockCurrentModel`という新規グローバル変数（`applyBackrockSelection()`が選定するたびに設定）を介して、現在選定中のモデルのW1/W2/Tを参照する。スクウェア形を円形立坑に強制適用した場合や、まだモデル未選定の場合は、従来通り`wallThickness`を直接使うフォールバックを残した。

### 38.5 間詰めコンクリート数量計算・局所支圧壁厚さの再計算

- `renderBackrockGapConcrete()`（36章で新設）は、`wallThickness`を直接読む代わりに、`computeWallBeff()`が返す補正後の`sagitta`・`R_eff`・`hCenter`を使うよう修正。弓形断面積A1の計算式自体（R²π(θ/360)-W(R-T)/2）は変更していない — 台形ブロックの前面(W1側)が占める深さがhCenterまでしか届かないことで生じる「幅方向の余白」を含め、弓形全体を間詰め対象とする36章の考え方と整合する。
- `renderJackStages()`の各段の局所支圧壁厚さ計算（`√(R²-x²)-hCenter`）も、`R`を`R_eff`に、`hCenter`を`computeWallBeff()`から直接取得する値に統一した。

### 38.6 平面図キャンバスの更新（`drawWallPlanView()`）

円形立坑・バックロック使用・テーパー形の場合、従来の「円弓形（レンズ）」表示を、実際の台形ブロック形状（W1・W2・T）＋ハッチングされた間詰めコンクリート領域＋オフセット付き推進中心線の表示に置き換えた。台形は推進中心線（`offset`だけ立坑中心からずれた線）に対して左右対称に描く。補強リングがある場合は、有効径R_effの円を橙の破線で追加表示する。据付不可（`placementOk=false`）の場合は警告文を表示する。現場打ち・スクウェア形強制適用時は従来通りの円弓形表示を維持。

### 38.7 検証

- ユーザー提供の参考図と同条件（φ5000立坑、BR-ST: W1=2.8m/W2=1.6m/T=0.6m、R=2.475m、offset=0、リングなし）で`hCenter=1.7421m`を算出。手計算（yFar=0.8, hFar=√(2.475²-0.8²)=2.3421, hCenter=2.3421-0.6=1.7421）と完全一致を確認。
- オフセット0.3m・補強リングH150mmのケースで、`R_eff=2.475-0.075=2.4`、`yFar=0.3+0.8=1.1`、`hFar=√(2.4²-1.1²)=2.1331`、`hCenter=1.5331`を算出。手計算と一致し、console errorなし。
- 立坑径を意図的に小さく（φ1.5m）してBR-STが収まらない状況を作り、`placementOk=false`・`hCenter=0`のフォールバックに正しく切り替わり、NaNやconsole errorが出ないことを確認。バックロックカード内に警告文が表示されることを確認。
- ブラウザで平面図キャンバスを実際にレンダリングし、円・台形・ハッチング・型番ラベル・角当たり位置の注記が表示されることをスクリーンショットで確認。
- `getProjectData()`→`setProjectData()`のラウンドトリップで`ringUse`・`ringH`（および既存の`centerOffset`）が正しく復元されることを確認。
- `service-worker.js`の`CACHE_NAME`を`v23`→`v24`に更新済み。

**未確認事項（次回・ユーザー確認が必要）:** 台形ブロックの据付位置（推進中心線に対して左右対称、幅広辺W1が中心寄り・幅短辺W2がライナー側）という幾何モデルは、ユーザー提供の手書き参考図からの読み取りに基づく実装であり、実際の計算書等で数値的に裏付けを取れていない（37章のBR-SST計算書のような一次資料と未突合）。平面図キャンバスの表示をユーザー自身に確認してもらい、参考図の見た目と一致するか、特に「推進中心線に対して対称」という配置の解釈が正しいかを確認すること。

## 39. 「バックロックの選定.xlsx」による38章の検算・「Tab2」表記修正・支圧壁高さの0.5mピッチ丸め

### 39.1 バックロックの選定.xlsxによる38章の裏付け

ユーザーから、同じ館町雨水現場の一次資料`バックロックの選定.xlsx`（`01_現場別書類\19_東京都\令和08年度\館町雨水(新幸技建)\02_計画書\03_推進工\`）を調べるよう依頼があった。Sheet1のB21セル（ラベルは「間詰COを含む支圧壁幅」だが、中身は支圧壁前面から立坑中心までの奥行き量＝本実装の`sagitta`に相当）の式：

```
D21 = B2/2（ライナー内側半径R=2.475）
E21 = B6/2（バックロック幅短辺W2の半分=0.8）
B21 = B7 + (D21 − √(D21² − E21²))　… B7=バックロック厚さT=0.6
    = 0.6 + (2.475 − √(2.475²−0.8²)) = 0.732859m
```
が、38章で実装した`sagitta = T + (R − √(R² − (W2/2)²))`と完全に同一の式であり、cached値`0.7328588855493784`はアプリの計算結果`0.7328588855493785`と（浮動小数点誤差の範囲で）完全一致することを確認した。これにより、**38章の「未確認事項」（角当たり位置の幾何モデルが一次資料で裏付けられていない）は解消**された。Sheet2の型番表（スクウェア形・テーパー形とも）もアプリの`BACKROCK_SQUARE`/`BACKROCK_TAPER`と数値が完全一致することを確認した。

このExcelにはオフセット・補強リングの概念は無く（それらはユーザーの口頭説明のみに基づく）、角当たり位置の核心式のみが裏付けられた形。

### 39.2 「Tab2」という表記が紛らわしかった問題

支圧壁タブの「推進力 F」入力欄まわりに「Tab2計算結果を自動反映」「Tab2の計算結果を読込」という表記があった。これは開発時の内部的な呼び名（推進力タブを指す）が残っていたもので、実際のタブ並び順（入出力・基本情報・立坑・推進力・支圧壁・測量）と数字が一致せず紛らわしいとユーザーから指摘があった。「推進力計算結果を自動反映」「推進力計算結果を読み込む」に修正（`syncForceFromCalc()`の未計算時トースト文言、および内部コメントも合わせて修正）。

### 39.3 支圧壁高さの0.5mピッチ丸め（プレキャスト対応）

ユーザーから「支圧壁高さはプレキャストに変わったので500mm単位にしなければならない」との指示があり、39.1のExcelの「バックロック使用時の補正高さ」（`B12 = ROUNDUP(設計支圧壁高さ/0.5,)×0.5`）と同じ考え方を実装した。

- 新規関数`effectiveWallHw()`：バックロック使用時、入力された（または水準高モードで導出された）「設計高さ」を、選定モデルのピース高さ（`backrockCurrentModel.H`、現状全型番0.5m）の倍数に切り上げた「施工高さ」に変換する。現場打ち（`backrockUse=false`）では丸めを行わず設計高さをそのまま使う。
  ```js
  function effectiveWallHw() {
    const HwDesign = n('wallHw') || 0;
    if (!backrockUse || !HwDesign) return HwDesign;
    const pieceH = (backrockCurrentModel && backrockCurrentModel.H) || 0.5;
    if (!pieceH) return HwDesign;
    return Math.ceil(HwDesign / pieceH - 1e-9) * pieceH;
  }
  ```
- `calcBacklock()`は`applyBackrockSelection()`（backrockCurrentModelの確定）を関数の先頭に移動し、そのすぐ後で`Hw = effectiveWallHw()`を計算するよう再構成。以降、①②③④の照査・受働土圧・曲げ照査などすべての構造計算がこの施工高さHwを使う（従来は`n('wallHw')`＝設計値をそのまま使っていた）。
- `calcLocalBearing()`（局所支圧照査）・`drawWallSectionView()`（断面図）も同様に`effectiveWallHw()`を使うよう修正。断面図では、設計高さと施工高さが異なる場合に「設計高さX.XXm→プレキャストは0.5m単位のため施工高さHw=Y.YYmに切り上げ」という注記を描画するようにした。
- UIには新規の注記欄を2箇所追加：
  - 「支圧壁の諸元」カード内`#wallHwNote`：直接入力・水準高モードいずれでも、設計高さと施工高さの差（および何ピース・何段か）を文章で表示。
  - 水準高モード内`#wallLevelTopActualNote`：バックロック使用時、丸めにより実際の支圧壁天端水準高が設計値からどれだけ変わるかを表示（39.1のExcelのB13「補正支圧壁天端水準高」に相当）。
- `getProjectData()`/`setProjectData()`はこれまで通り**設計高さ（丸め前の`wallHw`入力値）を保存**する（施工高さは毎回`effectiveWallHw()`で再計算されるため、保存不要）。

### 39.4 検証

- Excel同条件（呼径5.0m・推進管1350・BR-ST、設計高さ2.42m）で`effectiveWallHw()`が`2.5`（Excelの「補正高さ」と完全一致）を返すことを確認。
- 水準高モード（基礎コンクリート天端155.55m・支圧壁天端157.97m）で、施工高さ2.5mを反映した実際の天端水準高が`158.05m`と算出され、Excelの「補正支圧壁天端水準高」（B13）と完全一致することを確認。
- ①基礎反力照査の式表示が`f=F/(Bw×Hw)=1500/(2.80×2.50)`と、丸め後のHwを正しく使っていることを確認。
- 高さがちょうど0.5m単位（2.5m）の場合は「ちょうど過不足なし」という文言に、現場打ち（backrockUse=false）では丸め自体が行われず注記も出ないことを確認。
- `getProjectData()`→`setProjectData()`のラウンドトリップで、保存される`Hw`が丸め前の設計値（2.42）のまま維持されることを確認。
- console errorなし。

## 40. 印刷（PDF保存）時のファイル名を「現場名＋スパン名＋推力計算」にする

`printReport()`（[M_APP_005.html](M_APP_005.html)）は`window.print()`を呼ぶだけで、Chrome等のブラウザの「PDFに保存」の既定ファイル名は`document.title`から決まる。従来は常に固定タイトル「MYcurve 推進力設計 M_APP_005」のままだったため、印刷のたびにファイル名を手動で書き換える必要があった。

印刷直前だけ`document.title`を`${siteName}${siteSpan}推力計算`に一時的に書き換え、`window.print()`呼び出し後（`printing-report`クラスを外すのと同じ`setTimeout`内）に元のタイトルへ戻すようにした。現場名未入力時は「現場名未入力」で代用する。ブラウザタブのタイトル自体は印刷中の一瞬だけ変わるが、印刷ダイアログが閉じればすぐ元に戻るため実害はない。

検証: `window.print`を一時的にモック関数に差し替え、`printReport()`呼び出し時に`document.title`が`館町雨水工区A-1推力計算`になっていること、300ms後に元のタイトルへ復元されることを確認。現場名・スパン未入力時のフォールバックも確認。`service-worker.js`の`CACHE_NAME`を`v25`→`v26`に更新済み。

## 41. 支圧壁 寸法図（平面・断面）のテキスト重なりを解消、断面図に管内径を追加

ユーザーから「平面図は字が重なってまったく内容がわからない」「断面図は数値は合っているが絵がおかしい、外径だけでなく内径の線も入れて」と指摘があった。

### 41.1 原因

`drawWallPlanView()`（円形立坑・テーパー形の分岐）は、38章で台形・ハッチング表示に変更した際、ライナー径・ライナー内側径・補強リング・推進方向・型番＋W1/W2/T・角当たり位置、といった説明文をすべて`ctx.fillText()`でキャンバス中央付近に固定ピクセルオフセット（`-8`,`+16`,`+30`等）で積み上げていた。円形立坑の平面図キャンバスは高さ230px程度しかなく、6〜8個の長い文字列が同じ横位置（`tx(0)`付近）に密集するため、文字同士が重なって判読不能になっていた。

`drawWallSectionView()`側は、39章で追加した「設計高さ→施工高さ」の注記が、既存の「支圧壁幅」ラベルとほぼ同じ座標（矩形立坑等`sagitta=0`の場合は完全に同一座標）に描画される既知の不具合もあった。

### 41.2 対応

キャンバスには**図形（円・台形・矩形・ハッチング・寸法線）のみ**を描画し、説明文はすべてキャンバスの下に新設したHTMLキャプション欄（`#wallPlanViewCaption`・`#wallSectionViewCaption`、`report-note`スタイルで色付き凡例として1行ずつ`<div>`で出力）に移した。HTML要素は自然に折り返す・重ならないため、キャンバス側の文字重なりは構造的に発生しなくなる。`setWallPlanCaption()`という小さなヘルパーを新設し、円形／矩形いずれの分岐からも同じ形式でキャプションを書けるようにした。

断面図（`drawWallSectionView()`）にも同様の対応に加え、管の**内径**を新たに描画するようにした。従来は外径（`pipeOuter`）の円のみだったが、`pipeInner`（自動計算済みの内径フィールド）を使い、外径と同心の内径円をグレーの破線で追加し、キャプションに「外径φX.XXm／内径φX.XXm」を明記した。

### 41.3 検証

- 円形立坑・テーパー形（バックロック）・矩形立坑・円形立坑＋現場打ち（レンズ表示）の3パターンで`drawWallPlanView()`・`drawWallSectionView()`を実行し、キャプション欄のテキストが正しく生成されることを確認。
- ソースコードを静的に確認し、`drawWallPlanView()`内の`ctx.fillText()`呼び出しが「データ未入力時のプレースホルダー」1箇所のみになっていること（＝説明文がすべてキャプションに移り、キャンバス上の文字重なりが構造的に起こり得ないこと）を確認。`drawWallSectionView()`も「基礎コンクリート上面」「推進管芯hc=」「寸法線ラベル×3」の計5箇所のみで、いずれもキャンバス上の別々の領域（左上・管の右側・左端の寸法ガター）に独立して描かれており、重なる位置関係にないことを確認。
- `pipeInner`（内径）が正しく読み取られ、外径と同心の破線円としてキャプションに反映されることを確認。
- console errorなし。`service-worker.js`の`CACHE_NAME`を`v26`→`v27`に更新済み。

**注記:** この検証はブラウザの`Browser`ペインでのスクリーンショット撮影ができない環境だったため、実際のピクセル描画をスクリーンショットで目視確認できていない。キャンバス側のテキスト描画コードを静的に洗い出して重なりが構造的に発生しないことを確認し、キャプション欄の生成内容をテキストで検証する方法で代替した。次回、実際の見た目をスクリーンショットで確認し、必要なら追加調整すること。

## 42. 水準高モードの管中心高さ計算が誤って外径基準になっていたバグを修正

ユーザーから、「推進管の管頂は156.555(管底)＋1.35(管径)＋0.125(管厚)＝158.030なのに、断面図では補正支圧壁天端水準高158.050より高く描かれている」との指摘があった（メッセージ中の「165.555」は156.555の入力ミスと判断。156.555＋1.35＋0.125＝158.030はぴったり合うため）。

### 42.1 原因（数値計算そのもののバグ）

これは41章のような描画上の見た目の問題ではなく、`updateWallLevelDerived()`の**計算式自体が間違っていた**。

```js
// 修正前（バグ）
const D = n('pipeOuter') || 0;
const hc = (elInvert - elBase) + D / 2;   // 管底水準高 + 外径/2
```

下水道・推進工事における「管底水準高」は、通常**管の内側下端（フローライン）**を基準とする慣例だが、このコードは`+外径/2`を使っており、暗黙に「管底＝管の外側下端」という異なる前提で書かれていた。39章までに参照した一次資料`反力面積.xlsx`のB17セル（管中心水準高）の式は`=B9+B14/2`（B9=管底水準高、B14=**管内径**）であり、内径基準になっていることを確認済みだった（32章の裏付け作業で目にしていたにも関わらず、直接この式の実装には反映していなかった）。

内径基準・外径基準の違いは、片側管厚分（今回の例では0.125m）だけ管中心の計算結果がずれる。実際、修正前は`hc=1.805`（外径基準）、修正後は`hc=1.680`（内径基準、Excelと完全一致）になり、この0.125mのずれが「管頂が支圧壁天端を超えて描かれる」という一見描画バグに見える症状の真因だった。

### 42.2 修正

```js
// 修正後
const Di = n('pipeInner') || 0;
const hc = (elInvert - elBase) + Di / 2;   // 管底水準高 + 内径/2
```
水準高モードの説明文（「管中心高さは管底水準高＋管外径/2から求めます」）も「管底水準高＋管内径/2」に修正。直接入力モード（h_cを直接入力する場合）はこの式を使わないため影響なし。

### 42.3 検証

- 反力面積.xlsxと同条件（管底水準高156.555、基礎コンクリート天端155.55、外径1.6、内径1.35）で`hc=1.680`となり、Excelの「管中心から基礎コンクリートまで」（B19=1.680）と完全一致することを確認。
- 管頂水準高 = 基礎天端 + hc + 外径/2 = 155.55+1.680+0.8 = **158.030**（ユーザー指摘の値と完全一致）。
- 補正支圧壁天端水準高158.050 > 管頂158.030となり、管が支圧壁の中に正しく収まる（マージン20mm）ことを確認。断面図キャンバスの管の描画もこの`hc`を使っているため、修正後は管が支圧壁天端をはみ出さずに描かれるはずである。
- console errorなし。`service-worker.js`の`CACHE_NAME`を`v27`→`v28`に更新済み。

**注記:** 41章と同様、この検証もスクリーンショットでの実際の描画確認ではなく、数値計算の突き合わせのみで行った。次回、実際の画面で断面図の管が支圧壁の中に正しく収まって見えるか確認すること。

---

## 43. ③受働土圧安定照査のNG診断（館町雨水の実例）・α係数の意味・FAQ

コードの変更は無いが、ユーザーからの実運用中の質問と、それに対する調査結果は次回への申し送りとして重要なため記録する。

### 43.1 「③がNGになった。バグでは？」という質問への回答

館町雨水（φ1350、円形立坑φ5.0m、BR-ST自動選定、Hw=2.50m）を入力したところ③がNGになり、ユーザーから「バグか」との確認依頼があった。参考資料`BR-SST４台1350計算書.pdf`の1ページ目（八王子市幹線1級36号線、別現場）にある③の計算式

```
R = α・B・(γ・H²・Kp/2 + 2・C・H・√Kp + γ・h・H・Kp)
```

と、37章で実装済みのアプリの式を項ごとに突き合わせた結果、**式の構造は完全に一致**しており、バグではないと判明した。NGの原因は、館町雨水の入力条件（φ=5°・C=3kN/m²・土被り4.49m）が、参考にした八王子市の実例（φ=20°・C=5kN/m²・土被り7.75m）よりも大幅に弱い・浅いことにあった。特にφが③の全項に掛かるKp（受働土圧係数）を通じて効いてくるため、φ=5°と20°の差（Kp:1.191 vs 2.039）と土被りの差（4.49m vs 7.75m、上載荷重項が支配的なため効きが大きい）の２つがNGの主因。

**教訓:** ③がNGになったとき、まず疑うべきは実装ではなく**入力した土質パラメータ（特にφと土被りH最大）が現場の地盤調査報告書と一致しているか**。φ=5°のような極端に小さい値は軟弱地盤を意味し、実データなのか仮入力の値なのかをユーザーに必ず確認すること。もし式自体を疑う場合は、②の参考資料（`BR-SST４台1350計算書.pdf`1ページ目、`反力面積.xlsx`、`バックロックの選定.xlsx`）と項ごとに直接照合するのが最速。

### 43.2 「受働土圧 割増し係数 α（③照査用）とは？」への回答

α（既定2.5、範囲1.5〜2.5）は、受働土圧の理論値（`Kp`から求まる2次元・単位幅あたりの値）を、**壁の有限な幅による3次元的な土のクサビの広がり**を考慮して割り増すための経験的係数。壁の正面だけでなく、その左右にも斜めに土のクサビが広がって一緒に抵抗するため、単純な2次元計算では抵抗力を過小評価してしまう。この考え方はどの教科書にも明快な導出式があるわけではなく、実務の慣例値として1.5〜2.5の範囲で選ばれる（値が大きいほど3次元効果を多く見込む＝OKになりやすい）。参考にした2つの一次資料（`BR-SST４台1350計算書.pdf`、旧版の`反力面積.xlsx`系資料）はいずれも2.5（上限）を採用しており、アプリの初期値もそれに合わせている。

---

## 44. 測量タブに測量履歴・音声読み上げテンキー・保護⇔解除を追加（M_APP_022）

### 44.1 何を追加したか

測量タブ（`#p5`）のTP/KP入力を、ユーザーが提示した3枚のモックアップ画像に合わせて全面的に作り直した。

1. **📖 測量履歴カード（新設）**: 「新しい測量を入力」ボタン＋`<select>`。過去に保存した測定（`measurements[]`配列）を`YYYY-MM-DD / 測定No.N`形式で日付順に一覧し、選ぶと即座にその回の入力内容へ切り替わる。
2. **📝 測定数値入力カード（TP/KPカードを置き換え）**: 「本数」「測定日」に加え、「TP数」「KP数」を±ステッパーで指定すると、下に名称/測点名/距離/度/分/秒の表が自動生成される（TP行は青・KP行は桃色）。以前は「＋TPを追加」「＋KPを追加」ボタンで1行ずつカードを増やす方式だったが、本数を指定すれば一括生成される方式に変更。
3. **数値入力用テンキー（新設、`#surveyKeypad`）**: 表の距離・度・分・秒セルをタップすると画面下部にテンキーが開く。数字を押すたびに`SpeechSynthesisUtterance`（`lang='ja-JP'`）で「いち」「に」…と読み上げる。度は0〜359、分・秒は0〜59に自動制限。「元に戻す」（そのセルを開いた時点の値に一発で戻す）「BS」「↑/↓（同じ項目で前後の行へ）」「次へ/前へ（距離→度→分→秒→次の行…の順で移動）」「閉じる（確定して表示形式に整形）」を実装。
4. **保護⇔解除（`#surveyProtectBar`）**: 履歴を読み込む、または保存した直後は自動的に🔒保護状態になり、本数・測定日・TP/KP数・器械点/後視点・表のセルすべてが編集不可（テンキーも開かない）になる。「🔓解除して編集」ボタンで解除でき、もう一度押すと保護に戻る。誤って読込済みの過去データを上書きしてしまう事故を防ぐのが目的（ユーザーからの明示的な要望）。

### 44.2 データモデル・保存形式

- 新しいグローバル配列`measurementHistory`（各要素は`{version:'v24-history-measurement', measureDate, pipeCount, tpCount, kpCount, instX, instY, bsX, bsY, pipeDiaMm, internalPct, tolOk, tolWarn, tpRows:[{name,dist,deg,min,sec,memo}], kpRows:[{name,role,dist,deg,min,sec,memo}], updatedAt}`）を保持し、`getProjectData()`の**トップレベル**に`measurements: measurementHistory`として出力する（`survey`の中ではない点に注意。ユーザーが既に保有している現場JSON（例:`館町下水_１~2_統合.json`、`version:"split-2026-05-18"`の旧フォーマット）が、この`measurements`配列を最上位キーとして持っていたため、それに合わせた）。
- `setProjectData()`で`d.measurements`を`measurementHistory`へ復元。現在の作業セット（`tpRows`/`kpRows`）が空で、かつ履歴があれば、**日付＋本数で最も新しいものを自動的に読み込む**（`sortedHistoryIndexes()`→`loadSurveyHistory()`）。上記の実在ファイルは`survey.tpRows/kpRows`を持たない（履歴のみ）ため、このフォールバックがないと空の表が表示されてしまう。
- 併せて、旧フォーマットの`survey`が`instX/instY/bsX/bsY`ではなく`ox/oy/bx/by`というキー名だった点（既存コードは未対応で、たまたま既定値と一致していたため症状が出ていなかった）にも`sv.instX ?? sv.ox ?? 0`のようなフォールバックを追加し、実際の器械点座標を確実に復元できるようにした。
- 保存（💾ボタン）は「本数」をキーに`measurementHistory`をupsertする。既に同じ本数のデータがあり、かつ今読み込んでいるものと別のエントリであれば`confirm()`で上書き確認する（同じエントリを編集して保存し直す場合は確認なし）。

### 44.3 既存の計算エンジン・アコーディオン機構はそのまま流用

- `buildSurveyGeometry()` / `calculateSurveyTraverse()` / `judgePoint()` / `judgeStraight()` / `judgeCurve()` / `calcSurvey()` / `calcSurveyBaseAz()`（器械点・後視点座標からの基準方位角自動計算）は**一切変更していない**。新しい表・テンキーが操作するのは既存のグローバル配列`tpRows`/`kpRows`（`{id,dist,deg,min,sec,memo}` / `{id,role,dist,deg,min,sec,memo}`）そのものなので、計算側は「入力方法が変わったこと」を意識せず動く。
- 📍器械点・後視点カードの4つの入力欄（`surveyInstX/Y`, `surveyBsX/Y`）は、既存の`onSurveyRowChange()`をそのまま呼ぶ（中身は`calcSurveyBaseAz(); calcSurvey(); autoSave();`に簡略化。旧実装にあった「TP/KPの各`<input>`をDOMから読み直す」ループは、行がテンキー方式に置き換わって存在しなくなったDOM要素を参照していたため削除した）。
- 新設した2枚のカード（📖測量履歴・📝測定数値入力）は、9〜14章で確立済みの`class="card accordion"`＋`<div class="card-title" onclick="toggleAccordion(this)">`の規約にそのまま従っている。**独自のアコーディオン機構は作っていない**。`toggleAccordion()`は「同じ`.page`内の他の開いているカードを閉じてから、クリックされたカードを開閉する」という排他仕様を既に持っているため、ユーザー要望の「一つ開くと前のやつが閉じる」はこの既存関数を使うだけで満たされる。既存の測量タブの他5枚のカード（測量基準設定・器械点後視点・ひと目で分かる判定・計算結果一覧・計算式途中経過）は元々アコーディオン化済みだったので変更していない。
- テンキーの音声読み上げ以外はネイティブの`<input>`を使わず、`<td onclick="openSurveyKeypad(...)">`のクリックだけで完結させている（スマホのネイティブ数値キーボードの挙動が機種・ブラウザで不安定というユーザー指摘への対応）。

### 44.4 検証方法・結果

Browserペインからは`file://`直読みがタイムアウトする環境だったため、作業フォルダを一時的に`python -m http.server`でローカル配信して検証した（検証用の一時ポートのみ、`.claude/launch.json`の`m_app_022_static`は変更していない）。

- `showPage(5)`後、`toggleAccordion()`が測量タブの7枚のカードで排他的に開閉することを確認（1枚目開→2枚目クリックで1枚目が自動的に閉じる）。
- `openSurveyKeypad('tp',0,'dist')`→`surveyKeypadPress()`を複数回呼び、セル表示・テンキー表示（`#surveyKeypadDistance`等）の即時更新、`.`の重複禁止、度は359超・分秒は59超で弾かれること、`surveyKeypadUndo()`でテンキーを開いた時点の値に戻ること、`surveyKeypadNav()`/`surveyKeypadVert()`でのセル間移動、`closeSurveyKeypad()`での確定整形（`fmt(x,3)`・2桁ゼロ埋め）を確認。
- 本数・測定日・TP数2・KP数2で仮データを入力→保存→`measurementHistory.length===1`・自動的に保護状態になること→解除→再保護→「新しい測量を入力」で直前の保存分のTP/KP数を引き継いだ新規状態に戻ること→履歴から再読込して値が完全復元されることを確認。
- **実際にユーザーが保有する現場JSON**（`館町下水_１~2_統合.json`、`version:"split-2026-05-18"`、`measurements`7件、`survey`はTP/KP行を持たず`ox/oy/bx/by`のみ）を`setProjectData()`に直接読み込ませ、7件すべてが日付順で履歴セレクトに並ぶこと、最新の測定No.37（2026-06-02）が自動的に開かれ保護状態になること、器械点/後視点座標（`ox/oy/bx/by`→`0.000/0.000/10.000/0.000`）と基準方位角（自動計算で`0.000°`）が正しく復元されることを確認。
- `read_console_messages`でJSエラーなしを確認（一連の操作すべてを通して）。
- `service-worker.js`の`CACHE_NAME`を`v28`→`v29`に更新済み。

**未確認事項（次回への申し送り）:** 41・42章と同じ環境制約により、実機・実ブラウザでのスクリーンショット目視確認と、スマホ実機での音声読み上げ（`speechSynthesis`）・タップ操作感の確認ができていない。次回、実際のスマホ/タブレットでテンキーの操作感・読み上げ音声・保護バーの見た目を確認すること。

---

## 45. 41・42・44章の未確認事項をBrowserペインの実画面で目視確認（M_APP_022）

前回までの複数セッションでは環境上の制約（Browserペインが非表示でスクリーンショットのコンポジットがタイムアウトする）により、41・42・44章の変更は数値の突き合わせのみで検証し、**実際の画面での見た目は未確認のまま**だった。本セッションではBrowserペインのスクリーンショットが正常に取得できたため、`preview_start({name:'m_app_022_static'})`でローカル配信し、実画面で確認した。

### 45.1 測量タブ（44章）の確認結果

- 📖測量履歴・📝測定数値入力の各カードが仕様通りに描画されることを確認（本数/測定日/TP数・KP数ステッパー→TP行=青・KP行=桃色の表が自動生成）。
- 距離セルをタップすると画面下部にテンキー（`#surveyKeypad`）が正しく開き、選択中のセル（TP1の距離）がハイライトされることを確認。
- 数字ボタンのタップで表示値が正しく更新されること（`1`→`0`→`.`の順にタップし`10.000m`と表示されるまで確認）を確認。想定通りの桁の積み上げ方（末尾に追加していく方式）で動作している。
- `read_console_messages`でJSエラーなし。

### 45.2 支圧壁 寸法図（41・42章）の確認結果

- 平面図キャンバスには図形のみが描かれ、説明文（立坑寸法／推進方向／Bw）はキャンバス下のHTMLキャプション欄に独立して表示されており、**41章で懸念されていた文字の重なりは実際に発生していない**ことを画面で確認。
- 断面図キャンバスに、管の外径円（実線）に加えて**内径円（グレーの破線、同心）**が正しく描画されていることを画面で確認（42章で追加した内径描画）。
- 42章のバグ修正（管中心高さ = 管底水準高 + 内径/2）の再現テストとして、水準高モードで管底水準高156.555・基礎コンクリート天端155.55・支圧壁天端157.97を入力したところ、断面図上に**「推進管芯 hc=1.68m」**と表示され、42.3節で検算した理論値（hc=1.680）と一致することを画面上でも確認した。あわせて、支圧壁高さの0.5mピッチ丸め（39章）の注記「設計高さ2.42m→プレキャストは0.5m単位のため施工高さHw=2.50mに切り上げ」もキャプション欄に重なりなく表示されることを確認。断面図上で管（内径円まで含め）が支圧壁の枠内に収まって描かれており、修正前バグの症状（管頂が支圧壁天端をはみ出す）が解消されていることを目視でも確認できた。
- `read_console_messages`でJSエラーなし。

### 45.3 まとめ・今回のセッションでの変更点

コードの変更は無し（本仕様書のみ更新）。41・42・44章に残っていた「実機・実ブラウザでの目視確認ができていない」という申し送り事項を解消した。唯一残る未確認事項は、**スマホ実機での音声読み上げ・タップ操作感**（Browserペインはデスクトップ相当のエミュレーションのため、実機の挙動とは厳密には異なりうる）。

---

## 46. 支圧壁4段階照査に「計算結果 vs 判定条件」比較表示・数式の改行整理・NG時の形状変更提案を追加（M_APP_023）

ユーザーから、支圧壁4段階照査カード（`calcBacklock()`が描画する①〜④の照査結果）について3点の改善要望があった。①改行位置が読みにくい、②「判定条件」欄が計算結果と離れて書かれていて比較しづらい（特に③は推進力計算結果Fとの比較であることが分かりにくい）、③NGになったときに「では何mmくらい寸法を変えればOKになるのか」が分からない。

### 46.1 数式の改行整理

①②はもともと改行が一切なく1行の長文だった（`f = F/(Bw×Hw) = ... kN/m²（=... N/mm²）`のように、途中式・結果・単位換算がすべて連結）。③④はある程度`<br>`で分けていたが、ステップの区切りが視覚的に弱かった。

すべての式を「途中の計算ステップ」→空行（`<br><br>`）→「次のステップ／最終結果」という単位でグルーピングし直した。例えば①は
```
Beff×Hw = 2.00×2.50 = 5.00 m²

f = F/(Beff×Hw) = 2453/5.00 = 490.6 kN/m²（=0.491 N/mm²）
```
のように、面積計算とf算出を別の塊に分けた。②はe・A/Z算出のブロックとM・σmax算出のブロックを分離、③は「Kp・土被り・q」のブロックと「(a)(b)(c)自重・上載荷重・粘着力項」のブロックを分離、④は「q算出」「反力・モーメント算出」「断面諸元」「許容応力度・抵抗モーメント算出」の4ブロックに分離した。

### 46.2 「計算結果 vs 判定条件」比較表示（`compareBarHTML()`）

各照査の末尾にあった `<b>Ep=...</b>　vs　F=...` のような埋め込み比較や、別行の `判定条件: ≤ f_a=...` という素っ気ない表示をやめ、`compareBarHTML(calcVal, calcLabel, limitVal, limitLabel, cmp, unit, ok)` という共通ヘルパーに統一した（[M_APP_005.html](M_APP_005.html)の`calcBacklock()`直前に新設）。各照査ブロックの下に

```
計算結果（Ep（受働土圧抵抗力）） 1192.7 kN
［====バー（OK=緑/NG=赤、判定値に対する割合で塗り分け）====］
判定条件（F（推進力計算結果）） ≥ 2453.0 kN
```

という形で、数値・ラベル・バーを1箇所にまとめて表示する。③の判定値ラベルは明示的に「F（推進力計算結果）」とし、④は「生じるモーメント最大値」とするなど、**何と比較しているのかが一目で分かる**ようラベルを具体化した（旧表示は「判定条件: Ep ≥ F」のように数式記号のみで、Fが何を指すか初見では分かりにくかった）。

### 46.3 NG時の形状変更提案（`computeWallShapeProposal()` / `wallShapeProposalHTML()`）

①②③のいずれかがNGのとき、支圧壁の**高さHw・有効幅Beff**をどれだけ拡大すれば①②③すべてOKになるかを総当たりで探索し、新設の「🛠 形状変更の提案」カードに表示する。

- **探索範囲:** Hwは+0.1m刻みで+3.0mまで、Beffは+0.1m刻みで+2.0mまたは立坑制約（`maxBw`、円形なら`linerD`・矩形等なら`wallPitW`）までの小さい方。
- **候補の選び方:** (ΔHw+ΔBeff)の合計ステップ数が最小になる組合せを、合計ステップ数の昇順（0,1,2,…）に総当たりして最初に見つかったものを採用する。同じ合計でもBeff側を先に試す実装だが、実際には「最小の合計変更量」を返すことが目的であり優先順位はどちらでも実用上問題ない。全探索しても最大 31×21 通り程度なので毎回の`calcBacklock()`呼び出し内で同期的に計算しても支障ない。
- **④（プレキャスト支圧壁の曲げ照査）は対象外:** ④はバックロックのカタログ製品の型番・鋼材厚に依存し、Hw・Beffのように連続的に変えられる寸法ではないため、この探索には含めない。④がNGの場合は従来通り、カード上部の既存NG注記（ジャッキ据付位置・鋼材厚の見直しを案内）に従う。
- **達成できない場合:** 探索範囲内で解が見つからなければ、「この範囲では満たせない」旨と、f_a・土質条件の見直しまたは立坑拡幅の検討を促す注記を表示する（`achievable:false`）。
- **形状ごとの解釈の翻訳:** Beffは幾何学的な有効幅であり、実際にユーザーが変更する入力項目は立坑形状によって異なる。
  - 円形立坑・現場打ち（`!backrockUse`）: Beffは支圧壁厚さ（`wallThickness`）から幾何学的に逆算される（35章）。提案されたBeffを`t' = R_eff − √(R_eff²−(Beff/2)²)`で厚さに逆変換し、「支圧壁厚さをt'まで増やしてください」と案内する。
  - バックロック使用時（`backrockUse`）: 幅はカタログ型番で決まるため、具体的な数値ではなく「この有効幅を満たす、より大きい型番を選定してください」と案内する。
  - 矩形・オープンカット等（非円形）: Beff=Bwなので、そのまま「支圧壁幅Bwを○○m以上に」と案内する（立坑内法幅の制約に収まっているかも併記）。
- **簡易模式図（`wallShapeCompareSVG()`）:** 現在の形状（実線・灰色）と提案形状（破線・緑塗り）を重ねたBeff×Hwの矩形を1つのSVGで表示する。**あくまで寸法の比較を伝えるための簡略図であり、実際の立坑断面形状（円弧・弓形等）の正確な図面ではない**ことをキャプションで明記している。

### 46.4 検証

`preview_start({name:'m_app_023_static'})`でM_APP_023を配信し、以下を確認（すべて`read_console_messages`でエラーなしも確認済み）。

- **③のみNGのケース**（43章の館町雨水の実例条件: γ=13, φ=5°, C=3, 土被り=5.31, Hw=2.42, Beff=2.00, F=2453）: ①②④はOK、③はNG（Ep=1192.7 kN < F=2453 kN）と表示され、③の比較行が「判定条件（F（推進力計算結果））≥ 2453.0 kN」と明示されることを確認。「🛠 形状変更の提案」カードが自動的に開いた状態で表示され、Hw 2.50→2.60m・Beff 2.00→4.00mの提案（バックロック使用中だったため「型番を選定してください」という案内）が出ることを確認。
- **Hwを6.0mまで増やしたケース**（③はまだNG）: 提案が「Hwは変更なし・Beffのみ+0.40m」に切り替わることを確認 — Hwを増やすと③の上載荷重項（土被りが減る）が縮み、自重項の増分と相殺されて頭打ちになるため、単純に「Hwを増やせば増やすほど良い」わけではないことが探索結果からも裏付けられた（土被りが浅い区間ではHw一辺倒の増強では解決しないケースがあることが分かる、という点で実務的にも妥当な挙動）。
- **円形立坑・現場打ち（`backrockUse=false`）のケース**: 提案が「支圧壁厚さt=1.006mに相当します（現在t=0.500m）」という厚さベースの案内に切り替わることを確認。
- **矩形立坑（`wallShaftType='rect'`）でBeffが立坑内法幅の制約に近いケース**（Bw=2.98m、制約maxBw=3.00m）: Beff側の拡大余地がほぼ無いため、提案が「Beffは変更なし・Hwを+1.50m」に自動的に切り替わり、maxBwの制約を正しく尊重していることを確認。
- 3パターンとも`allOk`になるまでHwを十分大きくすると「🛠 形状変更の提案」カード自体が非表示になることを確認（`shapeProposal`が`null`になる分岐）。
- `service-worker.js`の`CACHE_NAME`を`v30`→`v31`に更新済み。

**未確認事項（次回への申し送り）:** 41・42・44章と同じ環境制約が今回も一部再現し、`computer{action:"screenshot"}`によるピクセル単位の目視確認ができない場面があった（`get_page_text`によるテキスト内容の検証と、比較バー・SVGのHTML構造確認は実施済み）。次回、実際の画面でバーの色・幅や模式図の見た目を確認すること。

**46章の教訓:** Dropbox同期フォルダ（`07_曲線プログラム`配下）内の大きいファイル（M_APP_005.html、約280〜290KB）を編集した直後、`Edit`ツールでは成功と表示されても、実際にOSレベルで配信・実行される内容（`curl`や実行中のpythonサーバー、PowerShellでの直接読み込み）には反映されていないことがあった（ファイル属性は`ReparsePoint`＝Dropbox Smart Syncのプレースホルダー）。**大きいファイルを編集した直後は、`Edit`ツールの成功表示を鵜呑みにせず、PowerShellの`[System.IO.File]::ReadAllText()`等で実ファイルの内容を直接読み直して変更が反映されているか確認すること。** 反映されていない場合は、PowerShellで直接文字列置換・書き込みを行うと確実に反映できる。

---

## 47. ライナープレート有効内径の説明文修正・支圧壁「推進作業スペース」照査（⑤）の追加（M_APP_024）

ユーザーから第一産商のライナープレートカタログPDF（`ライナープレート.pdf`）を提示され、2点の指摘があった。①立坑タブの「ライナープレート有効内径」の考え方が間違っている、②支圧壁タブの形状変更提案（46章）が構造照査だけを満たすようBeff（幅）を立坑の幾何学的最大幅まで広げてしまい、実際には立坑径5m・支圧壁幅4mでは推進作業に必要な残りスペースが取れない、という2点。

### 47.1 ライナープレート有効内径の説明文修正（数値は変更なし）

カタログを確認すると、ライナープレートの呼び径はボルト孔芯の径であり、波形（コルゲート）加工で強度を持たせる構造のため、有効内径は波形の内側同士の対面距離で決まる。ボルト孔芯から波形内側までの離れは25mm×2辺＝50mmで、これは板厚（カタログでは2.7〜7.0mmの数種類）とは無関係の固定値。

コード側の計算式（`computeWallBeff()`の`linerD = pitD - 0.05`）はもともとこの通り正しく実装されていたが、コメント・画面キャプションの表記が「板厚25mm×2辺」となっており、実際の板厚（2.7〜7.0mm）と紛らわしい・概念として誤った説明になっていた。ユーザーに確認の上、**数値は変更せず**、表記のみ「ボルト孔芯から波形内側まで25mm×2辺＝50mm（板厚2.7〜7.0mm自体とは無関係）」という正しい概念に修正した（[M_APP_005.html](M_APP_005.html)内、`drawWallPlanView()`のライナー内側描画コメント・キャプション、`computeWallBeff()`直前のコメント）。

### 47.2 支圧壁「⑤推進作業スペース照査」の追加

> **※必要スペースの式は47.4章・47.5章を経て47.9章で最終的に置き換えられ、本章の`wallJackHeight`・47.5章の`wallStrutClearance`入力欄はいずれも47.9章で削除済み（現在は存在しません）。現状の正しい式は47.9章を参照。**

**問題の構造:** 円形立坑では支圧壁幅Beffを広げるほど、支圧壁は「ライナープレート内側の円を弦で切り取った弓形」になるため厚さ（サジッタ）が増える（`t = R_eff − √(R_eff²−(Beff/2)²)`）。46章の形状変更提案は①②③の構造照査だけを満たすようBeff・Hwを拡大する探索だったため、立坑の幾何学的な最大幅（`maxBw`）までBeffを広げる提案を返すことがあった。しかしBeffが広がるほどサジッタが増え、支圧壁背面〜反対側ライナー内面までの残りスペース（`hCenter = R_eff − サジッタ`）が圧迫される。ユーザーの実例（立坑径5m・支圧壁幅4m）では、この残りスペースが推進管の挿抜やジャッキ設備に必要な奥行きを下回り、実際には施工できない提案になっていた。

**必要スペースの定義（ユーザーヒアリング）:** 「ジャッキ機高、または推進管1本を挿入・撤去する際に必要な奥行き（推進管 有効長l′×2.5）のどちらか大きい方」。ユーザーの実案件では管長1.2m×2.5＝3000mmが必要、との回答を得た。これを`requiredWorkDepth() = max(ジャッキ機高[mm]/1000, pipeLen×2.5)`として実装した。ジャッキ機高は新設の入力欄`wallJackHeight`（既定値900mm、支圧壁の諸元カード）。

**実装:**
- `computeWallBeff()`が返す`hCenter`（既存、38章で導入）を、新設の⑤照査で「残りスペース」として利用。
- `calcBacklock()`内、円形立坑（`isCircleShaft`）かつ`hCenter`が算出できる場合のみ「⑤ 推進作業スペース照査」を追加し、`hCenter ≥ requiredWorkDepth()`を判定する（矩形・小判型等ではサジッタの概念自体が無いため対象外）。NGの場合は専用のng-boxで「支圧壁幅Bwを縮小するか、立坑径を大きくしてください」と案内する。
- **46章の形状変更提案が「作業スペースを圧迫する側」に拡大提案してしまう問題への対処:** 円形立坑・現場打ち（`!backrockUse`、Beffがサジッタから閉形式で決まる場合）に限り、`computeWallShapeProposal()`に渡す`maxBw`を、幾何学的制約（`maxBw`）と「必要スペースを侵食しない上限」（`clearanceMaxBeff = 2×√(R_eff²−requiredDepth²)`、requiredDepthがR_effを超える場合は0）の小さい方に差し替えた（`maxBwForProposal`）。これにより、①②③のNG解消のためにBeffを拡大提案する際も、⑤を新たに壊す提案にはならない。バックロック使用時（幅がカタログ型番で決まり、サジッタとBeffの関係が単純な閉形式ではない）は自動搾込みの対象外とし、⑤のNG表示のみで案内する。

### 47.3 検証

`preview_start({name:'m_app_024_static'})`でM_APP_024を配信し、`javascript_tool`で`computeWallBeff()`・`calcBacklock()`を直接呼び出して確認（`computer{action:"screenshot"}`は本セッションでも利用不可だったため、`read_console_messages`でのエラー無し確認とHTML構造の直接検証で代替）。

- **ユーザー実例の再現:** 円形立坑D=5m・現場打ち・支圧壁厚さ1.017m（Beff≈4.00m相当）・推進管l′=1.2mの条件で、`hCenter=1.458m`・`requiredWorkDepth()=3.000m`（ジャッキ機高900mmとl′×2.5=3000mmの大きい方）となり、⑤が正しくNG（「支圧壁幅Bw（現在4.00m）が広すぎて…3.00mを下回っています」）と表示されることを確認。
- **①②③をNGにして提案が⑤を尊重するか:** F=50000kNまで引き上げて①②③をNGにした状態で、D=5mでは`clearanceMaxBeff=0`（requiredDepth=3.0mがR_eff=2.475mを超えるため幾何学的に達成不可能）となり、提案が「立坑制約により有効幅は最大0.00mまで」と正しく報告して`achievable:false`になることを確認。D=8mに広げると`clearanceMaxBeff≈5.22m`（手計算と一致）となり、提案の上限表示も「最大5.22mまで」に変わり、幾何学的最大幅7.95mより小さい値で正しく頭打ちになることを確認。
- **矩形立坑では⑤が出ないこと:** `wallShaftType='rect'`に切り替えると`hCenter`が`null`になり、⑤照査自体が生成されず「支圧壁3段階」の従来通りの表示に戻ることを確認。
- **ライナープレート説明文:** `drawWallPlanView()`のキャプション・コメントが新しい文言（「ボルト孔芯から波形内側まで25mm×2辺」）に置き換わっており、`linerD`の数値自体（`pitD - 0.05`）は変更していないことを確認。
- `wallJackHeight`をプロジェクトJSON保存/復元（`getProjectData()`/読込処理）に追加し、未保存の旧プロジェクトを読み込んだ場合は既定値900mmにフォールバックすることを確認。
- `service-worker.js`の`CACHE_NAME`を`v31`→`v32`に更新済み。

**未確認事項（次回への申し送り）:** 本セッションでも`computer{action:"screenshot"}`によるピクセル単位の実画面確認ができなかった（45章・46章と同じ環境制約）。次回、実際の画面で⑤照査カード・NG時のng-box・「ジャッキ機高」入力欄の見た目を目視確認すること。また、バックロック使用時（プレキャスト型番選定）に⑤がNGになるケースでの実際の運用（型番を替える／立坑径を変える等の現場判断）については、まだ実例で裏付けていない。

**47章の教訓:** ユーザーが「提案してくれたけど実際には使えない」と指摘した場合、既存の自動探索ロジック（46章の`computeWallShapeProposal`）が最適化している評価関数（今回は①②③の構造照査のみ）に、ユーザーが実際に気にしている制約（今回は⑤の作業スペース）が含まれていないことを疑うこと。単に新しい照査を追加するだけでなく、既存の「自動拡大提案」がその新しい制約を無視して逆方向に悪化させていないか（Beffを広げる提案が⑤を壊す）を必ず確認し、探索の上限そのものを差し替えること。

### 47.4 ⑤照査の比較基準・必要スペース式の修正（同セッション内、ユーザーの実測図提示による訂正）

47.2で実装した直後、ユーザーから実際の平面図（CAD、5000mm立坑・1350ヒュームー管の例）を提示され、2点の誤りが判明した。

1. **比較基準が「支圧壁前面〜立坑中心（hCenterのみ）」になっていた誤り:** `hCenter = R_eff − サジッタ`は支圧壁前面から**立坑の幾何中心**までの距離であり、発進側（推進管が立坑を出ていく側）のライナー内面までの距離ではない。実際に確保すべき「残りスペース」は、支圧壁前面からそこを通り抜けて発進側ライナー内面に達するまでの**全長**＝`hCenter + R_eff`である。47.2版はこれを半分弱に過小評価していた（＝実際より厳しすぎる/緩すぎるどちらの方向にもズレうる誤り）。ユーザーが提示した実測図の「1301mm」という寸法（「推進管カラー端部から支圧壁前面までの距離」と明言）を検証材料に、比較先を`hCenter`単独から`hCenter + R_eff`（`availDepth`）に修正した。
2. **必要スペースの式が`max()`だった誤り:** 47.2では「ジャッキ機高、またはl′×2.5のどちらか大きい方」という初回ヒアリングの回答をそのまま`Math.max(jackH, pipeLen*2.5)`と実装したが、後日ユーザーから「管有効長l′×2.5＋ジャッキ長(通常900mm)は最低必要となる。管を1本押し切り、次の管を据え付けるにはカラーの呑み込み代も考慮しておかなければならない」という明確な訂正があり、**加算式**（`pipeLen×2.5 + ジャッキ長 + カラー呑み込み代`）に修正した。「カラー呑み込み代」（次の管を接続する際にカラー同士が重なり込む分）用に新しい入力欄`wallCollarAllowance`[mm]（既定値0、要現場確認のプレースホルダー）を追加した。

**修正後の実装:**
- `requiredWorkDepth() = pipeLen×2.5 + wallJackHeight/1000 + wallCollarAllowance/1000`（[M_APP_005.html](M_APP_005.html)、`computeWallBeff()`直前）。
- ⑤照査の比較対象を`availDepth = hCenter + R_eff`に変更（`calcBacklock()`内）。ラベルも「残りスペース（支圧壁前面〜発進側ライナー内面）」に修正。
- `computeWallShapeProposal()`に渡す`maxBwForProposal`の导出も、`availDepth ≥ requiredDepth`から逆算した`サジッタ ≤ 2R_eff − requiredDepth`（ただしサジッタは最大R_effを超えない）という制約に修正（旧式`R_eff²−requiredDepth²`は誤った比較基準に基づくものだったため、47.2の式をそのまま使い続けると机上でも実際より過度に厳しい／緩い上限を返す）。
- 支圧壁の諸元カードの入力欄ラベルも「ジャッキ機高」→「ジャッキ長」に統一し、カラー呑み込み代の説明note-boxを追加。

**再検証:** `preview_start`でM_APP_024を配信し、ユーザー提示の実測図に近い条件（円形立坑D=5m・1350ヒューム管（外径1.6m、l′=2.43m、`onPipeDiaChange()`で自動設定）・支圧壁厚さ0.434m→Beff≈2.80m）で`javascript_tool`から`computeWallBeff()`・`calcBacklock()`を確認。`hCenter=2.041m`・`R_eff=2.475m`→`availDepth=4.516m`、`requiredWorkDepth()=2.43×2.5+0.900+0=6.975m`となり、⑤が正しくNG（「6.98mを下回っています」）と表示されることを確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v32`→`v33`に更新済み。

**未確認事項:** 「1301mm」という実測値そのものを本アプリの計算値と直接突き合わせる検証はまだ行っていない（ユーザーの実プロジェクトの`wallJackHeight`・`wallCollarAllowance`・実際のBeff等の入力値が本セッションでは分からないため）。次回、ユーザーの実プロジェクトJSONまたは実際の入力値を教えてもらった上で、`availDepth`または「推進管カラー端部〜支圧壁前面」の計算値がユーザーのCAD実測値と一致するかを直接検算すること。カラー呑み込み代`wallCollarAllowance`の標準値（採用する継手・カラーのカタログ値）もまだ未確認。

**47.4の教訓:** ユーザーが実測図・CADの寸法を「答え合わせ」として提示してきた場合、その数値が指す2点間の意味を最初から完全に言い当てられるとは限らない（本セッションでも「front-back方向の深さ」という大枠は合っていたが、比較基準の起点をhCenter単独に取り違えた）。ユーザーから明確な訂正（今回は「管有効長l′から支圧壁前面までの距離」という直接的な定義）が得られたら、それを最優先の一次情報として即座に式を差し替えること。また、複数回のヒアリングで得た式（`max()`→`+`）が矛盾する場合は、**より新しく・より具体的な説明を優先**し、古い版が他のロジック（今回は`computeWallShapeProposal`への上限渡し）に埋め込まれていないか横断的に確認すること。

### 47.5 ズリバケット搬出用ストラット余裕の追加

47.4の直後、ユーザーから別の実測図（「4φ400=1600」の注記付き）が示され、「バケットを管の中から搬出するためにはストラット4個分1600程度の余裕が必要となる」との追加説明を受けた。これはジャッキ長（900mm）とは別に必要な項目（ジャッキとは別にストラットが並ぶ分の空間で、バケットを管内から抜き出す際にその分の余裕がないと搬出できない）と判断し、`requiredWorkDepth()`にもう1項追加した。

- 新設入力欄`wallStrutClearance`[mm]（既定値1600mm＝ストラット4個×φ400mm相当の目安、支圧壁の諸元カード）。
- `requiredWorkDepth() = pipeLen×2.5 + jackH + collarAllowance + strutClearance`に修正。
- ⑤照査の数式表示にもストラット余裕の項を追加。

**再検証:** 同条件（D=5m・1350ヒューム管・Beff≈2.80m）で`requiredWorkDepth()`が6.975m→8.575m（ストラット1.6m分増加）に変わることを確認。①②③をNGにしたケースでの形状変更提案も、`clearanceMaxBeff`が要求スペースの増加に応じてさらに厳しく（0mまで）制限されることを確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v33`→`v34`に更新済み。

**未確認事項:** 「1301mm」「4φ400=1600」という実測値そのものを本アプリの計算値と直接突き合わせる検算はまだ行っていない（ユーザーの実プロジェクトの各入力値が本セッションでは分からないため）。次回、ユーザーの実プロジェクトの入力値を教えてもらった上で直接検算すること。

### 47.6 バックロック使用時に「形状変更の提案」が⑤の制約を無視していたバグの修正

47.2で`maxBwForProposal`（⑤の必要スペースを侵食しない上限）を導入した際、`isCircleShaft && !backrockUse`の条件を付けており、**バックロック使用時（プレキャスト、円形立坑のデフォルト構築方法）は対象外**にしていた。理由は「バックロックの実際のサジッタはカタログ型番の据付位置で決まり、単純な円弓形の閉形式では決まらないため」としていたが、これはユーザーが実際に使う既定の構築方法（`backrockUse`の初期値は`true`）を素通りしてしまうバグだった。ユーザーがM_APP_024のindex.htmlで実際に確認したところ、①②③がNGの場合の「🛠 形状変更の提案」カードが、⑤（推進作業スペース照査）を無視して従来通り立坑いっぱいまでBeffを拡大提案していた（Beff 2.80m→4.00mの提案例）。

**修正:** `maxBwForProposal`の算出条件から`!backrockUse`を外し、円形立坑であれば常に⑤の必要スペースに基づく上限（`clearanceMaxBeff`）を適用するようにした。理論的な円弓形の最小サジッタ（`t = R_eff − √(R_eff²−(Beff/2)²)`）は、実物のバックロック（プレキャストブロック）が同じBeffで実現するサジッタの**下限値**にあたる（カタログ製品は理想的な円弓形より薄くはできない）ため、この式で求めた上限を安全側（Beffを広げすぎない側）のキャップとして使うのは妥当と判断した。

**検証:** D=5m・1350ヒューム管・バックロック使用（既定）・F=50000kNで①②③をNGにした状態で、修正前は（このバグにより）「有効幅は最大○○mまで」という制約メッセージ自体が出ない状態だったが、修正後は他のバックロックなしのケースと同様に「立坑制約により有効幅は最大0.00mまで」（reqDepth=8.575mがこの立坑では幾何学的に達成不可能なため）と正しく表示されることを確認。より緩い条件（D=10m、`computeWallShapeProposal`を直接呼び出し）でも、`clearanceMaxBeff=6.87m`という中間的な上限が正しく効き、提案が`newBeff=4.70m`（上限を下回る最小十分な拡大）になることを確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v34`→`v35`に更新済み。

**47.6の教訓:** 新しい制約を既存ロジックに組み込む際、「対象外」とする条件分岐を入れる場合は、その条件がデフォルト設定・最も使われるパスを除外していないか必ず確認すること。今回は`backrockUse`の初期値が`true`（バックロックが標準構築方法）であるにもかかわらず`!backrockUse`だけを対象にしてしまい、実際にはほとんどのユーザーがこの安全チェックの恩恵を受けられない状態になっていた。ユーザーが実際に画面を開いて確認する、というフィードバックループがなければ発見が遅れていた可能性が高い。

### 47.7 バックロック使用時、①②③⑤の支圧壁幅Beffを「間詰めコンクリートを含めた全体の弦幅」に変更

ユーザーから館町雨水の実際の構造計算書（`支圧壁の計算.xlsx`、下水道実施設計の手引き（案）平成11年度・推進工法編に基づく「三日月形支圧壁」の計算）を提示され、実測・計算値である**支圧壁幅B=3.3437m・厚さT=0.651m**が、37〜38章で導入したバックロック用の代表幅（BR-STのW1=2.8m）や角当たり計算（`computeBackrockPlacement`、W2=1.6mベースでサジッタ0.733m）のどちらとも一致しないことが判明した。ユーザーからは「バックロック計算でバックロック自体の幅だけで計算する必要はなく、間詰コンクリートまで含めての計算でよい」と明確な方針が示された。

**検証:** B=3.3437m・T=0.651mは、円形立坑の弦・サジッタの標準式`t=R_eff−√(R_eff²−(B/2)²)`（35章、現場打ちで使っているのと同じ式）にR_eff=2.475mを代入するとほぼ完全に一致する（t=0.650m）ことを確認した。つまり実際の計算書は、バックロック製品自体の型番寸法（W1/W2/T）から据付位置を逆算するのではなく、**支圧壁の厚さTを（間詰めコンクリートを含めた）設計変数として自由に選び、その厚さになる弦幅Bを幾何学的に算出する**という、現場打ちの円形立坑（35章）と全く同じアプローチを取っていた。

**修正:** `computeWallBeff()`の円形立坑分岐を、`!backrockUse`（現場打ち）と`backrockUse && type==='taper'`（バックロック・テーパー形）とで統合し、どちらも`wallThickness`（バックロック本体＋周囲の間詰めコンクリートを含めた全体厚さ）から`Beff=2√(t(2R_eff−t))`で弦幅を算出するようにした（角当たり計算`computeBackrockPlacement`は、間詰めコンクリート数量計算`renderBackrockGapConcrete`用には引き続き使うが、①②③⑤の構造計算用Beffの算出には使わなくなった）。`applyBackrockSelection()`側も、円形立坑・テーパー形のときは`wallBw`を表示専用（弦幅の自動計算結果）、`wallThickness`を編集可能にし、型番を切り替えたときだけカタログの厚さを初期値として反映する（型番変更以外の再計算では上書きしない）ようにした。

**④（プレキャスト支圧壁自体の曲げ照査）は対象外:** ①②③⑤の構造照査は「支圧壁＋間詰めコンクリート」全体が土に対して働く反力・支圧を評価するものなので、間詰めを含めた全体幅Beffを使うのが妥当。一方④は、ジャッキの反力がバックロック本体（鋼材で補強された合成断面）に直接伝わり、それが梁として曲げを受ける照査なので、間詰めコンクリート（鋼材と一体化していない無筋または別体のコンクリート）を含めた幅を使うと抵抗モーメントを過大評価してしまう。そのため④の等分布荷重qおよび断面厚さHは、Beffではなく**バックロック本体の型番幅W1・型番厚さmodel.T**を使うよう明示的に分離した（他案件の参考検討書「バックロック検討.xls」でも、型番自体の幅を使っていたことと整合する）。

**検証（プレビュー・javascript_tool）:** D=5m・φ1350（1350ヒューム管、l′=2.43m自動設定）・BR-ST自動選定（W1=2.8, T=0.6）の状態で、`wallThickness`を0.6→0.651に手動編集すると`Beff=3.346m`（実際の計算書のB=3.3437mとほぼ一致）になることを確認。`wallHw`など無関係な入力を変更してもeditした`wallThickness=0.651`が保持される（型番変更時のみリセットされる）ことを確認。④の表示式が`Bo=2.80`（W1、Beffの3.35mとは別）になっていることを確認。型番をBR-MT→BR-STと切り替えると`wallThickness`がその都度カタログ既定値（0.6）にリセットされることを確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v35`→`v36`に更新済み。

**未確認事項（次回への申し送り）:** 支圧壁の計算.xlsxでは、①地盤反力照査に相当する受働土圧照査が明示的に「No」（R'=2068kN＜必要反力2943.6kN）と出ており、かつ主鉄筋量の計算が`#DIV/0!`（鉄筋降伏点強度σtが未入力）のまま出荷されていたこと、また土の粘着力Cが`推力計算1350.pdf`（C=3）と`支圧壁の計算.xlsx`（C=10）で食い違っていたことをユーザーに報告済み。

**47.7の教訓:** バックロック（既製品）を使う場合でも、構造計算上の「支圧壁」は製品単体ではなく、製品＋周囲の間詰めコンクリートを合わせた全体であることを見落とさないこと。既存の一次資料（BR-SST４台1350計算書.pdf）が「型番の幅をそのまま使う」方式だったからといって、それが唯一の正しい方式とは限らない——後から出てきた別の一次資料（支圧壁の計算.xlsx）で異なる、より妥当な方式（現場打ちと同じ弦幅・サジッタの幾何学）が使われていることが判明した。一次資料は複数あれば必ず互いに突き合わせ、矛盾があればより新しい・より直接的な証拠（実測寸法と一致する方）を優先すること。

### 47.8 「NG時の代替対策」は鉄筋補強計算ではなく、バックロックへの切替を案内する方針に修正

47.7末尾で「①鉄筋補強設計機能を実装するか」とユーザーに確認したところ、「基本的に、鉄筋を入れるくらいならバックロックを採用します」という回答があった。つまり実務上、現場打ち（無筋コンクリート）の支圧壁がNGになった場合の現実的な対策は、複鉄筋梁として設計し直すことではなく、**既製品のバックロックに切り替える**ことである（バックロックは表面が鋼材で覆われた合成構造のため、同じ厚さでも無筋コンクリートよりはるかに高い曲げ耐力を持つ）。

これを踏まえ、鉄筋補強計算機能は実装せず、代わりに`calcBacklock()`の総合判定カードに、**現場打ち（`!backrockUse`）かつ強度系の①②③のいずれかがNGのときだけ**「バックロックへの変更を検討してください」という案内note-boxを追加した（⑤作業スペース照査のNGはバックロックへの変更では解決しないため対象外とした。バックロックにしても厚さが増えればむしろ⑤を悪化させうる）。

**検証:** D=5m・現場打ち・wallThickness=0.3m・fa=1N/mm²・F=3000kNで①②がNGの状態では案内が表示され、同じ状態からバックロック使用に切り替えると案内が消えることを確認。一方、wallThickness=2.4m・F=100kN（①②③はOKだが⑤のみNGになる組合せ）では案内が表示されないことを確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v36`→`v37`に更新済み。

---

### 47.9 ⑤必要スペースの式を「残り管長＋Lc2×2＋管長＋押輪＋ストラット」に精緻化・Lc2を管諸元から自動参照

47.5〜47.8で組み立てた必要スペース式（`推進管l′×2.5` や `l′+ジャッキ長+カラー呑み込み代+ストラット余裕` 等）は、いずれもユーザーヒアリングの途中経過であり、最終的にユーザーから実際の目地部詳細図（埋め込みカラー・Lc/Lc1/Lc2・シール材の断面図）とともに、より正確な式が示された：

> 残り管長（前回推進終了時に坑内に残る既設管の端部長さ、例30cm）＋Lc2（既設管側カラー）＋推進管の有効長l′（今回据え付ける新管）＋Lc2（新管側カラー）＋押輪長さ（例30cm）＋ストラット（1本の長さ×本数、例40cm×3本）

**ジャッキ本体の長さはこの区間に含めない**（押輪・ストラットで尽くされており、ジャッキは支圧壁の検討とは別枠で扱う）ことも明言された。また、カラー呑み込み代Lc2は「管の種類・継手仕様で決まっている」ため自由入力ではなく管諸元（呼び径）から参照すべき、との指摘を受け、ユーザー提供の`推進管詳細.xlsx`「目地詳細」シートから実際の寸法表（Lc・Lc1・Lc2・Dc・D1・D2等、φ800〜3000mm）を読み取り、`PIPE_LC2`（φ800〜2200mm=130mm、φ2400mm以上=150mm）としてハードコードし、`collarLc2()`で呼び径から自動参照するようにした。

**実装:**
- `requiredWorkDepth()`を`stub + Lc2 + pipeLen + Lc2 + pressRing + strutUnitLen×strutCount`に全面差し替え（ジャッキ長`wallJackHeight`は削除）。
- 新設入力欄: `wallRemainingStub`（残り管長、既定300mm、現場ごとに編集）、`wallPressRingLen`（押輪長さ、既定300mm）、`wallStrutUnitLen`（ストラット1本長さ、既定400mm）、`wallStrutCount`（ストラット本数、既定4本）。
- `wallCollarAllowance`（カラー呑み込み代Lc2表示欄）は自由入力から読み取り専用の自動表示に変更し、`calcBacklock()`内で`collarLc2()`の値を毎回同期する。プロジェクトJSONの保存/復元からも除外した（呼び径から毎回再計算されるため）。
- 旧`wallJackHeight`・`wallStrutClearance`フィールドとその保存/復元コードは削除。

**検証:** D=5m・φ1350（Lc2自動参照=130mm）・BR-ST・厚さ0.651mの条件で、既定値（残り300mm・押輪300mm・ストラット400mm×4本）のとき`requiredWorkDepth()=0.300+0.130+2.43+0.130+0.300+1.600=4.890m`となることを確認。ストラット本数を4→3、押輪長さを300→250mmに変更すると`4.44m`に正しく再計算されることを確認。`wallCollarAllowance`表示欄が`readOnly`で自動的に130mmを表示することを確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v37`→`v38`に更新済み。

**47.9の教訓:** ユーザーヒアリングで得られる式は、実測図面という一次資料が提示されるたびに精緻化されていくことがある（47.2の`max()`→47.5の`+ジャッキ長`→47.9の`残り管長+Lc2×2+押輪+ストラット、ジャッキ除外`）。式が変わるたびに、その式を参照している箇所（UI表示・formula文字列・保存/復元・②形状変更提案の上限計算）をすべて洗い出して一括更新すること。また、「○○は管の諸元から決まっている値なので自由入力にすべきでない」という指摘があった場合、既存の自由入力フィールドをただ読み取り専用にするだけでなく、実際の一次資料（今回は`推進管詳細.xlsx`の目地詳細シート）から参照テーブルを作り、既存のPIPE_DB等のパターンに倣って自動参照するようにすること。

### 47.10 発進立坑とバックロック立坑内径の連動・47.7のバックロック厚さ下限漏れバグの修正

ユーザーから2件の指摘があった。

1. 「立坑タブで指定した立坑径と、支圧壁タブにある立坑内径は同じになるようにして」— これまで「基本情報」タブの発進立坑（`launchCircleD`）と「支圧壁」タブの立坑内径（`wallCircleD`）は同じ立坑を指すにもかかわらず完全に独立した入力欄で、別々の値を入れられてしまっていた。`onLaunchCircleDChange()`/`onWallCircleDChange()`を新設し、どちらかを編集すると即座にもう片方にも反映するようにした（双方向同期）。各欄に連動していることを示すnote-boxも追加。
2. 上記の指摘の直後、「立坑タブで入力ミスだったのか立坑内径が2.5mになっていました。このせいなのか、バックロック幅が立坑内径より広いと出てしまっています」と、`支圧壁の計算.xlsx`（47.7章）で自由入力にした`wallThickness`に、バックロック本体自体の厚さ（例BR-STならmodel.T=0.6m）を下回る値（例0.525m）を入力すると、間詰めコンクリート断面積がマイナスになる不具合が見つかった。原因は47.7で`wallThickness`を自由入力に変更した際、**バックロック本体自体の物理的な厚さを下回れない**という下限チェックを入れ忘れていたこと（バックロックは既製品なので、間詰めを含めた全体厚さがカタログの厚さより薄くなることはあり得ない）。

**修正:** `computeWallBeff()`のバックロック・テーパー形分岐に`minT = backrockCurrentModel.T`を追加し、`wallThickness`の実効値をこの下限でクランプするようにした。入力値がこの下限を下回った場合は、支圧壁の諸元カードの厚さ欄下に赤字で警告（「入力値がバックロック本体自体の厚さ（○○m）を下回っています。計算上は○○mとして扱っています」）を表示する。`renderBackrockGapConcrete()`内の古い（47.7以前の`computeBackrockPlacement`角当たり計算に基づく）formula表記もwallThicknessベースの新しい式に合わせて修正した。

**検証・残課題:** D=5m・φ1350・BR-ST・wallThickness=0.525mで、修正前は`minT`チェックが無く間詰め断面積が-0.282m²だったのに対し、修正後は`sagitta`が0.6m（=minT）にクランプされ、warning note-boxが正しく表示されることを確認。ただし**クランプ後の0.6m（カタログの最小厚さそのもの）でも、間詰め断面積はまだ-0.047m²とわずかにマイナスのまま**であることが判明した。原因はBR-STの台形形状（W1=2.8m・W2=1.6m）自体が、半径2.475mの円の深さ0.6mの弓形にぴったりとは収まらない（弓形面積1.327m²＜ブロック断面積1.374m²）という、より根本的な幾何学的な余裕不足で、47.7で間詰め数量計算の弓形サジッタを「角当たり計算（W2の角がR_effに接する位置）」から「支圧壁厚さ（wallThickness）そのもの」に統一したことの副作用（角当たり計算なら本来もっと浅い位置で収まるよう自動調整されていたが、その調整機構が失われた）と考えられる。①②③⑤の構造照査（Beff・受働土圧等）には影響しないが、間詰めコンクリート数量（材料拾い出し）の見積りとしては、このケースでは依然として不正確になりうる。次回、この残課題（間詰め数量計算だけは角当たり計算に戻す、または別の調整方法を検討する）をユーザーと相談すること。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v38`→`v39`に更新済み。

**47.10の教訓:** 入力欄を「カタログ値固定」から「自由入力」に変更する際は、その値が満たすべき物理的な制約（今回は「既製品自体の厚さを下回れない」）を必ず洗い出し、下限・上限のガードを実装すること。ユーザーが実際に画面を操作して初めて発覚するバグだった——特に、値を変更できるようにした直後は、極端な値（最小・最大・カタログ値ちょうど）を入れて壊れないか自分でも確認する習慣をつけること。

### 47.11 バックロックの支圧壁厚さ・有効幅を、角当たり計算ベースの表示専用値に戻す（47.7の再修正）

47.7〜47.10で、円形立坑・バックロック使用時の支圧壁厚さ(wallThickness)を「間詰めコンクリートを含めた全体厚さ」として自由入力にし、そこからBeffを逆算する方式にしていた。しかしユーザーから明確な訂正があった：「バックロックの計算は、製品なので寸法が決まっている。短辺(Ls)と立坑内径(R)がわかれば、短辺の面がライナー内面からいくつ離れた位置に来るか分かるはず」——つまりバックロックは既製品であり、厚さ・幅は自由入力すべき設計変数ではなく、**Ls（幅短辺=W2）とR_eff（立坑の有効半径）から幾何学的に一意に決まる**、という指摘だった。これは38章で既に実装していた角当たり計算（`computeBackrockPlacement`、yFar=Ls/2、hFar=√(R_eff²-yFar²)、hCenter=hFar-T）そのものであり、47.7でこの計算を①②③⑤の構造計算からは使わなくしていたのを、再び使うように戻した。

**Beff（間詰め含む全体の弦幅）の求め方:** 角当たり計算で求めたsagitta（=R_eff-hCenter）は「バックロック本体の前面から見た、立坑中心を通る直線上でのライナーまでの深さ」であり、①②③⑤で使う支圧壁の有効幅Beffは、**このsagittaを弦・サジッタの通常式に代入して逆算した弦幅**とする（間詰めコンクリートまで含めた全体の弦幅。ユーザー指摘：バックロック自体の幅W1だけに限定する必要はない）。BR-ST（φ5m立坑）の実例で検算すると、sagitta=0.733m、Beff=2√(0.733×(4.95-0.733))=**3.516m**となった。これは今回の会話で複数回ユーザーから提示された図面に繰り返し登場していた「3516」という寸法と一致しており、この値がずっと「正解」として図面に描かれていたことが分かった（47.7〜47.10で試みていた「支圧壁の計算.xlsx」のB=3.3437mは、弦幅1673m時点の単純なサジッタ計算による別の値で、今回の角当たり計算とは異なる量だったと判明——結果的に前回の質問「両者は矛盾しているのでは」への答えは「そもそも別の量を比較していた」だった）。

**実装:**
- `computeWallBeff()`のバックロック・テーパー形分岐を、`computeBackrockPlacement()`でsagitta/hCenterを求め、そのsagittaから弦・サジッタ式でBeffを逆算する形に戻した（`!backrockUse`の現場打ち分岐とは完全に別の分岐に戻した）。
- `applyBackrockSelection()`・`calcBacklock()`側で、`wallThickness`・`wallBw`を再び読み取り専用にし、角当たり計算の結果を毎回表示するようにした。
- `wallShapeProposalHTML()`のNG時提案メッセージも「厚さを増やしてください」から「より大きい型番を選定してください」に戻した（Beffが製品寸法から決まる以上、自由に厚さを増やすことはできないため）。
- `renderBackrockGapConcrete()`のformula表示・コメントも角当たり計算ベースの表記に戻した。

**検証:** D=5m・φ1350・BR-ST（自動選定）で、`wallThickness`が読み取り専用で0.733mを表示し、`wallBw`が3.516mを表示することを確認。この状態で間詰め断面積が0.402m²（プラス）になり、47.10で残っていた-0.047m²の問題も解消したことを確認（角当たり計算は定義上「ブロックの角がライナーに接するまで」の配置のため、ブロック自体が弓形からはみ出すことは原理的に起こらない）。立坑径を極端に小さく（φ1.6m）すると`placementOk=false`となり「立坑が小さすぎます」という警告が正しく表示されることも確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v39`→`v40`に更新済み。

**47.11の教訓:** ユーザーから「製品なので寸法が決まっている」という指摘があった場合、それは「自由入力を許すべきでない」という強いシグナルである。47.7で「間詰めコンクリートまで含めて計算してよい」という指摘を「厚さを自由入力にしてよい」と拡大解釈してしまったのが今回の遠因だった——「間詰めを含めて計算してよい」は「Beffの範囲を本体の幅W1だけに限定しなくてよい」という意味であって、「厚さの決め方（角当たり計算）まで自由入力に変えてよい」という意味ではなかった。ユーザーの指摘を実装に反映する際は、指摘の対象範囲（どの変数・どの計算ステップの話か）を早合点せず、変更後に元の一次資料（今回は複数の図面に一貫して現れていた「3516」という数値）と再度突き合わせる確認を怠らないこと。

---

### 47.12 補強リング使用時の有効半径の算出式を修正（呼び径−50mmを経由しない）

ユーザーから、支圧壁タブの補強リングの計算式について訂正があった：「補強リングがある場合、ライナープレート呼径から補強リングサイズを引いた数字＝内径となります」。

これまで（38章）は、まず通常通り「呼び径−50mm」でライナー内側半径Rを求め、そこから補強リングのH鋼せいの半分をさらに差し引いていた（R_eff = R − H/2 = (呼び径−50mm−H)/2）。しかしユーザーの指摘は、補強リング（H鋼）が波形部分の突出量（25mm×2辺＝50mm）より大きく内側に突出するため、**「呼び径−50mm」を経由せず、呼び径から直接H鋼せいを差し引く**（R_eff = (呼び径−H)/2）べき、というものだった。

**修正:** `effectiveLinerRadius(R)`を`effectiveLinerRadius(pitD, R)`に変更し、補強リング使用時は引数の`R`（呼び径−50mm由来）を使わず、`pitD`（呼び径そのもの）から直接`(pitD − H) / 2`を計算するようにした。呼び出し元`computeWallBeff()`も`effectiveLinerRadius(pitD, R)`に更新。UIの補強リング説明note-boxの文言も修正した。

**検証:** D=5m・補強リングH=100mmの条件で、修正前はR_eff=((5-0.05)-0.1)/2=2.425mだったのに対し、修正後はR_eff=(5-0.1)/2=**2.45m**になることを確認（差は25mm、ちょうど波形部分の片側突出量に相当）。リング未使用時はR_eff=2.475m（従来通り「呼び径−50mm」ベース）のままであることも確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v40`→`v41`に更新済み。

**47.12の教訓:** 「AからBを引く」「AからCを引く」という2つの計算が両方存在する場合、常に両方を順番に適用してよいとは限らない——今回のように、Cの方がBを包含する（H鋼せいが波形の突出量より大きい）場合は、Bの計算を丸ごと迂回してAから直接Cを引くべきケースがある。ユーザーから「呼び径から補強リングサイズを引く」という短い訂正を受けた際、既存の「呼び径−50mm」ステップにさらにH鋼せい/2を追加する式になっていないか、必ず立ち止まって検算すること。

### 47.13 補強リングの表記から「せい」を削除

ユーザーから「H鋼せい　の　せいって何ですか」という質問があり、「せい（成）」＝H形鋼の断面全高（フランジ外面〜外面）を表す専門用語である旨を回答した。続けて「せいというのを消して、H鋼だけにしてください」との指示があったため、支圧壁タブの補強リング関連表記から「せい」を削除した。

**修正:** ラベル「補強リング H鋼サイズ（せい）[mm]」→「補強リング H鋼 [mm]」に変更。`wallRingNote`のnote-box文言、`effectiveLinerRadius()`直前のコメント（3991〜3996行付近）も「H鋼せい」→「H鋼」に統一。なお「推進定規に使うH形鋼」（795行・4207行、ジャッキ配置カード用の別のH形鋼機能）の「せい」表記は、補強リングとは無関係の別機能のためユーザーの指示範囲外と判断し、そのまま残している。

**検証:** `wallRingH`欄のラベルが「補強リング H鋼 [mm]」と表示されることを確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v41`→`v42`に更新済み。

**47.13の教訓:** UIの表記変更を指示された際は、同じ単語が別の無関係な機能（今回は推進定規用H形鋼）にも使われていないか確認し、指示の対象範囲（今回は「補強リング」の文脈のみ）を超えて変更しないこと。表記統一の名目で無関係な箇所まで一括置換すると、意図しない変更になる。

---

## 48. 推進力タブに「元押ジャッキ仕様」を統合し、必要ジャッキ本数を選択式UIに変更／ジャッキ配置カードを支圧壁タブから移動

ユーザーから「推進力タブの一番上にある元押ジャッキ使用（＝『🔧元押ジャッキ仕様』カード）を計算結果サマリーに統合してほしい。必要ジャッキ本数の欄を選択してジャッキの構成を選べるようにしてほしい。支圧壁タブのジャッキ配置カードもこの下に統合して、ジャッキ関係の決定をすべて推進力タブで完結させたい」という要望があった。従来、ジャッキに関する入力・判断が「推進力タブ上部の元押ジャッキ仕様カード」「同タブ内サマリーの必要ジャッキ本数（文字表示のみ、選べない）」「支圧壁タブのジャッキ配置（推力分散用）カード」の3箇所に分散していたのを、推進力タブの計算結果サマリーカード1箇所に集約した。

**変更内容:**

1. **「🔧元押ジャッキ仕様」カード（推進力計算実行ボタンの上、常時表示）を削除し、その中身（`jackCapUnit`・`jackCount`入力欄）を「📊計算結果サマリー」カード内に移動。** これにより、ジャッキ1本の能力・使用台数の入力欄は計算結果（F_total等）が表示されているのと同じカード内、かつ計算実行後（`#calcResult`が表示された後）に現れる形になった。DOM上は`#calcResult`の中に入っただけで、`display:none`で隠れている間もinput要素自体は存在し続けるため、`n()`/`v()`/`setV()`によるJS側の読み書きには影響しない。

2. **必要ジャッキ本数の表示を、文字だけの案内から「クリックで採用できる選択式UI」に変更。** `renderResults()`内の`jackRows`生成ロジックを、`.jack-choice`という新しいクリック可能な行要素（CSSはmode-btn/shaft-chipと同系統の見た目で新規追加）に変更し、`#jackChoiceRows`コンテナへ描画するようにした。各行は`onclick="selectJackConfig(候補の能力, 候補の本数)"`を持ち、クリックすると新設の`selectJackConfig(cap, count)`関数が`jackCapUnit`・`jackCount`にその値を`setV()`で反映し、`autoSave()` → `refreshForceIfCalculated()`（既に計算済みなら`calculate()`を再実行して`renderResults()`が呼ばれ、選択状態のハイライトも含めて再描画される）→ `renderJackStages(computeWallBeff())`（下記3のジャッキ配置カードを最新の台数で再描画）を呼ぶ。現在の`jackCapUnit`/`jackCount`と一致する候補行には`.active`クラスと✅が付き、どれを採用中か一目で分かるようにした。1500kN・1000kNの2候補（左右対称配置のため常に偶数本に切上げ）は据え置き、手動で任意の値を直接入力したい場合は引き続き下の入力欄を直接編集すればよい（選択式UIは「よくある構成への近道」であり、自由入力を置き換えるものではない）。

3. **支圧壁タブの「🔩ジャッキ配置（推力分散用）」カード（`jackStagesCard`、推進定規のH形鋼選択・断面図・段別のジャッキ幅/設置高さ・局所支圧壁厚さの表）を、支圧壁タブ（`#p3`）から丸ごと切り出し、推進力タブ（`#p2`）の計算結果サマリーカードの直後（`#reportCard`の手前）に移動。** これにより、ジャッキの能力・台数を決めるとその場ですぐ配置（段数・幅・設置高さ）を確認・調整できるようになり、「ジャッキ関係はすべて推進力タブで決定する」というユーザーの要望どおりの構成になった。`renderJackStages()`・`drawJackPositionDiagram()`等のJS関数はすべて`document.getElementById()`によるID参照でDOM位置に依存しないため、カードを移しても計算・描画ロジック自体は無修正で動作する（`CANVAS_REFRESH_FNS`もcanvas idキーのマップなのでページ非依存）。案内文言のうち「推進力タブでジャッキ使用台数を設定すると表示されます」「『⚡推進力』タブの『ジャッキ1本の能力』で変更できます」等、**別タブへの参照だった箇所は、同タブ内を指す表現に書き換えた**（例:「上の『必要ジャッキ本数』で選択するか、使用台数を直接入力すると表示されます」）。計算書8章（推進設備の検討）の脚注も同様に「『🔧元押ジャッキ仕様』カード（このタブの上部）」→「『📊計算結果サマリー』カードの『採用ジャッキ』欄」に更新した。支圧壁タブには局所支圧照査カード（`localBearingCard`、ジャッキ受圧鉄板の照査）は従来通り残している（ユーザーの指示は「ジャッキ配置」カードのみが対象で、局所支圧照査カードへの言及はなかったため）。

**検証:** `preview_start({name:'m_app_025_static'})`でBrowserペインからロードし、`showPage`/`addCurve`/`calcAlignment`/`calculate()`をJS直叩きでテストデータ作成 → `#jackChoiceRows`に1500kN/1000kNの2行がクリック可能な状態で描画され、既定の`jackCapUnit=1500,jackCount=4`と一致する行に`.active`＋✅が付くことを確認。`selectJackConfig(1000,4)`を実行すると`jackCapUnit`が1000に更新され、選択行のハイライトが移動し、`#jackStagesTable`（ジャッキ配置カード）が能力1000kN・ジャッキ径目安200mmで即座に再描画されることを確認。`#p2`（推進力タブ）内に`jackStagesCard`が存在し`#p3`（支圧壁タブ）には存在しないこと、旧「元押ジャッキ仕様」という見出し文字列がページ内に残っていないことを確認。カードを開いた状態でジャッキ配置断面図canvas（`jackPositionDiagram`）が実際にサイズを持って描画されることも確認。`calcBacklock()`（支圧壁タブ側の再計算関数）をエラーなく実行できることを確認し、カード移動が支圧壁タブ側のロジックに悪影響を与えていないことも確認。`getProjectData().jack`にcapUnit/countが正しく含まれることを確認（JSON保存/復元は無修正のため影響なし）。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v42`→`v43`に更新済み。

**48章の教訓:** 複数タブに分散した同じテーマ（今回は「ジャッキ」）の入力・判断を1箇所に集約する場合、移動対象の関数・要素が「IDベースでDOM位置に依存していないか」を先に確認すること（依存していれば安全に移動でき、依存していれば別途配線し直しが必要になる）。今回は`renderJackStages`等がすべて`getElementById`ベースだったため、HTML側の位置を変えるだけで済んだ。ただし、**カード内の説明文（「〇〇タブで設定すると表示されます」等、他タブを名指しする文言）はDOM位置とは独立に残ってしまう**ため、機能面のテストが通ってもUI文言の矛盾（存在しないはずの「別タブ」への案内）を見落としやすい。移動作業では必ず該当カード内外のテキストをgrepし、タブをまたぐ言及がないか洗い出すこと。

### 48.1 計算書「3.施工条件」の線形ビジュアル図で、区間ラベルが点ラベルと完全に重なり判読不能になるバグを修正

上記48章の作業を進めていたユーザーが、計算書の「3.施工条件」に埋め込まれた線形ビジュアル図（`drawAlignmentVisual('reportAlignmentVisual', ...)`、13章・31章で導入）で「字が重なって何が書いてあるの見えません」と報告した。

**原因調査:** `CanvasRenderingContext2D.prototype.fillText`を一時的にフックしてすべてのラベル描画呼び出し（テキスト・座標）を記録し、実際の描画位置を突き合わせたところ、2種類のラベルが**完全に同一のピクセル座標**に描画されていることが判明した。
1. 直線区間の中点ラベル（「第1直線」「最終直線」等）の位置を`seg.pts[Math.floor(seg.pts.length / 2)]`で求めていたが、直線区間の`pts`は`[始点, 終点]`の2点しかないため、`Math.floor(2/2)=1`となり**区間の終点そのもの**を指してしまっていた。結果、「第1直線」ラベルはBC点ラベルと、「最終直線」ラベルはEP点ラベルと同じ座標に描画され、文字が完全に重なっていた。
2. 曲線区間の中点ラベル（「R1」等）も同じ`Math.floor(pts.length/2)`の式を使っていたが、SP点（曲線の弧の中点）の定義も`curvePts[Math.floor(curvePts.length/2)]`と**全く同じ式**だったため、両者が常に同一座標になり、「R1」ラベルとSP点ラベルが完全に重なっていた。

**修正（[M_APP_005.html:1807〜1819行付近](M_APP_005.html)の`segs.forEach`内ラベル描画）:**
- 直線区間: 中点を配列インデックスではなく、始点・終点座標の**平均**で計算するよう変更（`{x:(pts[0].x+pts[last].x)/2, y:(pts[0].y+pts[last].y)/2}`）。これにより区間の真の中点になり、BC/EP等の点ラベルとは別の位置になる。
- 曲線区間: 中点サンプル（SP点と同じ）を使うのをやめ、あえて曲線の30%位置のサンプル点（`Math.floor(pts.length * 0.3)`）にラベルを置くよう変更。SP点ラベルとは異なる位置になる。

**検証:** `fillText`フックで描画座標を記録し、修正前は「第1直線」とBC点、「R1」とSP点、「最終直線」とEP点がそれぞれ完全に同一座標（距離0px）だったのが、修正後はいずれも距離0pxの完全重複が解消されたことを確認（直線・曲線1本の単純なケース、直線2本・曲線2本の複合ケースの両方で確認）。全ラベル間の総当たり距離を計算し、25px未満に近接するのは「最終直線」〜EC・EP間（13〜14px、区間長が短い場合の自然な近接）のみで、完全重複（0px）は残っていないことを確認。`read_console_messages`でJSエラー無しも確認。この修正は`drawAlignmentVisual`という共有関数への変更のため、基本情報タブのインタラクティブな線形ビジュアル表示（`#alignmentVisual`）にも同様に適用される。`service-worker.js`の`CACHE_NAME`を`v43`→`v44`に更新済み。

**未確認事項:** このセッションのBrowserペインの表示パネルが非表示状態だったため、修正後の実際のスクリーンショットでの目視確認はできていない（座標の突き合わせによる検証のみ）。次回、実際の画面表示での目視確認が望ましい。

**48.1章の教訓:** キャンバスのラベル位置計算で「配列の中央インデックス」を使う実装は、(a)要素数が少ない配列（直線の2点等）では意図と異なる要素を指してしまう、(b)同じ式を別の目的（区間ラベル・点ラベル）で使い回すと必然的に同一座標になる、という2つの罠がある。「重なって読めない」という報告を受けたら、まず`fillText`をフックして実際の描画座標をログに出し、目視ではなく座標の突き合わせで原因を特定するのが速い（今回はこの方法で数分で原因を特定できた）。

### 48.2 線形ビジュアル図のラベルを、点（BP/BC/IP/SP/EC/EP）以外は手動トグルで表示/非表示にできるように変更

48.1の修正は「完全に同一座標になる」バグは解消したが、区間が短い場合はラベル同士が依然として近接し読みにくいことがある（例: 48.1検証時の「最終直線」〜EP間13px）。ユーザーから「ある程度拡大するまでは（文字同士が重ならなくなる程度は）BP・BC・IP・SP・EC・EP以外非表示にして」と要望があり、続けて「注記円（R・IA・CL・SL・TL）も対象に含めるか」を確認したところ「手動で各項目の表示・非表示を切り替えできるようにして」という回答だった。すなわち、自動のズーム連動ではなく、**ユーザーが明示的にON/OFFを選ぶチェックボックス**を求められた。

**実装:**
- グローバル状態`alignmentLabelVis = { ro: false, segLabel: false, annotation: false }`を新設（[M_APP_005.html:1654行付近](M_APP_005.html)、`alignmentView`等の宣言のすぐ下）。既定はすべて非表示＝BP・BC・IP・SP・EC・EPの点ラベルのみが常に表示される状態。
- `drawAlignmentVisual()`内の3箇所をこのフラグで分岐: ①`segs.forEach`内の区間名ラベル（「第1直線」「最終直線」「R1」等、48.1で修正した箇所）は`alignmentLabelVis.segLabel`、②RO点のマーカー＋ラベルは`alignmentLabelVis.ro`、③曲線要素の注記円（R/IA/CL/SL/TL、ドラッグ移動・リサイズ機能ごと）は`alignmentLabelVis.annotation`が`false`の間は関数を早期`return`して描画・当たり判定登録の両方をスキップする。作図補助の破線のうち、BC-RO・RO-ECの2本もRO表示時のみ描く（BC-IP・IP-ECは常時表示のまま）。
- BP・BC・IP・SP・EC・EPの点マーカー・ラベル自体（`drawPoint`呼び出し）は元々フラグ分岐の対象外で、常に描画される。
- 「📈線形ビジュアル表示」カード（基本情報タブ、[M_APP_005.html:431行付近](M_APP_005.html)）に3つのチェックボックス（RO表示・区間名ラベル表示・曲線注記表示）を追加し、`onchange="toggleAlignmentLabelVis('ro'|'segLabel'|'annotation', this.checked)"`でフラグを更新して`drawAlignmentVisual()`を再描画する。
- `alignmentLabelVis`はグローバル変数でJSON保存/復元の対象にしていない（画面表示だけの一時設定、プロジェクトデータではないためユーザー確認済み方針には含めず、素朴にセッション内のみ有効とした）。**計算書「3.施工条件」の`#reportAlignmentVisual`も同じ`drawAlignmentVisual()`を呼ぶ共有関数のため、この設定は印刷時にもそのまま反映される**（基本情報タブでチェックを入れた状態で印刷すれば、印刷版にもRO・区間名・注記が出る）。

**検証:** `fillText`フックで描画されるラベル一覧を確認。既定状態（全フラグfalse）では`["方眼: 20m","BP","BC","IP","SP","EC","EP"]`のみが描画され、区間名・RO・注記は一切描画されないことを確認。`ro:true`のみで`RO`が追加、`segLabel:true`のみで`第1直線`・`R1`・`最終直線`が追加、`annotation:true`のみで曲線注記（`R1`・`R＝40.000m`等）が追加されることを、それぞれ他のフラグをfalseに固定した状態で個別に確認。HTML側のチェックボックス（`#alignVisRO`/`#alignVisSeg`/`#alignVisAnn`）が存在し、`checked`変更→`change`イベント発火で`alignmentLabelVis`が正しく更新されることも確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v44`→`v45`に更新済み。

**未確認事項:** 48.1と同様、このセッションでは実際のスクリーンショットでの目視確認ができていない（座標・状態の突き合わせによる検証のみ）。次回、実際の画面表示・チェックボックスのクリック操作感の目視確認が望ましい。

---

## 49. 測量タブ: スプレッドシート出力・PC版でのテンキー非表示（直接キーボード入力）・入力欄フォーカス時の全選択

ユーザーから測量タブ（測定数値入力）について3点の要望があった。
1. 「測量で入力した測量結果、計算結果をスプレッドシート形式でダウンロードできるようにして」
2. 「測定数値入力で専用テンキーを表示する仕様ですが、PC版で表示しているときは出ないようにして」
3. 「テンキーでも、直接入力でも、次または前の入力欄へフォーカスが移った時に、中のテキストがすべて選択されている状態にしておいて（そうすれば数値を打った時に前のものが上書きされる）」

**前提として判明したこと:** 測定数値入力の表（TP/KP行の距離・度・分・秒セル）は、44章で追加された時点では`<td onclick="openSurveyKeypad(...)">`という**タップ専用のテキストセル**で、実際のキーボード入力を受け付ける`<input>`ではなかった。そのため2の「PC版でテンキーを消す」だけでは、PCユーザーが数値を入力する手段が無くなってしまう。ユーザーの発言「テンキーでも、直接入力でも」は、直接入力（PCでのキーボード編集）という**新しい入力手段の追加**を前提にした発言と解釈し、あわせて実装した。

**実装:**
- **タッチ/PC判定:** `const IS_TOUCH_DEVICE = window.matchMedia('(hover: none), (pointer: coarse)').matches`（[M_APP_005.html:2126行付近](M_APP_005.html)、`tpRows`等のグローバル変数の近く）。ホバー不可 or ポインタが粗い（＝タッチ主体）端末をタッチ端末と判定する。ウィンドウ幅ではなくポインタ特性で判定するため、デスクトップウィンドウを狭くしてもテンキー表示には切り替わらない（意図通り）。
- **セルのHTML生成を`surveyCellHTML(kind, idx, field, val)`ヘルパーに統一**（[M_APP_005.html:2324行付近](M_APP_005.html)）。TP/KP・dist/deg/min/secの8箇所で重複していたセルHTML生成を1箇所にまとめた。
  - タッチ端末: `<input readonly onclick="openSurveyKeypad(...)">` — 従来通りreadonly（OS仮想キーボードは出ない）でタップするとテンキーが開く。`data-mode="touch"`を付与。
  - PC（それ以外）: `<input onchange="onSurveyCellCommit(...)">` — 直接キーボードで編集でき、フォーカスが外れた時（blur/Enter/Tab）に確定・バリデーション・`calcSurvey()`再計算まで行う。`data-mode="pc"`を付与。テンキー同様、数値ごとの範囲チェック（度0〜359、分秒0〜59、距離は0以上）を`onSurveyCellCommit()`内で行う。
  - **どちらの場合も`onfocus="this.select()"`を付与**（3の要望に対応）。タッチ端末側もreadonlyだが`.select()`自体は可能なので、テンキーをタップで開いた瞬間に選択状態の見た目になる。
  - 全角文字での要素分離を避けるため、`oninput`ではなく`onchange`でPC側の確定処理を行っている（10章の教訓：入力欄を含むコンテナを`innerHTML`で再描画するハンドラは`onchange`にすべき、を踏襲。ただし今回`calcSurvey()`は`#surveyMeasureTable`自体は再描画しないため、実際には`oninput`でも壊れないが、念のため同じ方針で統一した）。
- **`openSurveyKeypad(kind, idx, field)`にフォーカス処理を追加**（[M_APP_005.html:2405行付近](M_APP_005.html)）: テンキーを開く・テンキーの次/前ボタン（`surveyKeypadNav`/`surveyKeypadVert`、内部で`openSurveyKeypad`を再度呼ぶ）で移動する、いずれの場合も対象セルの`<input>`に対して`focus({preventScroll:true})` + `select()`を実行し、選択状態を可視化する。`preventScroll`は、テンキー自体が画面下部に固定表示されるため、フォーカスによる意図しないスクロールを防ぐため。
- **`refreshSurveyCell()`を`el.textContent = text`から`el.value = text`に変更**（[M_APP_005.html:2446行付近](M_APP_005.html)）。セルが`<td>`から`<input>`になったことに伴う必須修正（`textContent`のままだとinput要素の表示値が更新されなくなるバグになるところだった）。
- **`applySurveyProtectionUI()`を拡張**（[M_APP_005.html:2280行付近](M_APP_005.html)）: 既存の`.locked`クラス切替に加え、`data-mode==="pc"`のセルのみ`readOnly`を保護状態と連動させる（タッチ端末セルは元々常時readonlyのため対象外、上書きしないよう分岐した）。
- **CSS**（[M_APP_005.html:132行付近](M_APP_005.html)）: `td.survey-cell`だったセレクタを`input.survey-cell`に変更し、`<input>`がそれまでの`<td>`と同じ見た目（枠なし・中央寄せ・太字）になるよう`border:0;background:transparent;width:100%;text-align:center`等をリセット。PC編集モード用に`input.survey-cell[data-mode="pc"]:focus`でフォーカス時のハイライト枠を追加。
- **スプレッドシート出力（1の要望）:** `exportSurveyCSV()`を新設（[M_APP_005.html:3960行付近](M_APP_005.html)、`exportPipeForceCSV()`の直後。UTF-8 BOM付きCSV、Excelでの文字化け対策という既存パターンを踏襲）。「測定数値入力」（TP/KP各行の距離・度・分・秒・役割）と「計算結果一覧」（KP・役割・X・Y・位置・RO-測点距離・Rとの差・計画X・計画Y・左右誤差・判定）の2セクションを1つのCSVにまとめて出力する。計算結果が未計算の場合はその旨の行だけ出力し、エラーにはしない。「📊計算結果一覧」カードにボタンを追加（[M_APP_005.html:923行付近](M_APP_005.html)）。

**検証:** Browserペインで`resize_window`によりデスクトッププリセット（`pointer:fine`）・モバイルプリセット（`pointer:coarse`）を切り替え、それぞれ`location.reload()`後に`IS_TOUCH_DEVICE`が`false`/`true`になることを確認。デスクトップ側: セルが`data-mode="pc"`の編集可能`input`として描画され、`.focus()`→`onchange`確定で`tpRows[0].dist`が正しく更新され、`openSurveyKeypad`は一切呼ばれない（テンキーの`visible`クラスが付かない）ことを確認。実際のfocusイベント経由で`selectionStart=0`・`selectionEnd`=値の長さ（全選択）になることも確認（`.focus()`を2回連続で呼ぶとブラウザがfocusイベントを再発火しないため、事前に`blur()`してから検証する必要があった点に注意）。モバイル側: セルは`readonly`の`data-mode="touch"`で、タップ相当の`openSurveyKeypad()`呼び出しでテンキーが開き、対象セルが選択状態（`selectionStart=0`, `end`=値の長さ）になることを確認。テンキーで数字を続けて押すと（`7`→`.`→`5`）、既存値`12.345`を上書きして`7.5`になる（追記されない）ことを確認——これは44章時点の`surveyKeypadFirstPress`機構により元々実装済みだった挙動で、今回の変更で壊れていないことも合わせて確認。`surveyKeypadNav(1)`でテンキーの対象フィールドが次に進み、そのセルも選択状態になることを確認。CSV出力は`Blob`コンストラクタを一時的にフックして内容を捕捉し、BOM付き・2セクション構成・見出し行が期待通りであることを確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v45`→`v46`に更新済み。

**49章の教訓:** 「PCでは○○を出さないで」という要望は、その○○が唯一の入力手段になっている場合、暗に「代わりの入力手段を用意してほしい」を含んでいることがある。今回はユーザーが後続の発言（「テンキーでも、直接入力でも」）で明示してくれたが、単発の要望文だけを読んで「テンキーを消す」だけを実装していたら、PC利用者が数値を一切入力できなくなる欠陥ができるところだった。UIの表示条件を変える依頼を受けたら、その表示が消えた後にユーザーが目的を達成できる手段が残っているかを必ず確認すること。

---

## 50. 測量タブ: Excel(.xlsx)出力を4シート・生の計算式付きに全面刷新（49章のCSV出力を置き換え）

49章で実装したCSV出力（測定入力＋計算結果一覧、値のみ）に対し、ユーザーから「セルに計算式が入った状態で、シートごとに分けて出力してほしい。誤差計算という一番複雑な部分も含めて生のExcel式で再現してほしい」という、はるかに踏み込んだ要望があった。CSVには計算式を持たせる仕組み自体が無いため、事前に2点をユーザーに確認した：①ファイル形式はExcel(.xlsx)で新規作成（外部ライブラリ不使用の単一HTMLという方針上、ZIP+OOXMLを自前実装する）、②最も複雑な「法線との測定誤差計算結果」（区間判定・垂線距離）も含めてすべて生の計算式で再現する——回答はいずれも「そのようにしてほしい」。加えて「シートごとに分けたい: 基線データ／測量基準データ／各座標データ／測定結果＋測定誤差」という4シート構成の指定があった。49章のCSV出力（`exportSurveyCSV()`とそのボタン）は本章の実装で完全に置き換えた。

### 50.1 実装方針

- **外部ライブラリは一切使わない**（この単一HTMLアプリの方針を踏襲）。.xlsxはZIPアーカイブ＋OOXML(SpreadsheetML)なので、CRC32とZIP（STORED=無圧縮）を自前実装し、`[Content_Types].xml`・`_rels/.rels`・`xl/workbook.xml`・`xl/_rels/workbook.xml.rels`・`xl/styles.xml`（最小限）・`xl/worksheets/sheet1〜4.xml`の9パートで構成した（[M_APP_005.html:3961行付近](M_APP_005.html)〜、`xlsxCrc32`/`xlsxBuildZip`/`xlsxMakeSheet`/`xlsxNum`/`xlsxStr`/`xlsxFormula`/`xlsxFormulaStr`/`xlsxSheetXml`等のヘルパー群）。
- セルは`<v>`（キャッシュ値）と`<f>`（計算式、先頭"="無し）の両方を持たせる。キャッシュ値はアプリ自身がJSで計算した「正解」の数値をそのまま埋め込むため、**Excelを一度も開かなくてもファイル内には正しい数値が表示される**。式はExcelが開いた時点（自動再計算）で評価され、通常はキャッシュ値と一致する。ユーザーが基線データや測量基準データのセルを書き換えれば、下流のセルがすべて連動して再計算される。
- **Excelの`ATAN2(x_num,y_num)`はJSの`Math.atan2(y,x)`と引数順が逆**という点を全式で統一的に注意した（JSの`atan2(dy,dx)`を再現する式は必ず`ATAN2(dx,dy)`と書く）。

### 50.2 各シートの内容

1. **基線データ**: 現場名・スパン名・測定本数・測定日 → 第1直線長(BP→IP1) → 曲線ごとの行（半径R・方向・交角の度/分/秒・**交角10進(式)** `=度+分/60+秒/3600`・直線長(前IP→当曲線IP)） → 最終直線長(最終IP→EP)。
2. **測量基準データ**: 「法線方向座標」（機械点(0,0)・後視点(10,0)固定、法線初期方向角=式、常に0°。基本情報タブの模式図が使う理論上の基準に対応する参考値）と、「測量基点座標」（実際の器械点・後視点の実測値、機械初期方向角=式）の2ブロック。**実際の座標計算を駆動するのは後者**（`buildSurveyGeometry()`が`surveyBaseAz`＝機械初期方向角を起点に全座標を計算するため）。
3. **各座標データ**: BP(0,0固定) → IP1〜IPn（前の点のX/Y・方位角OUT・基線データの直線長/交角10進を参照する式）→ EP。それに続けてBC/RO/EC座標（曲線の数だけ、IPの座標・TL・方位角を参照する式）。方位角IN/OUT・TL は「補助」列として保持し、後続の式から参照する。
4. **測定結果＋測定誤差**: まず測量結果（TP/KP各行の距離・度・分・秒・**角度10進(式)**）。続けてトラバース計算（後視方位角・前視方位角・XY座標をすべて式で算出、`calculateSurveyTraverse()`と同じ「器械点/後視点を1つずつ繰り上げる」規則を再現）。続けて判定基準（現在のUIのしきい値をそのまま反映）。最後に法線との測定誤差計算結果——**KPごとに、全直線区間＋全曲線区間を候補として垂線距離・半径差・優先度・「スコア」（`=優先度*10000000+誤差mm`という式）を計算する小テーブルを作り、`MIN`→`MATCH`→`INDEX`で最良候補（最小スコア）の計画X・計画Y・誤差・左右を抽出する**、という設計にした（優先度=0が最優先の曲線内、1が直線内、8/9がそれぞれ範囲外——`judgeStraight()`/`judgeCurve()`/`judgePoint()`のロジックそのもの）。`INDEX`/`MATCH`は配列数式（Ctrl+Shift+Enter）を使わずに済む標準関数のみで組んでいるため、一般的なExcelでそのまま開ける。各KPの結果は末尾の「一覧」表にも集約表示する。

### 50.3 検証（重要: 実際にExcelで開いての確認はできていない）

このセッションの環境にはExcel自体が無いため、**「Excelで実際に開いて数式バーや再計算結果を目視した」という意味での検証はできていない**。代わりに、次の方法でファイルの妥当性と数値の正しさを検証した。

1. **ZIP構造の妥当性**: 生成したバイト列（`Blob`コンストラクタを一時的にフックして捕捉）を、ブラウザ上で自前のZIPパーサ（中央ディレクトリ・EOCDを読む簡易実装）で再度パースし、9個のパートすべてが期待通りの名前・サイズで含まれることを確認。
2. **XMLの妥当性**: 各パートを`DOMParser().parseFromString(...,'application/xml')`でパースし、`parsererror`が1件も無いことを確認（全9パート）。
3. **壊れた参照が無いこと**: 生成した全XMLテキストに`undefined`/`NaN`という文字列が1件も含まれないことを確認（セルアドレスの取り違え・未定義参照があれば必ずこの文字列が漏れ出るはずのため）。
4. **数値の正しさ（最重要）**: アプリ自身が`calcSurvey()`で計算した`surveyResults`（実際に画面「計算結果一覧」に出る、既に信頼できる数値）と、エクスポートしたシート4の「一覧」表のキャッシュ値を突き合わせ、KP1〜KP3すべてで测定X/Y・計画X/Y・誤差mm・左右・判定が浮動小数点誤差の範囲（10桁以上一致）で完全一致することを確認。この過程で**実際に1件のバグを発見・修正した**（50.4節）。
5. **境界ケース**: 曲線0本（直線のみ、BP→EPの1区間だけ）、TP0件（KPのみ・器械点を直接基準にするパターン）でもクラッシュせず、それぞれアプリの計算結果と一致することを確認。
6. **ボタンからの実行**: JS関数を直接呼ぶだけでなく、実際に画面のボタン（`querySelector`で取得し`.click()`）を叩いて動作することも確認（これも50.4節のバグ発見につながった）。

**ユーザーへのお願い:** 上記はあくまで「ファイルとして壊れていないか」「アプリの計算結果とキャッシュ値が一致するか」の検証であり、**Excel自身が数式を評価して同じ答えを出すかどうかの最終確認は、実際にファイルを開いて行っていただく必要がある**。特に、基線データや測量基準データのセルを書き換えたときに下流のセル（各座標データ・測定結果＋測定誤差）が正しく連動して再計算されるか、一度试していただくことを推奨する。

### 50.4 検証中に発見・修正したバグ2件

1. **ボタンが旧関数`exportSurveyCSV()`を呼んだままだった:** 本章でCSV出力(`exportSurveyCSV`)をExcel出力(`exportSurveyXLSX`)に全面置き換えたが、「📊計算結果一覧」カードのボタンの`onclick`を更新し忘れており、そのままだとボタンを押した瞬間に`exportSurveyCSV is not defined`で失敗する状態だった。50.3節5の「実際にボタンをクリックして確認する」手順で発覚し、修正した。**教訓（再掲、44章相当）: 関数をリネーム/置き換えるときは、その関数を直接呼び出すテストだけでなく、実際のUI操作経路（ボタンのonclick等）からも検証すること。** JSを直接叩くテストは呼び出し側の配線ミスを検出できない。
2. **測量基準データの「機械初期方向角」で、アプリ本体との間に微小（0.02〜0.4mm程度）な数値の食い違いがあった:** キャッシュ値をアプリの`surveyResults`と突き合わせたところ、KP1の誤差が2895.31mm（エクスポート側）と2895.71mm（アプリ側）で、100分の数mm〜1mm未満のズレがあった。原因を辿ると、アプリ本体の`calcSurveyBaseAz()`が機械初期方向角を**小数点以下3桁に丸めた文字列**として非表示欄(`surveyBaseAz`)に保持し、`buildSurveyGeometry()`はその**丸められた**値を使って全座標を計算していることが分かった。一方、エクスポート側は器械点・後視点から**丸めずに**フル精度で角度を再計算していたため、桁落ちならぬ「丸め落ち」の差が生じ、それが直線・曲線チェーンを経て下流に伝播していた。**修正:** 機械初期方向角のキャッシュ値を`Math.round(x*1000)/1000`で丸め、Excel式側も`ROUND(MOD(DEGREES(ATAN2(...)),360),3)`と丸めを明示することで、アプリ本体の丸め挙動を式レベルでも再現した。修正後、KP1〜KP3すべてでアプリの計算結果と10桁以上の精度で一致することを確認した。**教訓: 「アプリの内部値をそのまま計算式として書き出す」場合、アプリがUIの表示欄（特に`fmt(x,3)`等で丸めてから非表示inputに文字列として保持しているような値）を経由して丸めを行っている箇所がないか必ず確認すること。丸め自体は数mm未満の些細な差に見えても、「アプリの計算結果を検証するための監査用シート」という本来の目的からすれば、丸めもろとも完全一致させるべきである（監査対象と監査ツールの答えが食い違うと、監査ツールの方が疑われてしまう）。**

### 50.5 既知の限界（意図的に対応を見送った点）

- **曲線の弧の境界（delta≡0、曲線内の点が幾何学的にBC点の真上に来る場合）で、左曲線(dir='L')の`delta_norm`計算式にごく僅かな理論上の不整合がある**（キャッシュ値のJS計算は正しいが、Excel式`MOD(delta,2π)-2π`はdelta=0の場合に0ではなく-2πを返す）。実測データでdeltaが厳密に0になることは浮動小数点の性質上まず起こらないため実害は無いと判断し、対応を見送った。将来この境界で不審な判定（本来「曲線内」であるべき点が「範囲外」と判定される）が疑われた場合は、この既知の限界を思い出すこと。
- 誤差計算の「候補一覧→MIN/MATCH/INDEXで最良候補を選ぶ」方式は、区間数（曲線数×2+1）が多いほどKPごとの候補行数が増える。曲線数が非常に多い（10本超など）現場では、シート4が縦に長大になる。実用上の現場（曲線数は通常数本程度）では問題にならない想定。

---

## 51. Excel出力（50章）で計算結果が#VALUE!/#DIV/0!になる致命的バグを発見・修正 — シート4の誤差計算式がシート3のセルをシート名修飾せずに参照していた

ユーザーが実際にアプリからダウンロードしたファイル（`MYcurve_館町下水_１~2_測量データ_2026-08-09.xlsx`、CAD貼付け用の内容を赤字で追記済み）をExcelで開いたところ「計算結果にエラーが出ています」と報告があった。

**調査方法:** ダウンロードされたファイルの実物を、xlsxスキル経由で`openpyxl`（`data_only=True`で値、`data_only=False`で式の両方を読む2パス方式）を使って直接検査した。「測定結果＋測定誤差」シートで**156個のセル**が`#VALUE!`または`#DIV/0!`になっていることを特定した。

**原因:** シート4（測定結果＋測定誤差）の「法線との測定誤差計算結果」ブロックは、シート3（各座標データ）で計算したBP・BC・RO・EC・EPの座標を参照して垂線距離や半径差を計算する設計だった。ところが実装時、これらの座標アドレス（`bpXAddr`・`epXAddr`・`curveGeomInfo[].bc/ro/ec.xAddr/yAddr`）を**シート名で修飾しないまま**（例: `"B9"`）保持しており、シート3自身の式の中で使う分には問題なかったが、**シート4の式の中で同じ文字列を使うと「シート4自身のB9」を指してしまい**、全く別の値（TP/KP測定データの一部やラベル文字列）を参照する結果になっていた。例えば実際に出力されていた式`=SQRT((B9-B3)^2+(C9-C3)^2)`は、本来`=SQRT(('各座標データ'!B9-'各座標データ'!B3)^2+('各座標データ'!C9-'各座標データ'!C3)^2)`であるべきだった。

このバグは50章の検証で見逃していた。50章の検証は「エクスポート側のJSでキャッシュ値として書き込んだ数値」と「アプリ本体の計算結果」を突き合わせる方式だったため、**キャッシュ値自体はJS側で正しく計算していたので一致して見えた**が、Excelが実際に式を評価すると全く別のセルを見てしまい、キャッシュ値とは無関係な（多くの場合エラーになる）結果を返していた。つまり「キャッシュ値の一致確認」は式のテキスト自体が正しいかどうかを検証できていなかった。

**修正（[M_APP_005.html](M_APP_005.html)の各座標データシート生成部分）:** BP・EP・BC・RO・ECの座標アドレスを、`xlsxFormula()`/`xlsxNum()`から返ってきた直後に`xlsxRef(s3, ...)`でシート名修飾した状態で変数に保持するよう変更した。同一シート内（シート3自身の後続の式）から使っても自己シート修飾は無害なため、修飾を「使う場所ごと」ではなく「値を確定させた場所」で一括して行うようにし、修飾漏れの再発を構造的に防いだ。曲線の中点方位角(IN/OUT)やTLなどシート3内でしか使わない補助列はシート3ローカルのまま（無修飾）とした。

**検証:** 修正前の実データで発生していたのと同じ形（複数曲線・複数KP）のテストデータで再度エクスポートし、生成されたXMLから該当セルの式テキストを抽出したところ、`=SQRT(('各座標データ'!B10-'各座標データ'!B3)^2+...)`のように正しくシート名修飾されていることを確認した。この環境にはExcel本体もLibreOfficeも無く（xlsxスキルの`recalc.py`はUnixソケット前提でこのWindows環境では動作しなかった）、**実際にExcelで再計算させて式の評価結果を目視確認することはできていない**が、少なくとも今回発覚したバグのパターン（シート名修飾漏れ）が該当箇所すべてで解消されていることは、生成されたXMLの式テキストを直接調べて確認した。

**教訓（重要）:** 生成した式のキャッシュ値をアプリ側の独立計算と突き合わせる検証は、**「キャッシュ値」対「本来あるべき値」の一致は確認できるが、「式のテキスト自体がExcelで正しく評価されるか」は別問題であり、検証できない**。クロスシート参照を伴う計算式を自動生成するときは、(1) 生成された式のテキスト自体を人間が読んで正しいセル参照になっているか確認する、(2) 可能であれば実際にExcel/LibreOfficeで開いて再計算させる、の両方が必要。今回のような「値は合っているが式は壊れている」バグは、キャッシュ値の突き合わせだけでは原理的に検出不可能だった。**次回このような生成的な計算式を扱う場合は、生成直後に必ず「クロスシート参照のアドレスは全てシート名修飾されているか」を機械的にチェックする（例: 該当する式テキストに対象シート名の文字列が含まれているか）ステップを検証手順に組み込むこと。**

---

### 51.1 Excel出力「判定基準」の項目名を「注意しきい値[mm]＝社内規格値」「NGしきい値[mm]＝規格値」に変更

ユーザーから、Excel出力シート4の「判定基準」欄の表記を明確化する指示があった。「注意しきい値[mm]」は実質的に社内管理値（規格値×任意設定した割合、既定80%）、「NGしきい値[mm]」は規格値そのものを指すため、それぞれ「＝社内規格値」「＝規格値」を項目名に付記した（[M_APP_005.html:4404行付近](M_APP_005.html)の`exportSurveyXLSX()`内）。数値・計算ロジック自体は変更していない（52章時点で確認済みの`specLimitByPipeDia()`・`internalPct`の挙動はそのまま）。生成XMLに新しいラベルが正しく反映されることを確認。`service-worker.js`の`CACHE_NAME`を`v48`→`v49`に更新済み。

---

### 51.2 検討事項（コード変更なし）: Jカーブ（IA>180°級の急曲線）での区間取り違えリスクについて

ユーザーからExcel出力の「スコア」列（`=優先度*10000000+誤差mm`）を見て、区間判定ロジック（`judgeStraight`/`judgeCurve`/`judgePoint`/`curveDelta`、48〜51章のExcel出力もこのロジックを式で再現している）についての設計上の懸念が示された。実際のJカーブ実例（IA=241°49'48"、R=61.874m、CAD図面）の提示もあった。

**懸念の内容:** 直線区間の判定（`judgeStraight`、2端点への投影による垂線距離）・曲線区間の判定（`judgeCurve`、曲線中心ROから見た角度による円弧内判定）は、いずれも「Xが常に増加する」といった前提を使わない、区間ごとの実座標に基づく幾何判定であることを確認した（この点はユーザーの懸念に反して問題なし）。一方で、IAが180°を大きく超えるような急曲線（Jカーブ／ヘアピンに近い形状）では、往路（曲線に入る前の直線）と復路（曲線を抜けた後の直線・曲線）が空間的に接近し、1つの測点が複数の区間で「範囲内」と判定されてしまい、優先度が同じ場合は誤差が小さい方が選ばれる（＝本来と異なる区間が選ばれる可能性がある）という理論上のリスクを議論した。

**対策案として本数（測定順序・想定チェーンエージ）を判定基準に加えることを検討したが、ユーザーの判断は「座標を優先し、本数は補助情報に留めるべき」というものだった。** 理由: 管の有効長の想定違いなどで実際の本数とズレる可能性があり、本数を主たる判定基準にすると誤判定の原因になりかねないため。

**結論（コード変更なし）:**
- 実際の現場でこれほど急な曲線（IA>180°級）を推進した実績はまだなく、この問題による実際の誤判定は発生していない。
- 座標優先の現行ロジック（`judgeStraight`/`judgeCurve`/`judgePoint`）はそのまま維持する。本数を用いた追加の判定基準は、今回は実装しない。

**訂正（重要）:** 上の議論の中で「実測時は測量者自身がどの区間にいるか把握しているため、自動判別への依存度は限定的」と要約したが、ユーザーから明確な訂正があった。**「測定結果を入力する→計算で座標が出力される→今どこ（どの区間）にいるのかが表示される→区間に合わせて計算方法を切り替える」という一連の自動化こそが、この測量タブの最も重要な価値である。** 測量者が区間を把握しているとしても、それをアプリが自動判定・表示し、区間ごとに異なる計算式（直線の垂線距離／曲線の半径差）を自動的に切り替えて適用することが本質的な機能であり、「限定的」という表現は不正確だった。今後この判定ロジック（`judgeStraight`/`judgeCurve`/`judgePoint`）に触る際は、これがアプリの中核機能であるという前提で慎重に扱うこと。

**次回このテーマに着手する場合への申し送り:** もし将来、実際にIA>180°級の急曲線プロジェクトで区間誤判定が実際に発生した場合は、その実測点の座標データを使ってどの区間がどう誤判定されるかをまず検証すること（この session で確立した「実データで検証してから直す」という方針を踏襲）。対策を入れる場合も、本数ベースの判定を主軸にするのではなく、あくまで座標判定を主とした上での補助的な安全網（例: 優先度・誤差が僅差の候補が複数ある場合にのみ、想定チェーンエージに近い方を参考情報として警告表示する、等）として設計するのが、ユーザーの意向に沿う。

---

## 52. Excel出力に「区間別計算方法一覧」（凡例）と「現在区間」セルを追加 — どの区間でどの式が使われたかを明示

51.2の議論を受け、ユーザーから「この区間はこの計算方法、という式一覧を作り、『今どこにいるか』が書いてあるセルを参照してどの式を使うかを判定する形にしてほしい」という具体的な実装依頼があった。BP-BC1・BC1-EC1（曲線）・EC1-EPのようなパターンで、曲線が増えても基本の考え方（直線用・曲線用の2種類の式）は同じ、という要望。

**実装（[M_APP_005.html:4412行付近](M_APP_005.html)、`exportSurveyXLSX()`内）:**
1. **「区間別計算方法一覧」（凡例）テーブルを新設**: 各KPの候補一覧ブロックより前に配置。BP→EPの実際のチェーンエージ順（BP-BC1・第1曲線・EC1-BC2・第2曲線・…・EC(N)-EP）で全区間を並べ、各行に「区間名・種別（直線/曲線）・計算方法（日本語での説明文）」を記載する。直線用・曲線用の説明文はそれぞれ1種類の定型文（`STRAIGHT_METHOD_DESC`/`CURVE_METHOD_DESC`）を使い回し、区間が増えても説明文自体は増えない。
2. **各KPの判定ブロックに「現在区間」セルを追加**: 「該当行位置」（`MATCH`で求めた候補行番号）を確定させた直後に、`=INDEX(候補ラベル列, 該当行位置)`で候補一覧の区間名（例:「第2曲線」「EC1-EP」）を取り出して表示する。続く計画X・計画Y・誤差・左右はすべて同じ`該当行位置`（`matchAddr`という1つの変数に集約）を参照してINDEXする形に統一し、「現在区間が決まる→その区間の計算結果を参照する」という流れが見えるようにした（以前は`該当行位置`のアドレスを`xlsxAddr(r4-1,2)`のように毎回相対計算していたが、`matchAddr`変数を直接使い回す形に整理し、行挿入時のズレのリスクも減らした）。
3. **「法線との測定誤差計算結果（一覧）」サマリー表にも「現在区間」列を追加**（名称・役割の次、測定点X/Yの前）。

**検証:** 曲線2本のテストデータで凡例テーブルが`BP-BC1(直線)→第1曲線(曲線)→EC1-BC2(直線)→第2曲線(曲線)→EC2-EP(直線)`の順に正しく並ぶことを確認。KP1の「現在区間」セルの式`=INDEX(A35:A39,B42)`が実際に「第2曲線」という値を返し、アプリ本体の`surveyResults[0].sectionName`（"第2曲線 右"）と一致することを確認。サマリー表の「現在区間」列も同じ値を正しく参照することを確認。生成XMLに`undefined`/`NaN`が無いこと、全パートがXMLとして妥当であることも確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v49`→`v50`に更新済み。

---

## 53. Excel出力を全面的に簡潔化 — 列幅最適化・ラベル簡略化・「区間別計算式一覧」を説明文からパラメータ表に変更・誤差計算を「区分決定→前述の式から選ぶ」構造に再設計

ユーザーから、52章までの出力に対して次の具体的な改善依頼があった。

1. 「注意しきい値[mm]」を「社内規格値」、「NGしきい値[mm]」を「規格値」と表記する。単位がmmの場合は数値の隣のセルに「mm」と表記する（ラベルに埋め込まない）。
2. 列の幅を最適化しておく（文字列がセルに表示しきれない問題への対応）。
3. 「測定結果　角度10進(式)[度]」を単純に「10進数」とする。
4. 全体的に説明的になり過ぎている。セルに表示しきれない文字列が多いので単純明快にする。
5. 「区間別計算方法一覧」（52章で追加した、区間ごとに計算方法を**文章で説明する**表）はやめる。計算方法を説明しても仕方がない。代わりに**区間別の計算式（実際に式が参照する生データ）**を入れておき、測量結果の座標から「今どの区分にいるか」を表示し、その結果を使って前述の式から実際に使う式を選んで答えを出す形にする（これで冗長な式が減る）。

### 53.1 列幅・ラベルの修正

- `xlsxMakeSheet`に`colWidths`プロパティを追加し、`xlsxSetColWidths(sheet, {col: width, ...})`で設定できるようにした。`xlsxSheetXml()`は`<sheetData>`の直前に`<cols>`要素を出力する（OOXMLのスキーマ順序: `cols`は`sheetData`より前でなければならない点に注意）。4シートそれぞれに列幅を設定した（[M_APP_005.html:4064行付近](M_APP_005.html)のヘルパー定義、設定呼び出しはZIP化直前にまとめて配置）。
- 判定基準: 「注意しきい値[mm]＝社内規格値」→A列「社内規格値」・B列に数値・C列に「mm」。「NGしきい値[mm]＝規格値」も同様に分離した。
- 「角度10進(式)[度]」（基線データの交角、測定結果の測定角度の両方）→「10進数」に短縮。

### 53.2 「区間別計算方法一覧」→「区間別計算式一覧」への変更（説明文からパラメータ表へ）

52章で作った表は区間ごとに「計算方法」列に長い日本語の説明文（垂線距離の説明・角度判定の説明）を入れていたが、ユーザーの指摘どおりこれは実用的でない（読んでも計算の中身は分からず、セルにも収まらない）。代わりに、各区間が実際に参照する**生データそのもの**（すべて式）を並べる表に変更した。

- 列: 区間｜種別｜P1X｜P1Y｜P2X｜P2Y｜R
- 直線区間: P1=始点、P2=終点（Rは空欄）
- 曲線区間: P1=中心(RO)、P2=基準点(BC)、R=半径
- すべてのセルは実際のクロスシート参照式（例: `='各座標データ'!B3`）であり、文章による説明は一切含まない。

### 53.3 誤差計算ロジックの再設計（「区分を決定→前述の式から選ぶ」構造、冗長な式の削減）

52章までの実装は、KPごとに全区間の候補行を作り、各候補行で**計画X・計画Y・誤差・左右をすべて計算**してから、優先度・誤差で最良の1行を`INDEX`で選ぶ、という構造だった。これだと使われない候補（最終的に選ばれない区間）の計画X/Yまで無駄に計算しており、候補1行あたり14列（座標計算の補助列を含む）にもなっていた。

**新しい構造:**
1. **候補一覧（KPごと、区間の数だけの行）**: 列を「区間｜種別｜優先度｜誤差mm｜左右｜スコア」の6列に削減。計画X/Yはここでは計算しない（優先度・誤差の判定だけに必要な最小限の式に絞った）。誤差mmも、直線の場合は元々`ux*ey-uy*ex`という単位ベクトルと足（垂線の交点）を経由する式だったが、外積の公式`(B-A)×(P-A)`を距離`|B-A|`で正規化するだけで足を経由せず直接誤差が求まることに気づき、その形に簡略化した（`ABS((Bx-Ax)*(Py-Ay)-(By-Ay)*(Px-Ax))/SQRT((Bx-Ax)^2+(By-Ay)^2)*1000`）。曲線側も同様に、優先度・誤差の判定に足（計画X/Y）は不要（半径との差だけで誤差が求まる）ことを利用した。
2. **区分の決定**: `MIN(スコア範囲)`→`MATCH`→`INDEX`で「現在区間」「種別」を確定させる（52章と同じ考え方）。
3. **P1/P2/Rの取得**: 確定した区分（`該当行位置`）を使い、53.2の「区間別計算式一覧」から`INDEX`でその区間のP1X/P1Y/P2X/P2Y/Rを取得する。
4. **計画X/計画Yの計算（ここで初めて1回だけ計算する）**: 取得したP1/P2/Rと、種別（`IF(種別="直線",...,...)`）を使って、直線用の垂線の足の式・曲線用の半径方向の足の式のどちらかを適用する。**この「種別を見て式を選ぶ」というIF分岐が、ユーザーの言う「前述の式から選んで答えを出す」を文字通り実装したもの。**
5. 誤差(mm)・左右は候補一覧で既に安く計算済みなので、そちらから`INDEX`で取り出すだけ（再計算しない）。

この再設計により、候補1行あたりの列数は14列→6列に、KPごとの「判定」ブロックで新たに計算する行は現在区間・種別・P1X・P1Y・P2X・P2Y・R・計画X・計画Y・誤差・左右・判定の12行（うち計画X/Yの2つだけが実際に新規計算、他はP1〜Rの取得や誤差/左右のINDEX参照）に整理された。

### 53.4 検証中に発見・修正したバグ: 直線区間の「左右」判定が反転していた

53.3の簡略化で、直線区間の誤差式を`(P-A)×(B-A)`という外積で書いたところ、符号がアプリ本体の`judgeStraight()`（`ux*ey-uy*ex`、実質`(B-A)×(P-foot)`と同じ向き）と**逆**になり、「左右」の判定だけが反転するバグを作り込んだ（誤差の絶対値・計画X/Y・優先度は`ABS()`を使うため影響を受けず、正しい値のまま一致していたので発見しづらかった）。

**発見方法:** 曲線半径を意図的に小さくして「第1直線」が実在するテストデータを作り（元のテストデータは曲線のTLが直線長を超える不正な形状で、常に曲線側と判定されてしまい直線側のテストができていなかった）、複数の角度でKPを測定→アプリ本体の`surveyResults`と輸出したExcelのキャッシュ値を突き合わせたところ、X/Y/計画X/Y/誤差はすべて一致するのに「左右」だけ全KPで逆になっていることに気づいた。

**原因・修正:** アプリ本体は`signed = ux*ey - uy*ex`（`(ux,uy)`=区間の単位方向ベクトル、`(ex,ey)`=測点から垂線の足を引いたベクトル）で、区間内側にある場合はこれを展開すると`(1/len)*[(B-A)×(P-A)]`になる。今回の簡略化式は`(P-A)×(B-A)`と書いてしまい、外積の順序が逆（符号が逆）になっていた。`(sec.bXAddr-sec.aXAddr)*(kp.yAddr-sec.aYAddr)-(sec.bYAddr-sec.aYAddr)*(kp.xAddr-sec.aXAddr)`（＝`(B-A)×(P-A)`の順序）に修正し、JS側のキャッシュ値計算・Excel式テキストの両方を直した（[M_APP_005.html:4493〜4506行付近と4554〜4562行付近](M_APP_005.html)）。

**検証:** 4方向（0°/90°/135°/270°）のKPで再テストし、アプリ本体の左右判定（左/右/右（外側）/左）とExcel出力の左右（左/右/右/左）が完全一致することを確認。誤差・計画X/Y・区分もすべて浮動小数点精度で一致することを確認。ZIP構造・全XMLパートの妥当性・`undefined`/`NaN`の不在も確認。`read_console_messages`でJSエラー無しも確認。`service-worker.js`の`CACHE_NAME`を`v50`→`v51`に更新済み。

**53.4の教訓:** 外積・法線ベクトルなど「符号が意味を持つ」計算を簡略化・式変形するときは、`ABS()`を使う値（誤差の絶対値など）だけで検証すると符号バグを見逃す。**符号がそのまま結果に出る値（左右・内外側など）を含めて、複数方向・複数パターンのテストデータで検証すること。** 今回はたまたま最初のテストデータの曲線半径が過大でTLが直線長を超える不正な形状になっており、直線区間が一度も現れず、この符号バグに気づくのが遅れた。**テストデータを作る際は、意図した区間（直線・曲線それぞれ）が実際に候補として現れているか（`surveyResults[].sectionName`等で）を先に確認してから、値の突き合わせに進むこと。**

---

## 54. Excel出力の測定誤差計算式を「区分（現在区間）をキーにしたVLOOKUP」に統一 — INDEX/MATCH位置参照から脱却

M_APP_027でのセッション再開後、ユーザーから実際にダウンロードしたExcelファイルの画面共有があり、「誤差(mm)列が複数KPで0のまま、区分も全KP同じ第1曲線になっている。曲線があるのに誤差が変化しないのはおかしい、根本的な間違いがあるはず」という指摘があった。調査のため、53章までの式（`judgeStraight`/`judgeCurve`/`judgePoint`本体とExcel出力側の再実装）を1行ずつ突き合わせたところ、符号・ATAN2引数順（50章の規約通り）・優先度判定・誤差式のいずれも数学的に一致しており、50.5節に既知の限界として明記済みのdelta≡0境界（左曲線限定）以外に新規の食い違いは見つからなかった。ユーザーに確認したところ「まだ式を作っていないから0が表示されているだけ」との回答で、実データ固有の問題の切り分けよりも、**そもそもの式の作り方（INDEX＋MATCHによる「該当行位置」という行番号ベースの間接参照）が分かりにくく、これを区分（区間名）をキーにしたVLOOKUP参照に作り替えてほしい**という要望だった。ユーザー案：`=VLOOKUP(D22,$A$17:$B$19,2,TRUE)`のように、区分の値で計算式一覧（区間別計算式一覧）を引く形。

**実装（[M_APP_005.html](M_APP_005.html)の`exportSurveyXLSX()`内）:**
- 「区間別計算式一覧」（区間名・種別・P1X・P1Y・P2X・P2Y・R、1列目が区間名でユニーク）の全体を`legendRange`（A列〜G列）としてまとめ、種別・P1X・P1Y・P2X・P2Y・Rの5項目はすべて`VLOOKUP(現在区間セル, legendRange, 列番号, FALSE)`で引く形に統一した。ユーザー例は`TRUE`（近似一致）だったが、区分はテキストの完全一致で引く必要があるため`FALSE`（完全一致）を採用した（`TRUE`はソート済みの数値範囲を近似で引く用途向けで、テキストキーの完全一致には不向き・誤動作の原因になり得るため）。
- 各KPの「候補一覧」（区間・種別・優先度・誤差mm・左右・スコア、こちらも1列目が区間名でユニーク）も同様に`candRange`としてまとめ、誤差(mm)・左右も`VLOOKUP(現在区間セル, candRange, 列番号, FALSE)`に統一した。
- 「該当行位置」（`MATCH`の結果を別セルに出していた行）は不要になったため削除し、`MATCH`は「現在区間（区分）」セル自身の式（`INDEX(区間名範囲,MATCH(最小スコア,スコア範囲,0))`）に内包した。区分の特定に使う`MATCH`はこの1箇所のみに集約され、以降の全参照は区分（テキスト）をキーにしたVLOOKUPで統一される。
- 不要になった個別レンジ変数（`legendP1XRange`等5個、`errRange`・`sideRange`・`typeRange`）は削除。

**検証:** ブラウザ上で`xlsxFormula`/`xlsxFormulaStr`を一時的にフックし、①例外なく`exportSurveyXLSX()`が完走すること、②生成される式が`VLOOKUP(B34,A29:F31,4,FALSE)`のような妥当な形であること、③曲線を含むテスト線形・測点データで、キャッシュ値（誤差(mm)＝4443.958594658461／4077.576977977458等）がアプリ本体`calcSurvey()`の`surveyResults[].errorMm`と小数点以下まで完全一致することを確認した。区分（現在区間）も両者で「第1曲線」に一致。この検証方法自体は50.3節・53.4節と同じ「アプリ本体の計算結果との突き合わせ」だが、今回は併せて生成された式のテキスト自体も目視確認し、51章の教訓（式のテキストが正しいかはキャッシュ値の一致だけでは検証できない）を踏まえた。

`service-worker.js`の`CACHE_NAME`を`v51`→`v52`に更新済み。

**54章の教訓:** 「誤差が0で不自然」という報告に対し、既存ロジックとの数式突き合わせだけで「バグは無い」と結論づけず、ユーザー自身に意図を確認したところ、実際の原因は数値バグではなく「式の参照方法（行位置ベース）が分かりにくい」という設計上の指摘だった。数値が合っているかどうかの検証と、その式が人にとって読みやすい・監査しやすい形になっているかは別の観点であり、後者はユーザーからの明示的な指摘なしには気づきにくい。**Excel出力のような「人間が最終的に開いて検算する」成果物では、数値の正しさだけでなく式の参照方法（INDEX/MATCHの行位置か、VLOOKUPのキー参照か）も含めてユーザーの意図を確認する価値がある。**

---

## 55. 一次資料（秦野市・坂本建設室川第９現場の推進検討書）との突き合わせ、区間別土質の実装、calcTerzaghi()の2つのバグ修正

ユーザーから実在の設計図書（`C:\Users\mande\Dropbox\Documents\01_現場別書類\08_秦野市\令和01年度\坂本建設室川第９（第２工区）\04_設計委託\設計図書(分割)\第３章.pdf`、刃口推進φ800mm、上流・下流各スパン）を提示され、推力計算・支圧壁計算の内容を本アプリと突き合わせるよう依頼があった（`pdfplumber`でテキスト抽出、大きな表は`extract_text(layout=True)`で列崩れを確認しながら精読）。

### 55.1 推力計算の突き合わせ結果

Fo/foの式自体（`Fo=10.0×1.32・π・Bs・N'`、`fo=α・π・Bc・ta+W・μ'`、`ta=σ・μ'+C'`、`σ=β'・q`）は記号レベルで完全一致していた。一方、この現場はLm1層（N'=1.0・φ=0.0°・C'=10、粘性土）とLm2-u層（N'=3.0・φ=18.0°・C'=0、固結土）という2つの地層を1本の推進区間内で通過しており、区間ごとに使う土質パラメータが切り替わっていた。これに対し本アプリの土質条件タブは現場全体で1組のγ/φ/C/N/N'/C'しか持てず、この種の現場を再現できないことが判明した。

### 55.2 区間別土質（任意）を実装

ユーザーから「何メートルから何メートルまでこの土質、といったものを採用した方がいい。実装してください」と明示的な指示があり、実装した。

**データモデル・UI（[M_APP_005.html](M_APP_005.html)）:**
- 新しいグローバル配列`soilZones`（各要素`{id, fromM, toM, gamma, phi, phiPipe, C, N, Nprime, adh}`）と`addSoilZone()`/`removeSoilZone()`/`renderSoilZones()`/`onSoilZoneChange()`を新設。UIは「🌱 土質条件」カード内、共通のγ/φ/C/N等の下に「区間別土質（任意・地層が変わる場合）」セクションとして追加し、`curve-block`スタイル（曲線区間リストと同じ見た目）でBPからの距離範囲ごとに1枚ずつカードを積む形にした。
- `resolveSoilAt(distFromBP)`: BPからの距離[m]を渡すと、該当する区間別土質（重複時は先に登録した方を優先）があればその値を、無ければ土質条件タブの共通値を返す。区間別土質が1つも無ければ常に共通値を返すため、既存プロジェクト（区間別土質未使用）の挙動は完全に維持される。
- `getProjectData()`/`setProjectData()`に`soil.zones`を追加（保存・復元対応、旧形式のJSONは`zones`が無いだけで問題なく読み込める）。

**計算への組み込み（`calculate()`内）:**
- `foForSpec(spec, hVal, distFromBP)`の第3引数を「区間の実長」から「BPからの距離」に変更し、内部で`resolveSoilAt()`を呼んでその位置の土質でテルツァーギ土圧・Fo/foを計算するようにした。
- EP→SP方向に区間を積算する既存ループに`bpCursor`（BPからの距離カーソル、到達側立坑手前`total-arrivalDist`から発進側へ向けて各区間長だけ減算していく）を追加し、各区間（最終直線・各曲線・中間直線・第1直線）の**中点**のBP距離で`resolveSoilAt()`を呼ぶようにした。先端抵抗Fo・foは「今まさに掘進している位置」＝到達側立坑手前の位置の土質を使う。
- 曲線の増幅係数K（`calcCurveK()`のφ引数）も、その曲線位置の区間別土質のφを使うよう修正（従来は共通φ固定だった）。
- 外圧耐荷力照査（`extResults`）も、区間ごとに区間別土質から求めたq_totalを使うよう修正（従来は共通のq_total固定だった）。

**既知の制約:** 1本の曲線区間が土質境界をまたぐ場合、現状は曲線全体をその中点が属する区間別土質1つに割り当てる（一次資料の`曲線CL1a`/`曲線CL1b`のように、同じ物理的な曲線を土質境界で分割して別々の抵抗力を計算する、という粒度には対応していない）。実務でこれが問題になる場合は、その曲線を土質境界の位置でIP（曲線区間）ごと2本に分割して登録する運用で回避できる。

**検証:** 区間別土質が空の状態でデフォルト計算が例外なく完走し、既存の挙動（区間別土質導入前と同じF_total）を維持することを確認。区間別土質2件（Lm1/Lm2-u相当）を投入し、曲線区間・直線区間それぞれで区間の中点位置に応じて異なるfo・N'・φが使われること、`resolveSoilAt()`が正しい区間を返すことを確認。

### 55.3 一次資料との突き合わせで発覚したcalcTerzaghi()の2つのバグを修正

55.2の検証中、一次資料の土被り部（φ'=0.0°・C=65kN/m²・γ=15kN/m³・H=14.88m）を投入したところ、本アプリの`q`（緩み土圧）が大きく負の値になり、F_totalが負数という物理的にあり得ない結果になった。一次資料ではこの条件で`ｑ kN/m2 0.00`と明記されており（この値がσ=β'・qを経由してfo=0.842kN・15.080kNまで正しく伝播していることも式を辿って確認した）、既存の`calcTerzaghi()`を検証したところ2つのバグが見つかった。

1. **φ≈0（`kmu<1e-9`）の特殊分岐で、粘着力Cによる緩み軽減効果が丸ごと抜け落ちていた。** 旧式は`q=gamma×H`としていたが、正しくは一般分岐と同じ`q=(gamma-2C/Be)×Ce`（`Ce`はkmu→0の極限でHに収束する除去可能特異点だが、q自体の式にはkmu=0での特異点は無い）。この現場の条件（γ=15,C=65,H=14.88,D=0.96）で検算すると、旧式は`q=223.2`、正しい式は`q=(15-2×65/2.559)×14.88=-532.7`と大きく異なる。
2. **qが物理的にあり得ない負値のまま使われていた。** 粘着力の緩み軽減効果が自重を上回る場合、土がアーチを組んで支圧壁側へ荷重を伝えなくなるだけで、負の圧力（管を持ち上げる方向の力）にはならない。一次資料でも高粘着力の地盤でq=0.00と明記されており、負値のままにはしていない。`q = Math.max(0, q)`を追加した。

この2点を修正した結果、上記の条件で`calcTerzaghi(15,0,65,14.88,0.96).q`が`0`（一次資料の0.00と一致）になることを確認した。

**未解決のまま報告のみに留めた別の疑義（コード変更なし）:** 55.2の区間別土質を使って一次資料の上流スパン（φ800mm、Lm1/Lm2-u 2層、F_total=1,275.83kN）を再現しようとしたところ、本アプリの計算値がそれより大きく乖離した。原因を追ったところ、一次資料の刃口推進σ式は`σ=β'・q`（テルツァーギの緩み土圧qのみ、活荷重を含まない）と読めるのに対し、本アプリの`calcFoFo()`のblade分岐は`sigma = beta * q_total`（q_total=q+活荷重p_live）を使っており、活荷重分だけ本アプリのσ・ta・foが大きくなっていた（この現場の条件でfo=0.840 vs 1.360、約62%の差）。ただし24章では別現場（館町雨水φ1350）で`q_total`を使ったままF_total=2205.75kNとPDF値が0.03%差で一致しており、その現場ではこの差が顕在化しなかった可能性がある（H・φ・Cの組合せ次第で活荷重の寄与度が変わるため）。また、一次資料の「土被り部土の内部摩擦角φ'」（テルツァーギqの計算用）と「管路部土の内部摩擦角φ」（μ'=tan(φ/2)の計算用）は同じ位置でも異なる値を取り得る（この現場のLm2-u区間ではφ'=0°・φ=18°）のに対し、本アプリ（区間別土質を含む）は1つのφしか持たず両方の用途に使っている、という設計上の簡略化もある。**この2点（q vs q_totalの選択、φ'とφの分離）はどちらも実装変更を伴う判断が必要なため、ユーザーの確認なしにコードは変更していない。** 次回このテーマに着手する場合は、まず館町雨水の実データ（24章）で`q`のみに変更した場合にF_total=2205.75kNとの一致が崩れないか検算してから判断すること。

`service-worker.js`の`CACHE_NAME`を`v52`→`v53`に更新済み。

### 55.4 支圧壁計算（3.3.5節）の突き合わせ結果 — 現場打ちは全く別の照査体系だった

一次資料の支圧壁（円形ライナープレート立坑、現場打ち・バックロック不使用）は、本アプリの①〜⑤とは異なる照査体系だった。

**一次資料にあって本アプリに無いもの:**
1. **支圧壁設置深さの計算**（総推力の作用位置と地山反力の合力作用位置が一致する深さhを、二次方程式`a・H1²-b・H1+c=0`を解いて求め、`|設計h-計算h|≤H/6`で妥当性確認する手順）。本アプリはHw・hcをユーザー入力のまま使うだけで、この最適設置深さの算出は行っていない。
2. **支圧壁自体の曲げ照査（現場打ち版）**（管底より下の根入れ部分を片持ち梁とみなし、受働土圧の台形分布による曲げモーメントを、コンクリートの引張強度`f_tk=0.23f'ck^(2/3)`に対する抵抗モーメントと比較する照査）。本アプリで同種の「壁自体の材料強度に対する曲げ照査」は④（バックロック使用時のみ）だけで、**現場打ち（`backrockUse=false`）にはこの照査が無い**。

**本アプリにあって一次資料に無いもの:** ①基礎反力照査・②偏心曲げ照査（支圧壁を剛体とみなした背面地盤への接地圧チェック）。一次資料にはこのタイプの照査は登場しない。

**一致している部分:** ③受働土圧安定照査は式の骨格（`R=α・W・(γH²Kp/2+2CH√Kp+γhHKp)≥F`、`Kp=tan²(45°+φ/2)`）が一致。一次資料の「h」は上記(2)の最適深さ計算から出た値だが、本アプリは`Hmax-(Hw-hc)`という簡易式を使っている。

ユーザーから「バックロックではなく現場打ちコンクリートの計算で根入れを見ているということですね」との確認があり、その通りである旨を回答した。**現場打ち支圧壁の④相当照査（片持ち梁・コンクリート引張強度照査）と(2)設置深さ最適化は、本アプリには未実装のまま**であり、次回このテーマに着手する場合は本節を参照すること。

### 55.5 館町雨水φ1350での試算（一次資料のPDF方式・③受働土圧のみ、現場打ちT=0.6m固定・φ5.0mライナープレート内で幅最大化）

ユーザーから「今回、1350館町の推進の条件でPDFの計算方法を採用したとして受働土圧OKになるためには、どんな寸法にするのがベストか。幅は支圧壁厚さ600mmとして呼径5mのライナープレートで取れる幅以上には広げられない」という試算依頼があった。

**入力値:** F=2205.75kN（24章で検証済みの館町雨水F_total）、φ=5°・Hmax=4.49m（43.1節で確認済み）、γ=18kN/m³（未確認のため仮定値、明記のうえ試算）、α=2.5（43.2節の既定値）、hc=1.0m（46〜47章の同規模試算で使われた値を踏襲）。粘着力Cは一次資料間で食い違いがあり（`推力計算1350.pdf`はC=3、`支圧壁の計算.xlsx`はC=10、47.7節末尾で既に報告済みの矛盾）、両方で試算した。

**試算方法:** `calcBacklock()`をブラウザ上で直接叩き、円形立坑φ5.0m・`backrockUse=false`（現場打ち）・`wallThickness=0.6`固定の状態でHwを振った（`wallThickness`固定により`computeWallBeff()`の弦幅計算式`Beff=2√(t(2R_eff-t))`からBeff=3.231mに自動的に頭打ちになる。R_eff=(5000-50)/2=2.475mはライナープレート内側径の既存の換算式）。

**結果:**
| C | ③OKになる最小Hw | Beff | Ep（Hwでの受働土圧抵抗力） |
|---|---|---|---|
| 3 kN/m²（`推力計算1350.pdf`） | 3.0m | 3.23m | 2231kN（F=2205.8kN比+1.2%） |
| 10 kN/m²（`支圧壁の計算.xlsx`） | 2.5m | 3.23m | 2276kN（F=2205.8kN比+3.2%） |

**重要な副作用:** どちらのHwでも、③をOKにした途端に**⑤推進作業スペース照査がNGになる**（残りスペース4.35m＜必要スペース4.89m、約0.54m不足）。⑤はhCenter+R_eff（立坑径・支圧壁厚さのみで決まる、Hwを変えても変化しない幾何量）で決まるため、Hwをいくら調整しても⑤単独では解消できないことも確認した。つまり**φ5.0mライナープレート・T=600mm固定という制約下では、③（受働土圧）と⑤（推進作業スペース）を同時にOKにすることはできない**——立坑径を広げるか、支圧壁厚さを薄くする（Beffを縮小しBw側は逆に不利になる）か、必要スペース側（残管長・ストラット構成等）を見直すかの検討が必要になる。ユーザーにはこの副作用込みで報告済み。この試算はコード変更を伴わない一時的な検算であり、アプリの入力値・保存データには反映していない。

---

## 56. 秦野市室川第９の別スパン2件（φ1100mm、中押しあり／無し）を追加確認、③受働土圧NGでも無対策で施工成功したという重要な実地情報

55章に続き、同じ秦野市室川第９（平成23年度）の別の2つの検討書（`1SP_刃口φ１１００推力計算－中押.pdf`＝中押しあり、`2SP_刃口φ１１００推力計算.pdf`＝中押し無し）をユーザーから提示され、精査した。

### 56.1 推力計算・外圧耐荷力

Fo/foの式・Faは55.1節までの知見と完全に整合（同じ下水道協会式）。新規の発見は無し。

### 56.2 中押しジャッキの段数計算（本アプリ未実装の機能、実例で再確認）

中押しあり版（L=157.2365m）では、`f0'=(F-F0)/L`という区間平均等価摩擦抵抗を使い、`La=Fa/f0'≈148m`（中押し無しで管が耐えられる最大距離）・`Lan1`・`Lan2`から中押し段数M=1段と算出していた。中押し無し版（L=104.529m）はL<Laのため中押し計算のセクション自体が存在せず、F_total=2,563.864kN≦Fa=4,374.5kNで完結していた。**「中押しの要否はLa=Fa/f0'という一本の閾値で決まる」という関係が、実際に2つの別スパン（あり／無し）の対比によって裏付けられた。** この中押し段数計算は本アプリに引き続き未実装（式・変数の詳細は本節および上記の通り。55.2節は区間別土質の実装についての節であり誤参照だったため訂正）。

### 56.3 支圧壁の受働土圧照査 — 確認できた3件すべてでNG

このスパン（中押し無し版）でも円弧ベースの受働土圧照査を実施しており、`F=2,563.864kN > R=1,626.80kN`でNG（必要反力の約6割）。これで55章の館町雨水（φ5°）・秦野市R01年度と合わせ、**φ=5°前後の低い内部摩擦角の現場で、確認できた一次資料3件すべてが受働土圧照査でNGだった**ことになる。

### 56.4 【重要】ユーザーからの実地情報 — 地盤改良を行わずに無対策で施工が成功している

ユーザーから、この秦野市室川第９現場について「土止め背面の地盤改良などは施工せずそのままだった。施工自体は問題なかった」との報告があった。つまり、③受働土圧照査が計算上NG（必要反力の6〜7割程度しか出ない）だったにもかかわらず、**実際には地盤改良等の追加対策を一切行わずに無事施工が完了している。**

**教訓・今後への申し送り:** これは24.4節末尾で触れた「JPTA式のFo/foが実際の達成推進力より保守的な場合がある」という話と対になる、支圧壁側での同種の実地証拠である。今回確認できた3件すべてで③がNGになっている一方、少なくともこの秦野市室川第９現場では無対策で施工が成功しているという事実は、**この受働土圧照査式（矩形ベース・円弧ベースいずれも）が、少なくともこの種の低φ地盤に対しては相当に保守的である可能性を示唆している。** ただし、これは「NGは無視してよい」という結論を意味するものではない——α係数（1.5〜2.5、43.2節）は元々3次元効果を見込む保守的な割増し係数であり、それでも計算上NGになったこと自体は事実として残るし、他の現場・他の地盤で同じ余裕があるとは限らない（地盤の実際のせん断強度が設計値より高かった、施工中に一時的な土のアーチ作用で持ちこたえた、等の可能性もある）。**次回このテーマ（受働土圧照査のNG対応、あるいは本アプリの③照査の妥当性）に着手する際は、この実地証拠（NGでも無対策で施工成功）を踏まえつつ、「計算上のNGを額面通りに危険とみなしてよいか」「どの程度の余裕（何%不足まで）なら実務上許容されているか」を、可能であればユーザーに追加でヒアリングしてから判断すること。** 現時点ではコード（判定ロジック・警告文言）は変更していない。

---

## 57. さらに別現場（水無第6、平成21年度、φ1800mm）の一次資料3件を確認 — BC点管種検討の式に未実装の補正係数を発見

ユーザーから水無第6（森下総建、進和技術、平成21年度）の3資料が提示された：`刃口φ１２００目地開口の検討.pdf`（実際の中身はφ1800mm）、`刃口φ１８００推力計算－曲線を考慮.pdf`、`刃口φ１８００推力計算－元設計の検証用.pdf`（同条件だが曲線を無視した直線換算版、比較用）。

### 57.1 目地開口（継手）チェック — 完全一致を確認

`JOINT_SPEC`定数（JA:許容30/40mm、JB:40/50mm、JC:60/70mm）が一次資料の継手性能表と数値レベルで完全一致した。S1（目地開口量）の核となる計算`D×L/(R-D/2)×1000`も、資料のR=200m・L=2.43m・D=2.12m（φ1800外径）の条件でS1≈25.8mm（一次資料は26mm、丸め誤差の範囲）と一致。`calcS1()`はこれで実データ照合済みとなった。

### 57.2 推力計算・外圧耐荷力 — 従来通り一致、曲線考慮版と直線換算版の比較で妥当性を再確認

Fo/fo/外圧耐荷力（Mr/M）は従来通り式が一致。「曲線を考慮」版（F_total=7,695.703kN）と「元設計の検証用（曲線無視・直線換算）」版（F_total=7,689.316kN）を比較すると、R=200mという緩い曲線1本では増幅の影響が0.1%未満とごくわずかであることが分かり、一次資料側もこの2パターンを作って（おそらく）曲線考慮の影響が軽微であることを確認する目的で両方計算していたと推測される。

### 57.3 【新発見】BC点管種検討 — 本アプリの`calcFaBC()`に無い補正が一次資料にあった

「曲線を考慮」版のp.8に、曲線始点（BC点）での管の許容耐力照査があり、本アプリの`calcFaBC()`・`FBC_arr`（BC点管種検討）に相当する箇所だった。一次資料の式を突き合わせたところ、**2つの点で本アプリの式より詳細だった。**

1. **有効長への補正係数（t/D・l/Dの回帰式）が本アプリに無い。** 一次資料は`t/D`（管厚/外径比）と`l/D`（管長/外径比）から`coefficient = -13.917×Rt - 0.579×RL + 10.506×Rt×RL + 2.033`という回帰式でRi係数を求め、`a = l / coefficient`（この現場ではa=1.906m、生の管長2.43mより短い）という「補正後の有効長」を使って計算していた。本アプリの`calcFaBC(lp, r, qa, alpha)`は生の`lp`（管長そのもの）をそのまま使っており、この補正が抜けている。
2. **式の係数・qaの求め方も異なる。** 一次資料は`Fa(BC) = π×a×r×qa / (sinα×1.5)`（π・1.5という安全率が明示的）で、qa自体も`(0.318×P+0.239×W)×r/(0.239×r²)`という動的な式（外圧耐荷力照査のMr式と同じ骨格）で毎回計算していた。本アプリの`calcFaBC()`は`lp×r×qa/(2×sin(alpha))`で、qaは`QA_TABLE`という静的な早見表（管種・呼び径ごとの固定値）から引いている。静的な早見表自体の出典・妥当性は今回未確認。

**この件はコード変更は行っていない。** 少なくとも今回の実データでは本アプリ・一次資料どちらも最終的にOK判定で一致した（11,092.90kN・11,093kN側の許容耐力に対し、実際の力7,695.70kN・7,689.32kNともに十分な余裕でOK）ため実害は出ていないが、余裕が少ない現場では判定が割れる可能性がある。次回このテーマに着手する場合は、まずQA_TABLEの出典（どの管種便覧のどの表か）を特定し、一次資料の動的計算式とどちらが実務上正しいとされているかを確認すること。

---

## 58. 入江土木（西東京、φ1000mm）の支圧壁4資料、および渋沢2工区・館町のFJリング資料を調査

### 58.1 支圧壁4資料（入江土木・平成28年度・φ1000mm）

`(改)支圧壁φ1000ＮＧ.pdf`は一般的な現場打ち支圧壁の検討で、これまでとは別のさらに3つ目のバリエーションを含んでいた。

1. **支圧壁の高さ配分（H0=H1+H2）**: 反力を上下に均等分割（R1=R2=F/2）し、それぞれが独立に受働土圧式を満たす高さH1・H2を個別に求め、実際に採用する高さH0（管外径の2倍程度、通常は立坑内のりいっぱい）に対する比率でH1・H2を配分する、という設置位置の考え方。55.5節の二次方程式による最適深さ計算ともBC点補正ロジックとも別の、3つ目のバリエーション。
2. **地盤支持力（③相当）**: `R=α・B[γH²Kp/2+2CH√Kp+γhHKp]≥F`で、今回はOK（R=5107.89kN>F=1788.00kN）——これまで確認した4件（館町・秦野市R01・秦野市S23×2件）はすべてNGだったので、初めてこの式がOKになる実例を確認できた。
3. **支圧壁の厚さ（④相当、二点支持オーバーハング梁モデル）**: ジャッキ2箇所を支点とする両端張り出し梁として、等分布荷重qのもとMA・MB（ジャッキ作用点）・MC（中央部）を計算し、コンクリート引張強度`ftk=0.23f'ck^(2/3)`に対する抵抗モーメント`Mr=0.28bT²ftk`と比較——**この式は本アプリの④（バックロック使用時専用のプレキャスト曲げ照査）のRa/Mc/Ma/Mbの式と完全に同一の力学モデル**（2支点オーバーハング梁）だった。ここでは無筋コンクリートの現場打ち壁に適用され、結果はNG（Mr=157.68kN・m<Mmax=247.88kN・m、「再検討」の朱書き）。

`ﾊﾞｯｸﾛｯｸの検討.pdf`は同じ現場で、上記NGを受けてプレキャスト支圧壁バックロック（BR-SST形、推進管呼び径1000mm）に切り替えた再検討で、複鉄筋長方形梁としての抵抗モーメント算出（k・x・Lc・j・v・Mrc・Mrs）が**本アプリの`calcBacklock()`④の実装（変数名・式の形まで）と完全一致**した。結果はOK（M=713.6kN・m>Mmax=131.8kN・m）。

**この2資料の対比から実務の流れが裏付けられた:** 現場打ち（無筋コンクリート）の壁厚照査がNG→プレキャストバックロックへ切替→OK、という流れが実データで確認でき、本アプリの既存メッセージ（「現場打ちでNGの場合、鉄筋補強より先にバックロックへの変更を検討するのが実務的」）が実際の設計現場の判断と一致していることが分かった。

`支圧壁バックロック検討.pdf`（φ1000mm・W2000×T500バックロック・立坑φ6000mm）と`ﾊﾞｯｸﾛｯｸ検討書.pdf`（同現場、11ページ、大半が図面でテキスト抽出できず）は主に施工図で、新たな数値情報は無し。

コードの変更は行っていない。55章で確認した「④相当の式は現場打ちには実装されていない」という結論を補強する実例が増えた。

### 58.2 クッション材（コルク／FJリング）の下調べ

ユーザーからの依頼で、コルクとFJリングの特性を調べた。ユーザーが追加で共有した実資料（`(R7-0804)FJリング組数計算表.pdf`＝館町雨水φ1350、`FJ選定.pdf`ほか3件＝秦野市渋沢2工区RM840）とWeb検索の両方から、**FJリングについてはかなり具体的な情報が得られたが、コルクの物性データは見つかっていない。**

**FJリングについて分かったこと:**
- 積水化成品工業製、**発泡ポリスチレン（EPS）製**の推進力伝達材。管径ごとの規格品（FJ-250〜FJ-3000、内径・外径・巾・厚さ・分割枚数がサイズ表で規定）。
- 材質の特徴: 圧縮されると気泡が一つ一つ潰れていくため、**ひずみが増加しても伝達応力の変化が少ない**（応力一定のプラトー領域を持つ、典型的な発泡材料の圧縮特性）。
- **発泡倍率（密度）によって応力-ひずみ特性が8種類**あり（実資料では1.7/2/2.5/3/3.5/4/4.5/5倍を確認）、倍率が大きいほど柔らかく潰れやすい。現場条件（曲線半径・推進力・目地の開き量）に応じて、専用の選定ソフトが**目地1箇所ごとに**「上下配置・左右配置それぞれの角度範囲（90°/120°/180°等）×厚さ（10mm単位の積層）×発泡倍率」の最適な組み合わせをシミュレーションし、圧縮応力が管の許容応力を超えないことを目地円周上の多数の分割点（実資料では94分割）で確認していた。
- BC点（曲線開始点、最も厳しい）ほど厚く・広い角度範囲の配置になり、直線区間ではFJリング自体を使わない（継手のみ）ケースもあった。
- 数量計算は「360°・厚さ10mmで1組」を基準単位とし、実際の配置（角度×厚さ）をこの基準単位に換算して組数を求める。

**本アプリとの比較でいえること:** 本アプリの「クッション材厚S4」「つぶれ代」は、現場全体（または区間別仕様を使えば区間ごと）で**一律の値**を入力する設計になっているが、実際のFJリング選定は**目地1箇所ごとに角度・厚さ・発泡倍率が個別に最適化**されており、粒度が全く異なる。目地開口量S1の計算（`calcS1()`）自体は変える必要は無いが、「その開口量に対して実際に必要なFJリング仕様・その圧縮応力が許容値内に収まるか」という照査は本アプリに存在しない。

**コルクについては未確認。** Web検索でもFJリングの情報しか得られず、コルクの許容応力度・厚さ規格等の一次資料は見つかっていない。ユーザーの手元に他のカタログ・検討資料があれば、それを共有してもらうのが最速。

### 58.3 クッション材選定の全体像がさらに明確になった（水無第6・渋沢2工区の一次資料）

ユーザーから追加で2件の資料（`クッション材の検討（秦野市）.pdf`＝日本ゼニスパイプ社、水無第6・φ1800、`クッション材計算.pdf`＝渋沢2工区第四分区第一号幹線・RM840）が提示され、クッション材選定の設計フローが式レベルで完全に判明した。

**クッション材選定は「2003年日推協提案ベース」という標準式に基づく、55.5〜57節で確認したBC点許容耐力照査と同一の計算チェーン**であることが確定した:

```
η（仮想影響範囲係数）＝ -13.917×(t/D) - 0.579×(Lp/D) + 10.506×(t/D)×(Lp/D) + 2.033
Ma（保証モーメント）＝ 0.318×P×r + 0.239×W×r　　　　　※外圧耐荷力照査のMrと同一式
qa（保証等分布耐荷力）＝ Ma / (0.239×r²)
Rg（許容地盤反力）＝ √2×(Lp/η)×r×qa　　　　　　　　　※Lp/ηが「有効長a」に相当
Fa（曲線部側圧許容推力）＝ Rg / (1.5×sinα)
```

つまり56〜57節で見つけた「有効長補正（a=Lp/η）」は、BC点の管許容耐力照査だけでなく、**クッション材の必要厚さ・材質を決めるqa（局部許容応力度照査）とも同じ根っこの式を共有している**ことが分かった。

**新たに判明した実務のポイント:**
- **クッション材の主材質はコルクではなく、パーティクルボード（PB）とFJリング（発泡スチロール/EPS）の2つが実務の主流**だった。直線・緩曲線区間はパーティクルボード、必要厚さが大きくなる区間（きつい曲線・BC点付近）はFJリングを使う、という使い分けが2現場（水無第6・渋沢2工区）とも共通していた。**コルクの一次資料はこれで7件調査しても見つかっていない**——古い工法や本記述とは別の呼称の可能性があり、ユーザーに確認が必要。
- 必要クッション材厚さは、区間（直線／曲線）だけでなく**円周上の取付角度範囲（0～22.5°／22.5～30°／30～45°／45～60°／60～90°）ごとに個別に決まる**（渋沢2工区の資料で確認、厚さが21/21/15.5/10/10mmのように角度によって変わる）。曲線の外側になるほど開口が大きくなるため、厚めのクッションが必要という理屈。
- **半管（1.200m）等の短い管を使うと必要クッション厚さが減る**（同じ位置で2.43m管なら21mm・1.2m半管なら15.5mm）——55章のBC点補正（有効長a）と整合する挙動で、短い管ほど折れ角の影響が小さいという物理と一致。
- **レベル1・レベル2地震動に対する継手抜出余裕の照査**も、クッション材選定と同じ検討書の中で行われていた。これは本アプリに全く無い機能（耐震照査）。

**材質選択機能の実装は、引き続きユーザーの指示通り「コルク・FJリング（実際にはパーティクルボードも）の特性をよく理解してから」判断する。** 上記の通りqa/η/Fa連鎖の計算式自体は判明したが、**コルクという材質固有の物性データはまだ確認できていない**ため、**現時点では実装に着手していない。**

## 59. 地震時継手抜出照査（レベル1・レベル2）とPB/FJクッション材選定を実装（コルク＝パーティクルボードの呼称であることが確定）

58.3節でユーザーから「コルク＝パーティクルボードのことです」との訂正があり、コルクという別材質を探す必要は無くなった。あわせて「地震時継手抜出照査（レベル1・レベル2地震動）を実装してほしい」「PB/FJから選定する方法を採用してください」という2つの実装指示を受けた。式の出典についてAskUserQuestionで確認したところ、地震時継手抜出照査は「標準的な式で進めてよい」、PB/FJの応力ひずみデータは「（PB側は）持っていないので」との回答だった。以下、簡易化した実装方針を含めて実装した。

### 59.1 地震時継手抜出照査（レベル1・レベル2地震動）

出典は下水道協会「下水道施設耐震計算例－管路施設編－2001年版」（下水道施設耐震対策指針と解説・標準土質モデル タイプⅡ準拠）。設計地盤ひずみは標準値（レベル1地震動=0.23%、レベル2地震動=1.5%）を使用する。参考として`https://www.jswa.jp/inquiry/qa-guideline/qa-kanroshisetsu/`（下水道協会の質疑応答集ページ）も確認し、「抜出し量照査は各計算値の最大値を許容値と比較する」「レベル2のみ永久ひずみ（側方流動）を考慮する」等、同シリーズの質疑応答集であることを裏付けとして確認した。

実装式:
```
地震時必要抜出量 = 管長(mm) × 設計地盤ひずみ
抜出し余裕 = 継手の抜出し長(pulloutOpen) − 据付時残留開口S1(calcS1()) − 地震時必要抜出量
判定: 余裕 ≥ 0 でOK
```

`calcSeismicJointCheck(pipeLen_m, S1_mm, pulloutOpenMm)`をM_APP_005.htmlに新設し、既存の`JOINT_SPEC`・`calcS1()`と同じ場所（継手・クッション関連セクション）に配置した。表示は基本情報タブの「📏 管長さ最適化アドバイス」カードの直下に新カード「🌏 地震時継手抜出照査（レベル1・レベル2地震動）」を追加し、直線区間（標準仕様、S1=0とみなす）と各曲線区間（`calcS1()`による据付時残留開口）それぞれについてL1・L2の余裕とOK/NG判定を表で示す`updateSeismicJointCheck()`を実装、`updatePipeLenOpt()`末尾から呼び出す形で既存の再計算トリガー（`onCurveChange`等）にそのまま乗せた。

ブラウザ実機確認（localhost:8654）で、標準仕様の直線区間はL1=34.4mm余裕/L2=3.6mm余裕でOK、R=200mの緩曲線もOKだったのに対し、R=30mの急曲線区間（2.43m標準管のまま）ではS1=99.0mmとなり継手抜出し長40mmを大きく超過、L1=-64.6mm/L2=-95.5mmでNGと表示されることを確認した。これは実際に問題のあるケース（きつい曲線に長い標準管を使っている）を正しく検出しており、既存の「管長さ最適化アドバイス」（短い管への変更を提案する機能）と組み合わせて使う設計として妥当な挙動。

### 59.2 PB/FJクッション材選定

58.3節で判明した「クッション材選定はBC点許容耐力照査（η/Ma/qa/Rg/Fa連鎖）と同じ計算チェーンを共有する」という知見はあるが、η補正自体（56〜58節で確認した実務の完全な式）はBC点管種検討（`calcFaBC()`）に未実装のまま（QA_TABLEの出典が未確認のため、ユーザーから明示的な実装指示があるまで着手しないと判断——59章時点でも据え置き）。そのため今回のクッション材選定は、既存の`bcResults`（BC点ごとの実際の軸力FBC、`FBC_arr`から算出済み）を再利用した簡易版とした。

**簡易化の考え方:**
- 無潰れ（剛体接触）を仮定した場合の局部圧縮応力度 `σ_req = FBC×1000 / (π/4×(D²-Di²)×1e6)` [N/mm²]（管壁断面積で軸力を除しただけの単純な値。BC点の実際の応力分布はこれより複雑だが、簡易的な目安として使用）。
- 推進管仕様カードに新規入力欄「継手部許容局部圧縮応力度 σ_allow [N/mm²]」を追加（未入力の間は判定を行わない）。目安として「管の設計基準強度の50%程度」と説明文に明記（過去セッションの調査で確認した慣例）。
- σ_req ≤ σ_allow ならPB（パーティクルボード）標準品で対応可能と判定。
- σ_req > σ_allowならFJリングを検討し、`FJ_GRADES`（1.7倍≈21N/mm²／2.0倍W≈14.5N/mm²／2.5倍G≈9.5N/mm²／3.0倍Y≈7.5N/mm²、プラトー応力の高い順）の中から、プラトー応力がσ_allow以下となる最も剛性の高い（プラトー応力が高い）グレードを推奨。発泡材料は圧縮時に応力がほぼ一定のプラトー域に留まる特性があるため、伝達される局部応力の上限はほぼプラトー応力で決まるという考え方に基づく。
- 最も柔らかい3.0倍Yのプラトー応力（≈7.5N/mm²）でもσ_allowを上回る場合はNG（規格品では対応不可、特殊クッション材や多重積層等の個別検討が必要と案内）。
- **PB（パーティクルボード）は応力-ひずみデータが未取得**（ユーザー確認済み「持っていないので」）のため、圧縮による応力低減効果は考慮せず、無潰れ想定での判定に留める安全側の簡易判定とした。FJリングのプラトー応力は「Center Plastic Buffer Curve System」応力-ひずみ曲線図（積水化成品工業、試験実施：日本建築総合試験所 試験番号VA-06-0046）からの読み取り概算値であり、正式な製品カタログ値ではない点に注意。

実装場所: `selectCushionMaterial(sigmaReq, sigmaAllow)`と`FJ_GRADES`を`calcFaBC()`の直後（BC点関連セクション）に新設。`calculate()`内でBC点ごとの`cushionResults`（FBC・σ_req・選定結果）を算出し`lastCalcResult`に格納、`renderResults()`で新カード「🧊 詳細: クッション材（PB/FJ）選定」（推進力タブ、BC点外圧管種検討表の直下）に表示する。

ブラウザ実機確認（同上）で、σ_allow=15の場合は両BC点ともPB判定、σ_allow=10の場合は片方がPB・片方がFJ 2.5倍G判定、σ_allow=5の場合は両方NG判定、σ_allow未入力の場合は両方「未入力」判定となることを`javascript_tool`経由で確認した。

### 59.3 その他

- `service-worker.js`の`CACHE_NAME`を`m-app-005-v53`→`m-app-005-v54`に更新。
- 59.1・59.2とも、コード変更後にブラウザ実機（`http://localhost:8654/M_APP_005.html`、`.claude/launch.json`の`m_app_027_static`設定）で`javascript_tool`により実際の関数呼び出し・UIスクリーンショットで動作確認済み。

### 59.4 σ_allow（継手部許容局部圧縮応力度）に一次資料値を反映し、管種変更で自動入力するように変更

59.2節時点ではσ_allowは完全な任意（空欄）手入力だった。ユーザーから播磨コンクリート工業「Wジョイント推進管」の製品ページ（`https://www.harima-con.co.jp/product/hume_propulsion_pipe/w_joint_propulsion_pipe/`）を参考情報として共有され、これを基に自動入力するよう指示を受けた。

ページから確認できた実数値（許容平均圧縮応力度、クッション材厚10mm考慮）:
- 50型（本アプリの1種/1shu相当）: **13N/mm²**
- 70型（本アプリの2種/2shu相当）: **17.5N/mm²**
- 90型: 22.5N/mm²（本アプリのヒューム管グレードには90N相当の選択肢が無いため未使用）

本アプリの管種セレクトは既に「1種（50N）」「2種（70N）」というN強度クラス表記を使っており、この2値がそのまま対応することを確認した。`HUME_SIGMA_ALLOW = {'1shu':13.0, '2shu':17.5}`を新設し、`applyCushionSigmaAllowDefault()`で管種カテゴリ（ヒューム管のみ）・グレード変更時（`onPipeCategoryChange()`・`pipeGrade`の`onchange`）に自動入力するようにした。既存の外径・内径・管厚等の自動項目（`PIPE_AUTO_FIELD_IDS`）とは異なり、メーカー・クッション材厚の違いでこの値は変わり得るため、**読み取り専用にはせず、手動での上書きを許可**し続ける設計とした。呼び径（管径）だけを変更した場合は上書きしない（グレード変更時のみ再設定）ことで、ユーザーが手入力した値を診断・断りなく消してしまわないようにした。レジン管・鋼管はこの一次資料の対象外のため自動入力せず、既存値をそのまま保持する。

ブラウザ実機（javascript_tool）で以下を確認済み: ①1種→13、②2種変更で17.5に更新、③手動で99.9に上書き後に呼び径のみ変更しても99.9のまま保持、④その後1種に戻すと13で再度上書き、⑤レジン管に切り替えても既存値は保持されそのまま変わらない。localStorage初期化後の新規プロジェクトでも1種のデフォルト13が正しく自動入力されることを確認した。

入力欄の説明文（推進管仕様カードのnote-box）も、この出典・自動入力の挙動を明記する内容に更新した。`service-worker.js`の`CACHE_NAME`を`v55`に更新。

**残課題（引き続き未実装、ユーザーからの明示指示待ち）:**
- BC点管種検討（`calcFaBC()`）へのη効果補正の実装——56〜58節で式は確定済みだが、QA_TABLEの静的ルックアップを動的Ma/qa連鎖に置き換えるかどうかの設計判断がまだ必要。ユーザーには57節で考え方を説明済みだが、実装の明示指示はまだ受けていない。
- 中押しジャッキ（中押し）段数計算（La/Lan1/Lan2/M）。
- 現場打ち支圧壁の④相当（片持ち梁・引張照査）。
- ③受働土圧の円弧扇形バリエーション。
- コルクという独立した材質の一次資料は最終的に見つからなかった——58.3節・本節の調査で「コルク＝パーティクルボードの呼称」と確定したため、以後探す必要は無い。

## 60. 測量タブ: Excel出力の「一覧」表を画面の「計算結果一覧」と同じ列構成に統一、KP役割から次の1本を推定する「予測点」機能を追加

> **※この章は60.1〜60.11まで11回改訂されています。** 最新の正しい仕様は冒頭「重要な読み方の注意」の該当項目を参照してください（要点: 予測点の判定は役割文字列不要・位置ベース=60.5章／予測点の座標式=60.8章／予測点の名称=60.9章／Excel「結果一覧」表の列構成=60.7章／埋め込みグラフ=60.11章）。60.1〜60.4章・60.10章の記述はいずれも後で変更された初版の内容です。

ユーザーから、画面の「📊計算結果一覧」カード（KP｜役割｜X｜Y｜位置｜RO-測点距離｜Rとの差｜計画X｜計画Y｜左右誤差｜判定の11列）のスクリーンショットが示され、①Excel出力の「法線との測定誤差計算結果（一覧）」表もこの見た目に揃えること、②直近2本（役割欄に「1本目前」「1本目後」「2本目前」「2本目後」と入力した測点）の折れ角から、次の1本の着地点を推定する「予測点」を実装すること、③表示順序は「予測点→KP1(1本目前)→KP2(1本目後)→KP3(2本目前)→KP4(2本目後)」にすること、の3点を依頼された。

### 60.1 予測点の考え方

役割欄（`kpRows[].role`、自由入力）に「1本目前」「1本目後」「2本目前」「2本目後」という文字列を入力すると、以下の手順で次の1本の着地点を推定する。

```
dir2（2本目の方向） = atan2(2本目後.y − 2本目前.y, 2本目後.x − 2本目前.x)
dir1（1本目の方向） = atan2(1本目後.y − 1本目前.y, 1本目後.x − 1本目前.x)
predDir（予測方向） = 2×dir1 − dir2　　※「dir2→dir1の折れ角を、dir1にもう一度適用する」のと同じ式
予測点 = 1本目後 + 管有効長l′ × (cos(predDir), sin(predDir))
```

2本目→1本目で実際に測定された折れ角が、1本目→次の1本でも同じだけ続くと仮定する単純な線形外挿。`predDir=2×dir1−dir2`という形にしたことで、角度の正規化（-180〜180化）は表示専用の値（折れ角の表示）にだけ必要で、実際の予測点計算（cos/sinに渡す角度）には不要という点に気づいた（cos/sinは周期関数のため、2πの整数倍のズレを気にする必要がない）。

役割欄の文字列は`parsePipeRole()`で正規表現（全角数字の半角変換込み）を使って解析し、「1本目」「2本目」がそれぞれ「前」「後」とも揃っている場合のみ予測点を計算する。1つでも欠けていれば予測点は計算しない（画面側は代わりに入力方法の案内を表示）。

### 60.2 実装（画面表示）

`computeKpPrediction(points)`（新規、`judgePoint()`の直後）が上記の計算を行う。`calcSurvey()`で`surveyResults`を計算した直後にこれを呼び、結果を`judgePoint()`にもう一度通すことで、他のKPと同じ「位置・計画X/Y・左右誤差・判定」を予測点にも持たせている。`renderSurveySummary()`・`renderSurveyTable()`は`pred`引数を追加し、予測点があれば最上部（画面の要望通りKP1より前）に破線・薄い青背景で区別した行/カードとして表示する。予測に必要な役割が揃っていない場合、KPが2本以上あれば「予測点を表示するには〜」という案内を表示する。

### 60.3 実装（Excel出力）

`exportSurveyXLSX()`で、KP座標（`kpPtRows`）を作った直後に役割文字列を解析し、`ATAN2`・`COS`/`SIN`を使った生の計算式（60.1の式をそのまま）で予測点X/Yを計算する新セクションを追加した。予測点が計算できた場合、他のKPと全く同じ「候補一覧→現在区間の判定→P1/P2/R取得→計画X/Y→誤差・左右→判定」のブロックに（`judgeTargets = [predKp, ...kpPtRows]`として）そのまま流し込んでいるため、コードを複製せずに済んでいる。

「法線との測定誤差計算結果（一覧）」表は、列を画面と同じ**KP｜役割｜X｜Y｜位置｜RO-測点距離｜Rとの差｜計画X｜計画Y｜左右誤差｜判定**の11列に作り直した（旧: 名称｜役割｜現在区間｜測定点X｜測定点Y｜計画X｜計画Y｜誤差(mm)｜左右｜判定の10列）。新設した列：
- **RO-測点距離／Rとの差**: 曲線区間のときだけ`IF(種別="曲線", …, "-")`で計算し、直線区間は画面表示と同じ「-」にする。
- **左右誤差**（誤差(mm)と左右を1列に結合）: 曲線区間は`左右&" "&ROUND(誤差,0)`（例:「右 40」）、直線区間は`ROUND(誤差,1)&" mm"`（例:「40.1 mm」）と、画面の`r.short`表示を式で再現した。**誤差(mm)自体（数値セル）はOK/注意/NG判定の比較式で引き続き必要なため削除せず残し、左右誤差列はその表示用の追加列**という位置付け。

### 60.4 検証

ブラウザ実機（`javascript_tool`）で、①`parsePipeRole()`が「1本目前」「２本目 後」（全角数字・空白入り）等を正しく解析すること、②距離・角度から実際にトラバース計算したKP4点（役割を1本目前/後・2本目前/後に設定）で`calcSurvey()`を実行し、画面の計算結果一覧テーブルに🔮予測点の行が最上部に表示されること、③`exportSurveyXLSX()`を`URL.createObjectURL`をフックして実行し、生成されたXMLに`undefined`/`NaN`が含まれないこと、④生成された「一覧」表の予測点行（1行目）の各セルが正しいセル参照式（例: 位置列が現在区間確定セルを直接参照）になっており、画面表示の値（X=26.954、Y=1.869、判定NG等）と完全一致することを確認した。

`service-worker.js`の`CACHE_NAME`を`v56`に更新。

### 60.5 ユーザー訂正2件 — 予測点は役割入力なしで常に表示、Excel出力の罫線・列幅・数値桁数を全面見直し

60.1〜60.4の直後、ユーザーから2点の訂正・追加指摘があった。

**1. 予測点の表示条件について:** 「予測点の表示には1本目、2本目を入力とは頼んでいません。常に表示してください」との指摘。60.1の実装は役割欄に「1本目前」「1本目後」「2本目前」「2本目後」という文字列を要求していたが、これはユーザーの依頼を勝手に狭めた実装だった（元の依頼は「表示順序」の説明としてこの4語を使っていただけで、入力必須条件ではなかった）。さらに、ユーザーが実際にダウンロードしたファイルの実データを確認すると、役割欄には「1F」「1R」「2F」「2R」という別の略記が使われており、そもそも当初の正規表現ではマッチしない実データだったことも判明した。

**修正:** 役割文字列の解析（`parsePipeRole()`）を廃止し、`computeKpPrediction(points)`は**KPの並び順（先頭4点）をそのままKP1=1本目前・KP2=1本目後・KP3=2本目前・KP4=2本目後として使う**方式に変更した。役割欄の中身は一切見ない。KPが4点以上あれば常に予測点を計算・表示し、4点未満のときだけ「KPを4点以上入力してください」という案内を表示する。画面側（`computeKpPrediction()`）・Excel側（`exportSurveyXLSX()`内の予測点セクション）とも、役割マッチングのロジックを削除し`kpPtRows`/`surveyResults`の先頭4件を直接使うよう統一した。

**2. Excel出力の見た目について:** 実際にダウンロードしたファイル（`MYcurve_館町下水_１~2_測量データ_2026-08-10 (2).xlsx`）を提示され、「列の幅が最適化されていない」「罫線がない」「非常に見にくい」「ブラウザと同じ表示形式にしたと言っていたが全然なっていなかった」との強い指摘があった。60.3節時点の対応は列の「並び順・列見出し」を画面に合わせただけで、罫線・数値の表示桁数・列幅の実効性という**見た目の作り込みには一切手を付けていなかった**ことが原因だった。実際にopenpyxlでダウンロードファイルを検査すると、スタイル定義が事実上未設定（"Workbook contains no default style"警告）で、数値セルはExcelの既定General書式のまま`26.954118478456284`のような17桁近い生の値がそのまま表示される状態だったことを確認した。

**修正（`M_APP_005.html`のxlsx生成部分を全面的に見直し）:**
- `XLSX_STYLES`に罫線（全辺thin）・数値書式（`0.000`・整数`0`）・見出し用の太字＋薄青塗りつぶしのセルスタイルを追加。`xlsxStr`/`xlsxNum`/`xlsxFormula`/`xlsxFormulaStr`の4つのセル書き込みヘルパーすべてに`styleId`引数（省略可）を追加し、**省略時は文字セルは罫線付き、数値セルは罫線付き0.000書式を自動的に適用**するようにした。この既定値の変更だけで、呼び出し側のコードを1行も変えずに全シート・全セルに罫線と適切な小数桁数を適用できた（ヘルパー関数の呼び出し箇所は300箇所近くあるため、既定値だけで対応できる設計にしたのが効いた）。
- 度・分・秒などの整数値には`XLSX_STYLE_INT`（整数書式）を明示的に指定。
- 各シートの主要な列見出し行（基線データの曲線表・各座標データの点一覧・測定結果の測量結果表とトラバース計算表・区間別計算式一覧・候補一覧・そして画面の計算結果一覧と対応する「一覧」表）に`XLSX_STYLE_HEADER`（太字・薄青塗りつぶし・罫線）を適用。
- 4シートすべての列幅を、実際にダウンロードされたファイルの内容量に基づいて見直し（特に測定結果シートの列Aは、長い見出し文字列に対して極端に狭かったため42文字幅まで拡大）。

**検証:** ブラウザで`exportSurveyXLSX()`を実行し、生成されたxlsxバイト列をbase64化してPython側（openpyxl）に転送し、実際にダウンロードされるファイルと同じ内容を直接検査した。①全シートの列幅が意図通り設定されていること、②任意のセル（見出し・データとも）に`border.left.style=='thin'`が付与されていること、③数値セルの`number_format`が`0.000`（または整数セルは`0`）になっていること、④「一覧」表の見出し行が`font.bold=True`・`fill.fgColor.rgb=='FFD9E2F3'`（薄青）になっていること、⑤予測点行（役割欄が空でも表示される）のキャッシュ値がアプリ本体の計算結果と一致することを確認した。この開発環境にはExcel本体もLibreOfficeも無いため、Excelが実際に数式を再評価した見た目までは確認できていない（50.3節以来の既知の制約）。

`service-worker.js`の`CACHE_NAME`を`v57`に更新。

### 60.6 「結果一覧」シートを新設 — 詳細な計算過程から独立させ、ファイルを開いたら最初に見える形に

60.5直後、ユーザーから実際にダウンロードしたファイル（`MYcurve_館町下水_１~2_測量データ_2026-08-10 (3).xlsx`）に自分でシートを追加し、必要な項目を手作業で切り取り・貼り付けて再現しようとしたが「予測点の座標だけ足りなかった」との報告があり、画面の計算結果一覧のスクリーンショット（予測点の行を含む）で「ほしいのはこんな感じです」と示された。

**調査結果:** 実際にダウンロードされたファイルをopenpyxlで検査したところ、「測定結果＋測定誤差」シート内の「一覧」表には予測点のX/Y（列C/D）とも正しい値（`=C28`/`=D28`参照、値`36.824506...`/`-12.762251...`）が入っており、**数値自体は欠落していなかった**。ユーザーが提示したファイルでは、手作業で追加した独自シートの一部の列（計画X/計画Y・左右誤差・判定）がその独自シートへの参照に化けていたことから、Excel上で「切り取り（カット）」操作を行うと、元のセルを参照している他の数式が自動的に新しい貼り付け先を指すよう書き換わるという、Excelの挙動によるものと判明した（アプリ側の出力自体のバグではない）。

とはいえ、**「一覧」表が全11シート中でも特に長い「測定結果＋測定誤差」シート（本セッションの検証データでも150行超）の末尾に埋もれており、目的の表にたどり着くまでに大量の計算過程を読み飛ばす必要がある**という構造上の使いにくさは実在する問題だった。ユーザーが手作業で必要な部分だけを別シートに再構成しようとした行為自体が、この構造上の不便さの表れだった。

**対応:** 「一覧」表を独立した5枚目のシート「結果一覧」として新設し、詳細な計算過程（基線データ・測量基準データ・各座標データ・測定結果＋測定誤差の4シート）とは完全に分離した。「結果一覧」シートの各セルは、引き続き「測定結果＋測定誤差」シートの該当セルをクロスシート参照する生の数式（`='測定結果＋測定誤差'!C26`等）であり、値をコピーしているわけではない——詳細シートの入力値を書き換えれば「結果一覧」も連動して再計算される。

- `xlsxWorkbookXml()`に`activeTabIndex`引数を追加し、`<bookViews><workbookView activeTab="4"/></bookViews>`を出力することで、**タブの並び順（左から基線データ→…→結果一覧の順、既存の4シートの並びは変更していない）はそのままに、ファイルを開いたときに最初から「結果一覧」タブが選択された状態**になるようにした。
- Content_Types・workbook.xml.rels（rId5=sheet5.xml、styles.xmlはrId6に繰り下げ）にsheet5.xmlの関連付けを追加。
- 「結果一覧」シートの先頭に「詳細な計算過程は『測定結果＋測定誤差』シートを参照してください」という案内文を明記。

**検証:** ブラウザで生成したファイルをbase64経由でPython（openpyxl）に転送し、①`結果一覧`シートが存在し中身が画面の計算結果一覧と完全一致すること（予測点行のX/Y含む）、②ワークブックのアクティブシートインデックスが`結果一覧`（4、0始まり）になっていること、③「結果一覧」シートの各セルが実際に`'測定結果＋測定誤差'!...`という形のクロスシート参照式になっていること、④見出し・罫線・列幅のスタイルも他シートと同様に適用されていることを確認した。

`service-worker.js`の`CACHE_NAME`を`v58`に更新。

### 60.7 「文字列＆数字＆文字列」の連結セルを数値セル＋ユーザー定義表示形式に変更、左右誤差は列を分割

60.6直後、ユーザーから「測定結果＋測定誤差シートで誤差などに文字列(右)＆数字＆文字列(mm)みたいになっているものがあるが、こうすると数字として扱えなくなるので、ユーザー定義表示形式にするか、それが無理なら複数列に文字・数字・文字というふうに分けてほしい」との指摘があった。60.3〜60.4で追加したRO-測点距離・Rとの差・左右誤差の3列は、いずれも`TEXT(数値,"書式")&"単位"`という文字列連結の式にしており、セルの中身が数値ではなく文字列になっていた（Excel上で並べ替え・SUM等の数式に使えない状態）。

**対応:**
- **RO-測点距離・Rとの差** はユーザー定義表示形式で解決した。`XLSX_STYLES`に`0.000" m"`（numFmtId=166）・`0.0" mm"`（numFmtId=167）の2つのカスタム数値書式を追加し、セルの中身は生の数値（メートル・ミリメートル）のまま、表示だけ単位が付くようにした（`XLSX_STYLE_M`・`XLSX_STYLE_MM`）。直線区間で該当しない場合の「-」表示は、その時だけ文字列セル（数値書式は数値以外の値には影響しないため共存可能）として扱う。
- **左右誤差**（「右」+数字+「mm」のように、方向という文字情報と数値を結合していた列）は、表示形式だけでは方向を出し分けられないため、ユーザー提案の代替案（複数列に分割）を採用した。結合セルを廃止し、既存の「誤差(mm)」（数値のみ、これは元から数値セルだった）と「左右」（文字列のみ）を、そのまま独立した2列として「結果一覧」シートに並べる形にした。この変更で「結果一覧」シートの列数は11列から12列（KP｜役割｜X｜Y｜位置｜RO-測点距離｜Rとの差｜計画X｜計画Y｜誤差(mm)｜左右｜判定）になった。画面の「計算結果一覧」表（左右誤差を1列に結合する表示）はユーザーが最初に指定した見た目のままなので変更していない——今回の変更はExcel出力側だけの対応。

**検証:** ブラウザで生成したファイルをopenpyxlで検査し、①「結果一覧」「測定結果＋測定誤差」両シートの誤差(mm)列が実際に数値型セル（`isinstance(cell.value, (int,float))`）で書式`0.000`になっていること、②RO-測点距離・Rとの差列が数値セルで書式`0.000" m"`・`0.0" mm"`になっていること（直線区間は文字列「-」のまま）、③左右列が独立した文字列セルになっていること、④生成された全シートに`undefined`/`NaN`が含まれないことを確認した。

`service-worker.js`の`CACHE_NAME`を`v59`に更新。

### 60.8 予測点の座標が間違っていた2つのバグを修正（過去バージョンM_APP_001の実装と突き合わせて発見）

ユーザーから「予測の座標値が間違っています。過去のバージョン（`曲線Vr018\M_APP_001\05_measurement_horizontal_error.html`）では実装されて正確に機能していましたので、間違えを探してみてください」との指摘があった。同ファイルの`updatePredictions()`（予測点計算の本体）を読み、60.1で実装した`computeKpPrediction()`と1行ずつ突き合わせたところ、**2つの独立したバグ**が見つかった。

```
過去バージョン（正）:
  先頭管方向 = atan2(KP1.y-KP2.y, KP1.x-KP2.x)   // KP2→KP1（後ろから前へ）
  後方管方向 = atan2(KP3.y-KP4.y, KP3.x-KP4.x)   // KP4→KP3
  次回方向 = 先頭管方向 + (先頭管方向 − 後方管方向)
  予測点 = KP1 + 次回方向 × 管長                 // 起点はKP1（現時点の最先端）

60.1の実装（誤）:
  dir1 = atan2(KP2.y-KP1.y, KP2.x-KP1.x)         // KP1→KP2（前から後ろへ、向きが逆）
  dir2 = atan2(KP4.y-KP3.y, KP4.x-KP3.x)         // KP3→KP4（同じく逆）
  predDir = 2*dir1 - dir2
  予測点 = KP2 + predDir × 管長                   // 起点がKP2（1本目の後端）になっていた
```

**バグ1（方向ベクトルの向きが逆）:** 先頭管方向を「前→後（KP1→KP2）」で計算していたため、実際の進行方向（KP2→KP1）とちょうど180°反対になっていた。後方管方向も同様に逆だった。
**バグ2（延長の起点が違う）:** 予測点を「KP1から」ではなく「KP2から」延長していた。KP1（1本目前＝現時点で最も推進が進んでいる先端）から延長すべきところを、KP2（1本目後）から延長していた。

2つの誤りが組み合わさった結果、方向は180°反転し起点も1本分ずれるという、大きく見当違いな座標になっていた（`predDir=2*dir1-dir2`の式自体は、正しい向きのベクトルを使えば過去バージョンの式と数学的に同一だったため、式の骨格は合っていたが、入力する角度の定義が逆だった）。

**修正:** `computeKpPrediction()`（画面表示側）と`exportSurveyXLSX()`内の予測点計算ブロック（Excel側）の両方を、過去バージョンの`updatePredictions()`と同じ定義（KP2→KP1・KP4→KP3の向き、KP1を起点に延長）に合わせて修正した。

**検証:** KP2=(10,0)・KP1=(19.848,1.736)（KP2→KP1方向=10°）・KP4=(-10,0)・KP3=(0,0)（KP4→KP3方向=0°）という手計算しやすい座標で試算し、期待値（折れ角10°→次回方向20°→予測点=KP1+2.43×(cos20°,sin20°)=(22.131,2.567)）と`computeKpPrediction()`の実際の出力（22.1315, 2.5676）が一致することを確認した。Excel側も同一の入力データで生成し、キャッシュ値が画面表示の値と完全一致すること、生成された数式（`ATAN2(kp1.x-kp2.x,kp1.y-kp2.y)`の向き、`kp1.x+l′*COS(...)`の起点）が過去バージョンの定義と一致することを確認した。

`service-worker.js`の`CACHE_NAME`を`v60`に更新。

**残った検討事項（ユーザーに確認済み）:** 予測点を延長する距離に使っている`n('pipeLen')`（推進管仕様カードの管有効長l′）は、過去バージョンM_APP_001の対応する値（`predictionLength`、隠しフィールドで既定1.2m固定）とは別の概念で、M_APP_005の標準管長設定をそのまま流用している。区間別個別仕様を使っている場合に実際の次の1本と長さがずれる可能性を確認事項として挙げたところ、ユーザーから「推進管仕様カードから予測位置を決定するで問題ありません」との回答があり、標準仕様のl′をそのまま使う設計で確定した。

### 60.9 予測点の名称を「（測量本数＋1）本目予測」に変更

60.8直後、ユーザーから「予測点の名称を現在の測量本数＋１にして　38本目予測としてください」との指示があった。過去バージョン（`曲線Vr018\M_APP_001\05_measurement_horizontal_error.html`）の`predictionPipeLabel()`（`${現在の本数+1}本目予測`、本数未入力時は`次回予測`）と同じ考え方をM_APP_005に移植した。

**実装:** `predictionPipeLabel()`を新設し、測量タブの「本数」入力欄（`surveyPipeCount`、測定数値入力カード）の値+1を使って`${pipe+1}本目予測`を返す（本数が0以下・未入力なら`次回予測`にフォールバック、過去バージョンと同じ挙動）。この名称を、画面（`calcSurvey()`が`lastKpPrediction`を作る際の`name`、`renderSurveySummary()`・`renderSurveyTable()`の表示）とExcel出力（`predKp.name`）の両方で使うようにした。あわせて役割（role）欄も、意味のない`'次の1本'`から過去バージョンに合わせた`'予測'`に変更した。

**検証:** ブラウザで「本数」を37に設定して予測点を計算すると、画面・Excel出力とも予測点の名称が`38本目予測`・役割が`予測`になることを確認した。「本数」を空にすると`次回予測`にフォールバックすることも確認した。

`service-worker.js`の`CACHE_NAME`を`v61`に更新。

### 60.10 「各座標データ」シートに曲線の弧を含む散布図を自動で埋め込み

ユーザーが「各座標データ」シートに手動で散布図を挿入し、直線区間（BP-IP-EP）と曲線の主要点（BC-RO-EC）を系列にして表示したところ、「曲線も書きたいのですが何かいい方法ありますか」「自動でグラフを配置できますか」という2つの質問があった。BC・RO・ECの3点だけを直線で結んでも実際の弧の形にはならないため、①弧を実際に描く方法、②グラフ自体をアプリ側で自動配置する方法、の両方に対応した。

**曲線描画用サンプル点（60章の新規追加）:** 「各座標データ」シートのBC/RO/EC座標ブロックの直後に、各曲線をBC→ECまで24分割したサンプル点（すべて生の計算式）を追加した。ROから見たBCの方位角を起点に、`dir='R'`なら+方向・`dir='L'`なら-方向にIA（交角）だけ掃引し、`X=ROx+R×cos(角度)`・`Y=ROy+R×sin(角度)`で各点を求める（`curveDelta()`の符号規約と同じ）。この25点（0〜24分割の端点含む）を散布図の1系列として選べば、実際の弧の形がそのまま表示される。

**散布図の自動埋め込み:** 外部ライブラリを使わずOOXMLの図形描画パーツ（`xl/drawings/drawing1.xml`）とグラフパーツ（`xl/charts/chart1.xml`）を自前実装し、「各座標データ」シートに散布図を自動配置するようにした。
- 系列は「直線区間(BP-IP-EP)」（オレンジ、マーカーあり、既存のBP〜EP範囲をそのまま参照）と、曲線ごとの「第N曲線」（青系のパレットで色分け、マーカー無しの滑らかな線、上記サンプル点範囲を参照）。
- `xlsxSheetXml(sheet, drawingRId)`に`drawing`要素を出す機能を追加し、`xl/worksheets/_rels/sheet3.xml.rels`経由で図形描画パーツと関連付けた。`[Content_Types].xml`にも`drawing1.xml`・`chart1.xml`のOverrideを追加した。
- グラフは散布図データの右側（列H〜、行2〜36相当）に自動配置される。

**検証:** ブラウザで生成したxlsxをbase64経由でPythonに転送し、①ZIP内に`xl/charts/chart1.xml`・`xl/drawings/drawing1.xml`・関連する`.rels`が正しく含まれること、②全XMLパートが`xml.etree.ElementTree`でパースエラー無く読めること、③**openpyxlで実際にワークブックを開き、`worksheet._charts`に`ScatterChart`オブジェクトが1つ検出され、系列が2本（直線区間`$B$3:$B$5`、第1曲線`$B$14:$B$38`）とも意図した色・データ範囲になっていること**を確認した（openpyxlのチャート読み込みはOOXMLスキーマに厳格なため、これが通ったことはExcelでも読み込める可能性が高いことの一定の裏付けになる）。この開発環境にはExcel本体が無いため、実際の見た目の最終確認はできていない。

`service-worker.js`の`CACHE_NAME`を`v62`に更新。

### 60.11 散布図の縦横反転を修正、測量結果の系列とラベル付き拡大図を追加（60.10の改良）

60.10直後、ユーザーから4点の要望があった。①座標をグラフにすると左右上下が逆になるのでX/Yを反転してほしい、②測量結果（測点）も表示してほしい、③ラベルに「測点名／改行／誤差（右2mm）」のように2行で表示してほしい、④ラベルが重ならないようにしてほしい、⑤測定箇所だけの拡大図も別途用意してほしい。さらに直後の追加指示で「ラベルは拡大図だけでよい」「全体図ラベルはBPなどの点だけにしてください」と、ラベルの掲載範囲が明確化された。

**①軸の反転（原因究明）:** `xlsxChartXml`の軸タイトルはすでに「横軸=Y（右+）・縦軸=X（前方+）」と表記していたが、実際に系列へ渡していたデータは標準的な数学の向き（列B=X→横軸、列C=Y→縦軸）のままで、**表記と実データが食い違っていた**。アプリ本体の線形ビジュアル表示（`drawAlignmentVisual()`）は縦軸=X（前方+）・横軸=Y（右+）で描いており、この向きに合わせるのが正しい。`xlsxChartSeriesXml()`内で、渡されたX列レンジをc:yVal（縦軸）に、Y列レンジをc:xVal（横軸）に、あえて入れ替えて紐づけるよう修正した（呼び出し側の`xColRange`/`yColRange`という命名はデータの意味のまま変えず、入れ替えは関数内に閉じ込めている）。

**②測量結果（予測点含む）の系列を追加:** 「結果一覧」シート（60.6章）はすでに予測点＋KP1〜KPnが連続した行で並んでいるため、この範囲（C列=X、D列=Y）をそのまま1つの系列にできた。マーカーのみ・線無し（`noLine:true`）、色は測量データらしい赤系（D62728）にした。

**③④ラベルの2行表示・重なり対策:** OOXMLの`c:dLbl`（系列内の個別データラベル上書き）を使い、各点に「1行目=測点名、2行目=誤差（左右＋mm、四捨五入）」の2段落リッチテキストを埋め込む`xlsxDLblXml()`を新設した。厳密な重なり回避アルゴリズムは実装していないが、点の並び順に応じて6方向の手動オフセット（`LABEL_OFFSET_CYCLE`、チャート領域に対する割合指定）を順番に割り当てることで、隣接する点のラベルが同じ位置に重ならないようにする簡易ヒューリスティックを入れた。**この方式はJS側で計算した時点の値をそのまま静的なリッチテキストとして埋め込むため、Excel上でセルの値を書き換えてもラベルの文字は自動更新されない**（点の座標自体は引き続き生の数式でシートを参照しているため、グラフの形そのものは連動する）。

**⑤ユーザー追加指示（ラベルの掲載範囲を限定）:** 全体図（chart1）は直線区間（BP/IP/EPの点名のみ、単行ラベル）と曲線・測量結果を表示するが、**測量結果の系列にはラベルを付けない**。拡大図（chart2）は測量結果の系列にだけ②③のラベルを付け、直線・曲線の系列にはラベルを付けない。

**拡大図の軸範囲:** 測量結果（予測点含む）のX/Yそれぞれの最小値〜最大値に、範囲の30%（最低1.0m）の余白を加えた値を`xlsxChartXml()`の`axisRange`引数（`hMin/hMax/vMin/vMax`）として渡し、`<c:valAx><c:scaling>`に明示的な`<c:max>`/`<c:min>`を出力することで拡大表示にした。全体図側は軸範囲を指定せず自動スケールのまま。

**実装（複数チャート対応）:** 1つの`drawing1.xml`に2つの`<xdr:twoCellAnchor>`（全体図・拡大図）を持たせ、`drawing1.xml.rels`もrId1→chart1.xml・rId2→chart2.xml の2件に拡張した（`xlsxDrawingXml(anchors[])`・`xlsxDrawingRelsXml(chartFiles[])`に汎用化）。測量結果が1件も無い場合（KP未入力）は拡大図自体を生成しない。

**検証:** ブラウザで生成したファイルをbase64経由でPythonに転送し、①ZIP内に`chart1.xml`・`chart2.xml`・`drawing1.xml`とも正しく含まれ全XMLが妥当であること、②各系列の`c:xVal`/`c:yVal`が想定通り入れ替わっていること（全体図の直線系列で`x='各座標データ'!$C$3:$C$5`・`y=...$B$3:$B$5`）、③全体図は直線系列だけに3件（BP/IP1/EP）のラベル、拡大図は測量結果系列だけに5件（予測点＋KP1〜4）の2行ラベルが付いていること、④ラベルのテキストを実際にUTF-8で復元し「38本目予測」「誤差（右 631 mm）」「KP1」「誤差（左 0 mm）」等、意図した内容と完全一致すること、⑤拡大図の軸に明示的なmin/maxが設定され、全体図側には無いこと、⑥全体を通じて`undefined`/`NaN`が含まれないことを確認した。openpyxlの`DataLabel`オブジェクトはリッチテキスト（`c:tx`）を読み込みモデルに公開していないため`s.dLbls.dLbl[i].tx`は存在しないが、これはopenpyxl側の制約であり、ZIPから直接取り出した生XMLには正しく`<c:tx><c:rich>...`が入っていることを確認済み。

`service-worker.js`の`CACHE_NAME`を`v63`に更新。

---

## 61. 刃口推進の周面抵抗式を選択式に — 下水道協会式／佐藤・山本改定式

### 61.1 発端

ユーザーから、渋沢2工区フォルダ内にあった`下水道協会刃口推力計算.xlsm`（マクロ有Excel、シート内工事名は入江崎水処理センター＝汎用テンプレート流用）の解析依頼を受けた。読み込んだ結果、以下が分かった。

- xlsmの「協会式」ブランチ（`入力!F36=2`、自立地山用）を展開すると、当時の`calcFoFo()`のblade分岐と完全に同一構造（`f=α·π·Bc·(β·q·μ'+Ca)+W·μ'`）であることを確認した。**アプリの式構造自体は下水道協会2000年版指針と一致しており、変更不要**と判明。
- 一方、現状の初期値`soilAlpha=0.625`・`soilBeta=1.20`の出所は不明なまま残っていた。

ユーザーから続けて「土質および自立性による係数の違い」の要約（α/β/Ca表）が提示され、さらに一次資料として次の2点が送付された。

1. 佐藤直昭・山本稔「鉄筋コンクリート管を用いた推進工法実績から考えられる推進力算定式」（土木学会年次学術講演会概要集III-237）
2. 望月崇「大中口径管推進工法の推進力算定式の変遷」（月刊推進技術2014年2月号、（公社）日本推進技術協会技術部長による業界団体自身の解説記事）

両資料を突き合わせた結果、下水道協会式（α・β係数なしの元祖1976年式、または上記の自立地山用アプリ式）とは**構造が異なる第三の式**であることが判明した。

```
F = F0 + π・Bc・τa・L
τa = β・Ca + σ'・μ'
σ' = α・q + 2W/{π²(Bc-t)}
μ' = tanδ, δ=φ/2
```

αはq（土圧）のみに、βはCa（付着力）のみにかかる点が、アプリの現行式（αが式全体、βがqにかかる）と異なる。望月記事はこの式が「下水道協会式は実績と比べ過大になりがち」という問題意識から佐藤・山本が独自に提案した実績ベース補正式であることを明記しており、土質を①関東ローム（自立）②粘土（非自立）③砂礫④砂・シルトの4種類（実際は砂礫・砂シルトが荷重状態でさらに分岐し計7区分）に分けたα・β・Caの目安値も同記事内に再掲されている。

さらにユーザーから`推進力算定モデル_再構築.xlsx`という自作の検証用Excel（「算定パラメータ一覧」シートに7区分の表、「推進力計算モデル」シートに`σ=α·q+2W/{π²(Bc-t)}`・`τa=β·Ca+σ·μ`・`F=F0+π·Bc·τa·L`という数式）が提示され、**上記の式・数値と完全に一致**することを確認した。3つの独立資料（論文原典・業界誌記事・ユーザー自身の再構築モデル）が相互に裏付ける形になったため、実装に進んだ。

### 61.2 実装

ユーザーの「選択出来る形にして」という指示に従い、**下水道協会式（現行、既定）と佐藤・山本改定式を選べる**ようにした。

- `calcFoFo()`のblade分岐に`foFormula`パラメータを追加し、`'satoyamamoto'`時は上記の`σ'/τa/F`式（`Fo`の計算式`10×1.32π×Bs×N'`自体は両式共通、今回変更していない）、既定は従来通りの下水道協会式ロジックに分岐（[M_APP_005.html](M_APP_005.html)）。
- 「🌱土質条件」カードの刃口専用行（`bladeRow`）に「周面抵抗の算定式」セレクタ（`bladeFoFormula`）を新設。佐藤・山本式を選ぶと「土質・自立性区分」セレクタ（`soilSelfSupport`）が現れ、7区分（`SATO_YAMAMOTO_TABLE`）からα・β・Ca（`soilAdh`欄をkN/m²で流用、範囲がある値は中央値、Ca(t/m²)→kN/m²は×9.8換算）を自動入力する。既存の他のα/β自動入力パターン（Bs初期値・σ_allow等）と同様、自動入力後も手動で自由に上書き可能。③④の区分はCa=0のためβが計算に影響しない点を`onSoilSelfSupportChange()`内で意図的に「β上書きしない」形にし、note-boxにも明記した。
- 「📄計算書」5節（推進力の計算）のステップ表示を`foDetail.formula`で分岐し、佐藤・山本式選択時は`σ'`の式を含む正しい計算過程を表示するようにした（[M_APP_005.html:3947](M_APP_005.html)付近）。
- `t`（管厚、σ'の分母`(Bc-t)`に必要）を`foForSpec()`のパラメータに追加（従来`calcFoFo()`に渡っていなかった）。
- JSON保存/復元（`getProjectData()`/`setProjectData()`）の`soil`オブジェクトに`foFormula`・`selfSupport`を追加。

### 61.3 検証

ブラウザで実際に計算を実行し確認した。

- 佐藤・山本式・関東ローム区分（α=0,β=1/3,Ca=26.95）選択時: 計算書に「計算過程（佐藤・山本改定式）」の見出しと`σ'=α・q+2W/{π²(Bc-t)}`の式が表示され、手計算（σ'=0.625×34.77+2×5.31/(π²×0.88)=22.95、τa=0.333×26.9+22.95×0.2679=15.13、f=π×0.96×15.13=45.64kN/m）と一致した。
- 下水道協会式（既定）に戻して再計算してもコンソールエラーなし・既存の計算過程表示（`τa = β・q・μ′+C′`）のまま変わらないことを確認（回帰なし）。
- 砂礫（通常荷重）区分選択時、αのみ更新されβは直前の値のまま保持される（Ca=0のため計算に影響しない）ことを確認。

### 61.4 未解決事項（持ち越し）

- **下水道協会式ブランチ（既定）のα=0.625・β=1.20の出所は依然不明**。佐藤・山本式用の表はこの式には使えない（係数の意味が違うため）。現場データや2000年版指針の原本があれば別途確認すること。
- **先端抵抗F0（`10×1.32π×Bs×N'`）のπの有無問題は今回未解決のまま**。24章で採用したπありの式（`推力計算1350.pdf`館町雨水案件で誤差0.03%一致）と、xlsmの「協会式（自立地山）」ブランチのπなしの式（`10×1.32×Bs×N'`）が対立したままである。両者がそれぞれ別の地盤自立条件（基本式=非自立／協会式=自立）を指しており、`推力計算1350.pdf`がどちらの地盤条件だったか分かれば解消できる可能性がある。次回、地盤自立性に応じてF0側も切り替える設計にするかどうか、ユーザーと相談すること。

`service-worker.js`の`CACHE_NAME`を`v64`に更新。

---

## 62. 線形ビジュアル表示: 測量KP点のラベルを「N本目F/R」に修正、実測線・予測線を追加

### 62.1 発端

ユーザーから、基本情報タブ「📈線形ビジュアル表示」に重ね描きされる測量タブのKP点について3点の指摘を受けた。

1. ラベルが「2本目 KP1」のような表示になっており、本数の数え方が誤っている。KP1は正しくは「1本目前」（60章の予測点ロジックと同じ意味）であり、表示は測量本数を基準に「N本目F（前）／N本目R（後）」でよい。
2. 予測点（60章で実装済みの機能）がこの図には表示されていない。KPから予測点までを点線でつなぐ形にしてほしい。
3. 実測KP点どうしを結ぶ線を目立つ色の実線にしてほしい。

### 62.2 原因

`drawAlignmentVisual()`内の測量KP重ね描き処理（[M_APP_005.html:2074](M_APP_005.html)付近）は、本数を`Math.round((totalDist - r.distFromBP) / lp)`という距離ベースの独自の概算式で求めており、60章で確立した「KP1=測量本数本目の前面・KP2=同後面・KP3=1本前の前面・KP4=同後面（以降2点ごとに1本ずつ遡る）」という位置ベースの本数付与ロジック（`computeKpPrediction()`と同じ考え方）と整合していなかった。また、この処理は各KP点を独立した菱形マーカーとして描くだけで、点どうしを結ぶ線や予測点（`lastKpPrediction`、60章で実装済みのグローバル変数）の重ね描きを一切行っていなかった。

### 62.3 修正内容

- ラベルを`kpLabel()`関数で算出するように変更。`r.name`（"KP1"等）から番号を取り出し、`測量本数(surveyPipeCount) - Math.floor((kpIdx)/2)` を本数、`kpIdx%2===0`なら"F"・そうでなければ"R"として`"${本数}本目${F/R}"`を表示する。
- 実測KP点を測定順（KP1→KP2→…→KPn、先頭管前面から後方へ）に結ぶ実線を追加。計画線（緑=直線・青=曲線）と重複しない目立つ紫色`#9333ea`（`SURVEY_LINE_COLOR`）を使用。
- 予測点が存在する場合（KP4点以上入力済み）、KP1（先頭管前面＝現時点の管列最先端）から予測点まで同じ紫色の点線（`ctx.setLineDash([6,4])`）で結び、予測点自体も円マーカー＋ラベル（60章で確立済みの「(測量本数+1)本目予測」表記をそのまま使用）で重ね描きする。
- 判定ランク（OK/注意/NG）による色分けは実測KP点の菱形マーカー自体には引き続き適用し、線と予測点マーカーの色（紫）とは独立させた（判定色と「目立つ色」の意味が混ざらないようにするため）。

### 62.4 検証

ブラウザのJSコンソールから`kpRows`に4点分のダミー測量データ・`surveyPipeCount=5`を直接投入し`calcSurvey()`→`drawAlignmentVisual()`を実行して確認した。

- `surveyResults.map(kpLabel)`が`["5本目F","5本目R","4本目F","4本目R"]`と、意図通りの結果になることを確認。
- `lastKpPrediction.name`が`"6本目予測"`（測量本数5+1）であることを確認（60章の既存ロジックがそのまま使われている）。
- canvasの`getImageData()`でピクセルを走査し、紫`#9333ea`のピクセルが370px分描画されていること（＝実線・点線・予測点マーカーが実際に描かれていること）を確認。コンソールエラーなし。
- 本セッションはBrowserペインが非表示のためスクリーンショットでの目視確認はできていない。次回、実際の画面表示での最終確認が望ましい。

`service-worker.js`の`CACHE_NAME`を`v65`に更新。

---

以上。1〜62章の内容はすべてコミット・push済み（M_APP_028で実施、最新コミット`6aa40ba`、2026-08-11）。**2026-08-11、ユーザー指示によりM_APP_028からM_APP_029へ`robocopy`で複製し、本ドキュメントの冒頭（対象フォルダ名・最終更新日・「次回再開する人へ」・場所/保存先節）を更新した。以後の作業はM_APP_029側で行うこと。M_APP_028は複製元として残すが直接編集しない。次回さらに新しいバージョンが必要になったら、同じ手順でM_APP_029からM_APP_030へコピーすること。** **62章（線形ビジュアル表示の測量KPラベル修正・実測線/予測線）はBrowserペイン非表示のため実画面での目視確認がまだ済んでいない。次回、実機・実ブラウザでの確認を優先すること。** 続けて着手する場合は、まず**45.3節・46.4節・47.3節・47.4節・47.5節・47.6節・47.7節・47.8節・47.9節・47.10節・47.11節・47.12節・47.13節に残る「実機・実ブラウザでの目視確認」**から着手するとよい。**46章の教訓: Dropbox同期フォルダ内の大きいファイルは、Editツール成功表示だけでなくPowerShellでの実ファイル直接読み込みで反映を確認すること。****43章の教訓: ③がNGになった際は、まず入力した土質パラメータ（φ・土被り）が現場の地盤調査報告書と一致しているか疑うこと。式自体の疑義がある場合は、参考一次資料と項ごとに直接照合するのが最速。****42章の教訓: 一次資料（Excel）を過去に読んでいても、その式の細部（今回は「管内径/2」であって「管外径/2」ではない点）を自分の実装に本当に反映したかは別問題。一次資料を確認した = 実装も正しい、と思い込まないこと。ユーザーからの「数値が合わない」という指摘は、既存の一次資料と再度突き合わせる良いきっかけになる。****41章の教訓: キャンバスに文字を`fillText()`で積み上げる実装は、要素数が増えるたびに重なりリスクが上がる。説明文が2〜3行を超えるなら、最初からキャンバスの外（HTML要素）に出す設計にすること。この教訓は35〜38章で追加した各種ラベルにも当てはまっていた。****39章の教訓: ユーザーが「一次資料を調べて」と言った際は、単に内容を要約するだけでなく、既存実装の未確認事項（今回は38章末尾に明記していた検算待ちの部分）と直接照合し、一致すればその旨をはっきり報告すること。一次資料による裏付けが得られたかどうかは、次にコードを触る担当者が信頼して良い実装かどうかを左右する重要な情報。****38章の教訓: ユーザーから手書き図・写真ベースの仕様説明を受けたときは、文章の記述（「呼径よりH鋼の半分」等）が指す物理量（半径かそれとも別の量か）を早い段階で仮決めして式に落とし込み、既存の関連ロジック（今回はrenderJackStages・renderBackrockGapConcreteの両方が「R」を直接使っていた）をすべて洗い出してから一括で置き換えること。1箇所だけ直して他を直し忘れると、画面ごとに違う数字が出るバグになる。****37章の教訓: 抽象的な要求（「円形立坑は厚さ→幅を逆算」）から実装したロジックでも、後から実際の承認済み計算書という一次資料が出てきたら、それに合わせて特定条件（バックロック使用時）だけ挙動を反転させることを恐れないこと。カタログの「対象範囲」は絶対のルールではなく、実務では個別の構造計算（③④）で裏付けが取れれば範囲外採用もあり得るため、自動選定だけでなく手動上書きの余地を必ず残すこと。**36章の教訓: 円形立坑のwallBwはBeffの表示専用フィールドであり、どんな分岐であっても（バックロック使用有無を問わず）Beffと同期させ続けること。「特定の条件のときだけ同期する」という書き方は同期漏れの温床になる。****33章の教訓: 専門用語は業界での既存の意味と衝突していないか必ず確認すること（「多段ジャッキ」はテレスコピックジャッキ製品を指す既存語だった）。**区間別個別仕様（4.6節）まわりの入力欄を触るなら、**10章の教訓（`oninput`+innerHTML再描画の組み合わせを避ける）を必ず踏まえること**。線形ビジュアル表示・支圧壁寸法図・局所支圧照査など新しいcanvasを追加する場合は、**14.3節のCANVAS_REFRESH_FNSへの登録を忘れないこと**（局所支圧照査カード自体はcanvasを使わないため登録不要）。**プロジェクトのJSON保存/復元に新しい選択式UI（チップ・タブ等）を追加する場合は、20章の教訓（内部変数を直接書き換えるだけでなく、必ずUI更新関数経由で復元すること）を踏まえること。** **推進力計算のような数値ロジックを触る場合は、24.3節の教訓通り、既知の正解値（実際のPDF計算書等）と必ず突き合わせて検証すること — 今回も検証中に新しいバグ（Ce式のBe掛け忘れ）を自分で作り込んだが、同じ検証プロセスの中で即座に発見できた。** **このアプリのようにPWA的に`html,body{overflow:hidden}`で画面固定しているレイアウトで印刷機能を追加する場合は、25.2節の教訓通り、印刷対象要素だけでなく祖先要素の高さ制約・overflowもすべて解除すること。**
